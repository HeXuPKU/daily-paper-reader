---
title: "Tabular LLMs for Interpretable Few-Shot Alzheimer's Disease Prediction with Multimodal Biomedical Data"
title_zh: 用于多模态生物医学数据可解释少样本阿尔茨海默病预测的表格大语言模型
authors: "Sophie Kearney, Shu Yang, Zixuan Wen, Weimin Lyu, Bojian Hou, Duy Duong-Tran, Tianlong Chen, Jason H. Moore, Marylyn D. Ritchie, Chao Chen, Li Shen"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17191v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 用于阿尔茨海默病预测的领域自适应表格大语言模型
tldr: 针对阿尔茨海默病（AD）诊断中表格生物标志物数据规模小且不完整的挑战，本研究提出了TAP-GPT。该框架基于TableGPT2，通过表格提示词而非纯文本进行微调，实现了少样本下的多模态AD分类。实验表明，TAP-GPT在少样本场景下优于传统机器学习模型，且无需填补即可处理缺失数据，并能生成符合生物学逻辑的可解释推理，为临床决策支持系统奠定了基础。
selection_source: fresh_fetch
motivation: 旨在解决深度学习模型在处理小规模、不完整的阿尔茨海默病表格数据时表现不佳且缺乏可解释性的问题。
method: 提出了基于TableGPT2微调的TAP-GPT框架，利用表格化提示词对多模态生物标志物数据进行少样本分类预测。
result: TAP-GPT在少样本设定下超越了传统机器学习基线，在数据缺失情况下表现稳定，并能提供结构化的临床推理。
conclusion: 该研究证明了专门化的表格大语言模型在处理结构化临床预测任务中的有效性，为多智能体临床决策系统提供了新思路。
---

## 摘要
阿尔茨海默病 (AD) 的准确诊断需要处理表格形式的生物标志物数据，然而此类数据通常规模较小且不完整，深度学习模型在此类数据上的表现往往难以超越经典方法。预训练大语言模型 (LLMs) 具备少样本泛化、结构化推理和可解释输出的能力，为临床预测提供了强有力的范式转变。我们提出了 TAP-GPT（表格阿尔茨海默病预测 GPT），这是一个基于 TableGPT2 构建的领域自适应表格 LLM 框架，并针对少样本 AD 分类进行了微调，使用表格提示词而非纯文本。我们在四个源自 ADNI 的数据集上评估了 TAP-GPT，包括用于二分类 AD 分类的 QT-PAD 生物标志物以及区域级结构化 MRI、淀粉样蛋白 PET 和 tau 蛋白 PET。在多模态和单模态设置下，TAP-GPT 相比其骨干模型有所提升，并在少样本设置下优于传统机器学习基准，同时与最先进的通用 LLMs 相比保持了竞争力。我们展示了特征选择可以缓解高维输入带来的性能下降，并且 TAP-GPT 在模拟和真实缺失情况下无需插补即可保持稳定的性能。此外，TAP-GPT 能够生成与既定 AD 生物学相一致的结构化、模态感知的推理，并在自我反思下表现出更高的稳定性，支持其在迭代多智能体系统中的应用。据我们所知，这是表格专用 LLM 在基于多模态生物标志物的 AD 预测中的首次系统应用，证明了此类预训练模型可以有效处理结构化临床预测任务，并为表格 LLM 驱动的多智能体临床决策支持系统奠定了基础。源代码可在 GitHub 上公开获取：https://github.com/sophie-kearney/TAP-GPT。

## Abstract
Accurate diagnosis of Alzheimer's disease (AD) requires handling tabular biomarker data, yet such data are often small and incomplete, where deep learning models frequently fail to outperform classical methods. Pretrained large language models (LLMs) offer few-shot generalization, structured reasoning, and interpretable outputs, providing a powerful paradigm shift for clinical prediction. We propose TAP-GPT Tabular Alzheimer's Prediction GPT, a domain-adapted tabular LLM framework built on TableGPT2 and fine-tuned for few-shot AD classification using tabular prompts rather than plain texts. We evaluate TAP-GPT across four ADNI-derived datasets, including QT-PAD biomarkers and region-level structural MRI, amyloid PET, and tau PET for binary AD classification. Across multimodal and unimodal settings, TAP-GPT improves upon its backbone models and outperforms traditional machine learning baselines in the few-shot setting while remaining competitive with state-of-the-art general-purpose LLMs. We show that feature selection mitigates degradation in high-dimensional inputs and that TAP-GPT maintains stable performance under simulated and real-world missingness without imputation. Additionally, TAP-GPT produces structured, modality-aware reasoning aligned with established AD biology and shows greater stability under self-reflection, supporting its use in iterative multi-agent systems. To our knowledge, this is the first systematic application of a tabular-specialized LLM to multimodal biomarker-based AD prediction, demonstrating that such pretrained models can effectively address structured clinical prediction tasks and laying the foundation for tabular LLM-driven multi-agent clinical decision-support systems. The source code is publicly available on GitHub: https://github.com/sophie-kearney/TAP-GPT.

---

## 论文详细总结（自动生成）

这是一份关于论文《Tabular LLMs for Interpretable Few-Shot Alzheimer's Disease Prediction with Multimodal Biomedical Data》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：阿尔茨海默病（AD）的临床诊断高度依赖于表格形式的生物标志物数据（如 AT[N] 系统）。然而，这类数据通常具有**样本量小（Few-shot）**、**特征维度高**、**数据缺失严重**以及**缺乏可解释性**的特点。
*   **研究背景**：传统的深度学习模型在处理此类小规模表格数据时，表现往往不如随机森林或 XGBoost 等经典机器学习方法。虽然大语言模型（LLM）具备强大的推理和泛化能力，但直接将表格序列化为纯文本会丢失结构信息且效率低下。
*   **研究意义**：本研究旨在开发一种专门针对医疗表格数据优化的 LLM 框架，通过结合表格专用架构与领域知识，提升 AD 预测的准确性、鲁棒性及临床可解释性。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：提出 **TAP-GPT**（Tabular Alzheimer’s Prediction GPT），基于 **TableGPT2** 架构进行领域自适应。它不使用简单的文本序列化，而是利用 TableGPT2 的**语义表格编码器**来保持数据的结构化特征。
*   **关键技术细节**：
    *   **架构组成**：由一个固定的语义表格编码器和一个基于 Qwen2.5-7B 的解码器组成。
    *   **微调策略**：采用 **QLoRA**（量化低秩自适应）进行参数高效微调，仅更新解码器部分，注入 AD 诊断的领域知识。
    *   **提示词工程**：设计了四种提示格式（零样本/少样本 × 表格/序列化），重点在于**少样本表格提示（Few-Shot Tabular）**，即在提示词中包含 $k$ 个带标签的示例行和 1 个待预测的目标行。
    *   **特征选择**：针对高维影像数据，引入 LASSO 或 LLM 引导的特征排序，将特征数降至 $p$ 个，以适应 LLM 的上下文长度限制。
    *   **可解释性增强**：通过思维链（CoT）提示，要求模型输出 JSON 格式的结果，包含预测标签、推理过程和置信度评分。

### 3. 实验设计
*   **数据集**：源自 ADNI 数据库的四个数据集：
    1.  **QT-PAD**：包含 15 个临床标志物（PET、CSF、MRI、遗传、人口统计学）。
    2.  **区域影像数据**：包括结构化 MRI（皮层厚度/体积）、淀粉样蛋白 PET 和 Tau 蛋白 PET（各 74 个区域特征）。
*   **Benchmark 与对比方法**：
    *   **传统 ML**：逻辑回归（LR）、随机森林（RF）、SVM、XGBoost。
    *   **表格基础模型**：TabPFN。
    *   **通用 LLM**：Qwen2.5-7B-Instruct、Qwen3-8B、GPT-4o-mini。
    *   **消融对象**：未微调的 TableGPT2、TableGPT-R1。
*   **任务目标**：二分类任务（AD vs. 认知正常 CN）。

### 4. 资源与算力
*   **硬件环境**：使用高性能计算集群，配备 **NVIDIA A100 (80GB HBM)** GPU。
*   **内存与存储**：每个节点分配 160GB 系统 RAM。
*   **实验规模**：共执行了 **335 个实验变体**（涵盖不同模态、种子、提示格式和超参数组合），产生约 0.25TB 的模型检查点和适配器。
*   **训练细节**：使用 SLURM 调度器，每个任务占用 1 个 GPU 和 4 个 CPU 核心。

### 5. 实验数量与充分性
*   **实验组数**：非常充分。研究不仅对比了多种模型，还进行了深入的消融实验：
    *   **k-Ablation**：测试了 $k \in \{2, 4, \dots, 20\}$ 个示例对性能的影响。
    *   **p-Ablation**：测试了特征选择数量对高维影像数据的影响。
    *   **鲁棒性测试**：模拟了 10% 到 50% 的数据缺失，并测试了真实世界中的自然缺失数据。
    *   **稳定性测试**：引入“自我反思”步骤评估模型在迭代推理下的稳定性。
*   **公平性**：使用了 **10 个随机种子**进行重复实验，并对所有基准模型进行了超参数优化（使用 Optuna）。

### 6. 主要结论与发现
*   **性能领先**：TAP-GPT 在少样本设定下显著优于传统机器学习模型和 TabPFN，尤其是在多模态 QT-PAD 数据集上（F1 分数达到 0.89）。
*   **结构化优势**：表格化提示词在少样本场景下优于纯文本序列化，证明了 TableGPT2 编码器捕捉结构信息的能力。
*   **缺失值鲁棒性**：TAP-GPT 在无需数据插补的情况下，对高达 40% 的模拟缺失和真实自然缺失表现出极强的韧性。
*   **推理一致性**：相比 GPT-4o-mini，TAP-GPT 在“自我反思”实验中表现出更高的预测稳定性，更适合集成到多智能体临床决策系统中。

### 7. 优点
*   **领域自适应**：成功将通用表格 LLM 转化为专业的医疗诊断工具。
*   **端到端处理缺失值**：打破了传统模型必须先插补后预测的局限。
*   **高可解释性**：生成的推理过程符合 AD 生物学逻辑（如提及海马体萎缩、APOE4 风险等），增强了临床信任。
*   **特征选择策略**：系统地解决了 LLM 处理高维表格数据时的上下文溢出问题。

### 8. 不足与局限
*   **架构依赖**：由于 TableGPT2 的固定架构，难以直接将解码器升级为更新、更强的模型（如 Qwen3）。
*   **推理幻觉风险**：定性分析发现，模型偶尔会误解特征的方向性（例如误认为 FDG 升高是 AD 迹象）或引用错误的行数据。
*   **任务局限**：目前仅限于二分类（AD vs. CN），尚未扩展到多分类（如 MCI 预测）或回归任务。
*   **计算成本**：虽然推理时是少样本，但微调过程仍需要 A100 等高端算力支持。

（完）
