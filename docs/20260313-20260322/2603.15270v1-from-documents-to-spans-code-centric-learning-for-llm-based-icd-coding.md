---
title: "From Documents to Spans: Code-Centric Learning for LLM-based ICD Coding"
title_zh: 从文档到片段：基于大语言模型的以代码为中心的 ICD 编码学习
authors: "Xu Zhang, Wenxin Ma, Chenxu Wu, Rongsheng Wang, Kun Zhang, S. Kevin Zhou"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15270v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 基于LLM的临床文档ICD编码
tldr: 针对ICD编码中代码覆盖有限、缺乏可解释性及长文本微调成本高的问题，本文提出了一种以代码为中心的学习框架（CCL）。该框架将监督重点从完整临床文档转向可扩展的短证据片段，通过混合训练策略和数据扩展，显著降低了训练成本，提升了模型对未知代码的泛化能力和可解释性。实验表明，该方法使小型开源LLM达到了与大型闭源模型相当的性能。
selection_source: fresh_fetch
motivation: 现有的ICD编码微调面临代码覆盖不足、缺乏证据支撑导致的可解释性差以及长文档处理成本高昂的挑战。
method: 提出以代码为中心的学习框架，通过短证据片段进行监督，并结合混合训练策略与数据扩展来优化模型。
result: 该方法在保持可解释性的同时，显著提升了对罕见代码的预测准确率，并使小规模模型表现出媲美巨型商业模型的性能。
conclusion: 将学习重心从文档级转向片段级是提升LLM在医疗编码任务中效率、泛化性和透明度的有效途径。
---

## 摘要
ICD 编码是医疗保健领域一项关键且具有挑战性的任务。最近，基于大语言模型（LLM）的方法在 ICD 编码中表现出比判别式方法更强的泛化能力。然而，针对 ICD 编码微调 LLM 面临三个主要挑战：首先，现有的公开 ICD 编码数据集对 ICD 代码空间的覆盖有限，限制了模型泛化到未见代码的能力；其次，朴素的微调会降低 LLM 的可解释性，因为很少有公开数据集包含所分配代码的明确支持证据；第三，ICD 编码通常涉及冗长的临床文档，使得微调 LLM 的计算成本很高。为了解决这些问题，我们提出了“以代码为中心的学习”（Code-Centric Learning），这是一种将监督从完整的临床文档转向可扩展的短证据片段的训练框架。该框架的核心思想是，片段级学习能够提高 LLM 执行文档级 ICD 编码的能力。我们提出的框架由混合训练策略和以代码为中心的数据扩展组成，这显著降低了训练成本，提高了在未见 ICD 代码上的准确性，并保留了可解释性。在相同的 LLM 主干网络下，我们的方法显著优于强基准模型。值得注意的是，我们的方法使小规模 LLM 能够达到与大得多的私有模型相当的性能，证明了其在全自动 ICD 编码方面的有效性和潜力。

## Abstract
ICD coding is a critical yet challenging task in healthcare. Recently, LLM-based methods demonstrate stronger generalization than discriminative methods in ICD coding. However, fine-tuning LLMs for ICD coding faces three major challenges. First, existing public ICD coding datasets provide limited coverage of the ICD code space, restricting a model's ability to generalize to unseen codes. Second, naive fine-tuning diminishes the interpretability of LLMs, as few public datasets contain explicit supporting evidence for assigned codes. Third, ICD coding typically involves long clinical documents, making fine-tuning LLMs computationally expensive. To address these issues, we propose Code-Centric Learning, a training framework that shifts supervision from full clinical documents to scalable, short evidence spans. The key idea of this framework is that span-level learning improves LLMs' ability to perform document-level ICD coding. Our proposed framework consists of a mixed training strategy and code-centric data expansion, which substantially reduces training cost, improves accuracy on unseen ICD codes and preserves interpretability. Under the same LLM backbone, our method substantially outperforms strong baselines. Notably, our method enables small-scale LLMs to achieve performance comparable to much larger proprietary models, demonstrating its effectiveness and potential for fully automated ICD coding.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**以代码为中心的学习（Code-Centric Learning, CCL）**的微调框架，旨在解决大语言模型（LLM）在自动 ICD 编码任务中面临的效率、泛化性和可解释性三大挑战。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：ICD 编码是将临床文档转化为标准化疾病代码的过程，对医疗计费和数据分析至关重要。
*   **核心挑战**：
    1.  **代码覆盖有限**：公开数据集（如 MIMIC）仅覆盖约 10% 的 ICD 代码，导致模型难以处理罕见或未见代码。
    2.  **缺乏可解释性**：标准微调仅让模型输出代码，而不提供临床证据支撑。
    3.  **训练效率低下**：临床文档通常极长，直接微调 LLM 的计算成本呈平方级增长。
*   **论文意义**：提出将监督信号从“长文档”转向“短证据片段（Spans）”，通过片段级学习带动文档级编码能力。

### 2. 方法论
核心思想是 **CCL 框架**，包含两个关键策略：
*   **混合训练策略（Mixed Training）**：
    *   模型同时在两种数据上训练：一种是带有证据标注的完整文档（极少量），另一种是“证据片段-代码”对（大量）。
    *   **公式化表达**：损失函数结合了文档级监督 $L_{SFT}(f_\theta(x), (E, C))$ 和片段级监督 $L_{SFT}(f_\theta(e), c)$。这迫使模型在分配代码前先定位证据。
*   **以代码为中心的数据扩展（Code-Centric Data Expansion）**：
    1.  **金牌数据（Gold）**：从官方 ICD 索引和列表提取术语与代码对。
    2.  **银牌数据（Silver）**：利用 LLM 从噪声较大的公开数据集中提取并总结代表性证据片段。
    3.  **合成数据（Synthetic）**：针对完全未见的代码，利用 LLM 根据 ICD 层级结构中的邻近代码推断并合成合理的证据片段，实现 100% 的代码覆盖。

### 3. 实验设计
*   **数据集**：
    *   **MIMIC-IV**：作为大规模噪声训练源。
    *   **MDACE**：专家标注的证据片段数据集，用于评估可解释性和准确性。
    *   **ACI-Bench**：合成的临床笔记，作为分布外（OOD）测试集评估泛化性。
*   **Benchmark 与对比方法**：
    *   **判别式模型**：PLM-ICD, PLM-CA, GKI-ICD（基于 RoBERTa）。
    *   **零样本/少样本 LLM**：CoT, MAC, CLH（基于 GPT-4 或大型开源模型）。
    *   **微调 LLM**：标准的文档级 SFT。
*   **主干网络**：Llama-3.1-8B, Qwen3-4B 等。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA H20 GPU (96GB VRAM)**。
*   **训练效率**：
    *   传统 SFT（10万文档）：需要约 **210 小时**。
    *   CCL 方法（200文档 + 15万片段）：仅需约 **40 小时**。
    *   **结论**：训练时间缩短了约 **80%**（效率提升 5.2 倍）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.5B 到 8B 不同参数规模的多个模型家族（Llama, Qwen）。
*   **消融实验**：详细分析了证据标注、金牌/银牌/合成数据对性能的贡献。
*   **评估维度**：不仅测试了 Micro/Macro-F1 准确率，还通过 GPT-4 评估了证据提取的召回率（Recall），并进行了人类医生评估。
*   **客观性**：在分布外数据集（ACI-Bench）上的表现证明了模型并非死记硬背，而是真正理解了代码逻辑。

### 6. 主要结论与发现
*   **性能飞跃**：在相同主干网络下，CCL 显著优于传统 SFT。8B 规模的模型在 CCL 赋能下能媲美参数量大得多的私有模型。
*   **未见代码的泛化**：对于文档训练集中未出现的代码，准确率从 8.8% 提升至 **50.0%**。
*   **可解释性**：模型能够生成与人类专家高度一致的证据片段（证据召回率达 74%）。
*   **人机协作**：实验显示，若由人类修正证据，模型的编码准确率（Micro-F1）可从 59.3 飙升至 78.0。

### 7. 优点
*   **高效性**：通过将长文本转化为短片段，极大降低了 LLM 进入医疗领域的算力门槛。
*   **全覆盖**：解决了医疗 AI 中长期存在的“长尾代码”覆盖难题。
*   **透明度**：强制模型输出证据，符合医疗场景对可解释性的刚需。
*   **灵活性**：支持人机协同工作流，允许临床医生介入纠偏。

### 8. 不足与局限
*   **合成数据质量**：人类评估显示，合成证据在“临床真实性”上仍有提升空间（约 42% 的样本被认为真实性一般）。
*   **复杂逻辑处理**：目前的片段学习主要针对直接映射，对于 ICD 准则中复杂的“排除规则”或“组合编码逻辑”涉及较少。
*   **语言限制**：实验主要集中在英文数据集，跨语言（如中文）的迁移效果尚待验证。

（完）
