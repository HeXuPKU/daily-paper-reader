---
title: "EviAgent: Evidence-Driven Agent for Radiology Report Generation"
title_zh: EviAgent：证据驱动的放射学报告生成智能体
authors: "Tuoshi Qi, Shenshen Bu, Yingfei Xiang, Zhiming Dai"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13956v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 放射科证据驱动智能体
tldr: 针对多模态大模型在放射报告生成中存在的黑盒决策和缺乏外部知识等问题，本文提出了证据驱动的智能体EviAgent。该方法通过将复杂的生成过程分解为细粒度的操作单元，并整合多维视觉专家与检索机制，实现了透明的推理轨迹和显式的视觉证据支持。在多个主流数据集上的实验证明，EviAgent在性能和可信度上均优于现有模型，为临床应用提供了更具解释性的方案。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在生成放射报告时面临决策过程不透明、缺乏显式视觉证据以及难以获取外部领域知识的挑战。
method: 提出EviAgent框架，通过将生成过程分解为细粒度操作单元，并集成多维视觉专家与检索机制来提供显式证据和临床先验。
result: 在MIMIC-CXR、CheXpert Plus和IU-Xray数据集上的实验结果显示，EviAgent的性能显著优于通用大模型和专门的医疗模型。
conclusion: EviAgent通过透明的推理轨迹和证据驱动机制，为自动化放射报告生成提供了一种鲁棒且可信的解决方案。
---

## 摘要
自动放射学报告生成在减轻放射科医生沉重工作量方面具有巨大潜力。尽管近期的多模态大语言模型（MLLMs）具备强大的视觉语言能力，但其临床部署受到固有局限性的严重制约：其“黑盒”决策机制由于缺乏支持诊断的显式视觉证据，导致生成的报告不可追溯，且难以获取外部领域知识。为了应对这些挑战，我们提出了证据驱动的放射学报告生成智能体（EviAgent）。与不透明的端到端范式不同，EviAgent 通过将复杂的生成过程分解为细粒度的操作单元，协调出透明的推理轨迹。我们集成了多维视觉专家和检索机制作为外部支持模块，赋予系统显式的视觉证据和高质量的临床先验。在 MIMIC-CXR、CheXpert Plus 和 IU-Xray 数据集上的广泛实验表明，EviAgent 的性能优于大规模通用模型和专业医学模型，为自动放射学报告生成提供了一种稳健且值得信赖的解决方案。

## Abstract
Automated radiology report generation holds immense potential to alleviate the heavy workload of radiologists. Despite the formidable vision-language capabilities of recent Multimodal Large Language Models (MLLMs), their clinical deployment is severely constrained by inherent limitations: their "black-box" decision-making renders the generated reports untraceable due to the lack of explicit visual evidence to support the diagnosis, and they struggle to access external domain knowledge. To address these challenges, we propose the Evidence-driven Radiology Report Generation Agent (EviAgent). Unlike opaque end-to-end paradigms, EviAgent coordinates a transparent reasoning trajectory by breaking down the complex generation process into granular operational units. We integrate multi-dimensional visual experts and retrieval mechanisms as external support modules, endowing the system with explicit visual evidence and high-quality clinical priors. Extensive experiments on MIMIC-CXR, CheXpert Plus, and IU-Xray datasets demonstrate that EviAgent outperforms both large-scale generalist models and specialized medical models, providing a robust and trustworthy solution for automated radiology report generation.

---

## 论文详细总结（自动生成）

以下是对论文《EviAgent: Evidence-Driven Agent for Radiology Report Generation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLMs）在自动生成放射学报告时存在两个致命缺陷：
    1.  **“黑盒”决策**：生成过程不透明，诊断结论缺乏显式的视觉证据支持，导致报告不可追溯，难以在临床部署。
    2.  **知识局限**：模型主要依赖内部参数化知识，难以实时获取外部专业的临床先验或领域知识。
*   **研究背景**：放射科医生工作量巨大，自动化报告生成需求迫切。虽然通用模型（如 GPT 系列）和医疗专用模型（如 MedGemma）取得了一定进展，但仍面临事实性错误和缺乏临床可信度的问题。

### 2. 论文提出的方法论
EviAgent 提出了一个**证据驱动的智能体框架**，将“像素到文本”的端到端映射转变为“计划-行动-报告”的结构化推理轨迹。
*   **核心思想**：通过协调多个视觉专家工具和检索机制，主动获取显式证据，并基于这些证据合成报告。
*   **关键技术流程**：
    1.  **规划与分解 (Planning)**：Planner（基于 Qwen3-VL-8B）将复杂的诊断任务分解为细粒度的操作单元（如“先检测病变，再定位分类结果”）。
    2.  **工具增强的 ReAct 循环**：智能体动态调用工具箱。工具包括：
        *   **感知专家**：Swin-Transformer（疾病分类）、CheXagent（体位识别）、MAIRA-2（视觉定位/Grounding）、MedSAM（解剖分割）。
        *   **检索增强知识 (RAG)**：从 MIMIC-CXR 提取 14 类病理知识库，利用多模态嵌入模型（GME-Qwen2-VL）检索相似历史案例。
    3.  **证据提取 (Evidence Extraction)**：从冗长的交互历史中过滤噪声，提取分类概率、坐标框和临床先验，构建结构化证据链。
    4.  **输出生成 (Output)**：严格基于提取的证据链合成最终报告，确保结论“有据可查”。
*   **公式/算法细节**：在 RAG 模块中，通过计算查询向量 $q_c$（图像+病理标签）与知识库向量 $v_{c,j}$ 的余弦相似度来检索 Top-k 相关的参考报告。

### 3. 实验设计
*   **数据集**：
    *   **MIMIC-CXR**：官方测试集（2,347 份报告，3,858 张图像）。
    *   **IU-Xray**：标准测试集（590 份报告）。
    *   **CheXpert Plus**：官方测试集（234 份报告）。
*   **Benchmark 指标**：
    *   **RaTEScore**：关注医学实体和解剖细节的评分。
    *   **SembScore**：基于 14 种病理指标的临床一致性评分。
    *   **RadCliQ⁻¹**：结合词汇、语义和临床关系图的综合指标。
*   **对比方法**：
    *   **闭源通用模型**：GPT-5.1, Claude 4.5 Sonnet, Gemini-2.5-Flash。
    *   **医疗专用模型**：LLaVA-Med, HuatuoGPT-V, MedGemma, Lingshu-7B。
    *   **开源通用模型**：InternVL 系列, Qwen-VL 系列。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA H100 GPU** 上运行。
*   **推理引擎**：使用 **vLLM** 高吞吐推理框架，以确保本地部署的效率和隐私合规。
*   **训练时长**：文中未明确提及训练时长，因为 EviAgent 核心是一个**无需额外训练 (Training-free)** 的智能体框架，主要依赖于预训练模型的推理和工具集成。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了全量测试，并针对 1,000 个实例进行了智能体能力（VR, Tool Call 等）的专项评估。
*   **消融实验**：做了多组消融实验，分别验证了分类专家、定位专家、检索模块、规划机制以及证据提取模块对最终性能的贡献。
*   **主观评估**：引入了 **LLM-as-a-Judge**（使用 DeepSeek-V3.2），从准确性、定位精度、专业性和临床采纳度四个维度进行评分。
*   **充分性评价**：实验设计非常充分且客观。通过对比不同规模、不同领域的模型，并结合自动指标与大模型评测，全面验证了框架的有效性。

### 6. 论文的主要结论与发现
*   **性能领先**：EviAgent 在几乎所有指标上均优于现有的闭源巨头和医疗专用模型。例如，在 MIMIC-CXR 上，其 SembScore 远超 Lingshu-7B。
*   **证据的重要性**：消融实验显示，移除分类专家会导致性能剧烈下降（甚至低于基座模型），证明了准确的“诊断锚点”是后续推理的基础。
*   **可追溯性验证**：定性分析证明，即使出现诊断偏差，也可以通过证据日志追溯到具体的感知工具错误，而非模型幻觉。

### 7. 优点（亮点）
*   **透明度与可信度**：通过显式的证据链解决了医疗 AI 的“黑盒”问题，符合临床诊断逻辑。
*   **本地化与隐私**：系统完全基于开源模型和本地工具构建，无需调用云端 API，保护了敏感的医疗数据隐私。
*   **高度可扩展**：采用 Model Context Protocol (MCP)，支持“即插即用”地集成医院现有的私有化工具。
*   **性能卓越**：在不进行额外参数微调的情况下，通过流程优化超越了经过大量指令微调的专用模型。

### 8. 不足与局限
*   **推理延迟**：由于采用了多轮交互的智能体工作流，其生成报告的时间显著长于端到端的单次推理模型。
*   **误差传递风险**：系统高度依赖底层感知工具（如分类器和定位模型）的准确性，如果前端工具产生错误证据，可能会误导 Planner 产生错误的诊断结论。
*   **计算开销**：运行多个专家模型（如 MedSAM, MAIRA-2 等）对显存和计算资源有一定要求。

（完）
