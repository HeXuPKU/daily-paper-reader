---
title: "SCALE:Scalable Conditional Atlas-Level Endpoint transport for virtual cell perturbation prediction"
title_zh: SCALE：用于虚拟细胞扰动预测的可扩展条件图谱级终点传输
authors: "Shuizhou Chen, Lang Yu, Kedu Jin, Songming Zhang, Hao Wu, Wenxuan Huang, Sheng Xu, Quan Qian, Qin Chen, Lei Bai, Siqi Sun, Zhangyang Gao"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17380v2"
tags: ["query:med-ai"]
score: 9.0
evidence: 用于虚拟细胞扰动预测的大规模基础模型
tldr: SCALE是一个针对虚拟细胞扰动预测的大规模基础模型，旨在解决现有模型在训练效率、建模稳定性和生物学评估方面的局限。它基于BioNeMo框架提升了扩展性，采用条件传输与LLaMA编码器结合的架构，并在Tahoe-100M基准上通过生物学指标验证了其优越性，显著提升了预测精度和计算效率。
selection_source: fresh_fetch
motivation: 现有的虚拟细胞扰动预测面临训练推理效率低、高维稀疏空间建模不稳定以及评估指标缺乏生物学忠实度的问题。
method: 提出SCALE模型，结合BioNeMo分布式框架、基于LLaMA的细胞编码器以及面向端点的条件传输流架构。
result: "在Tahoe-100M基准上，预训练速度提升12.51倍，PDCorr和DE Overlap指标分别比现有SOTA提升了12.02%和10.66%。"
conclusion: 推进虚拟细胞研究需要协同设计可扩展的基础设施、稳定的传输建模以及符合生物学意义的评估体系。
---

## 摘要
虚拟细胞模型旨在通过单细胞测量数据预测细胞对遗传、化学或细胞因子扰动的反应，从而实现干法实验（in silico experimentation）。然而在实践中，大规模扰动预测仍受限于三个相互关联的瓶颈：低效的训练和推理流水线、高维稀疏表达空间中不稳定的建模，以及过度强调重建准确性而低估生物学保真度的评估协议。在这项工作中，我们提出了一个专门用于虚拟细胞扰动预测的大规模基础模型 SCALE，共同解决了上述局限性。首先，我们构建了一个基于 BioNeMo 的训练和推理框架，显著提高了数据吞吐量、分布式可扩展性和部署效率，在相同系统设置下，其预训练速度比之前的 SOTA 流水线提升了 12.51 倍，推理速度提升了 1.29 倍。其次，我们将扰动预测表述为条件传输，并利用一种集合感知流架构来实现，该架构将基于 LLaMA 的细胞编码与面向终点的监督相结合。这种设计带来了更稳定的训练和更强的扰动效应恢复能力。第三，我们在 Tahoe-100M 上使用严格的细胞级协议对模型进行评估，该协议以具有生物学意义的指标为中心，而非仅仅关注重建。在该基准测试中，我们的模型相比 STATE 将 PDCorr 提高了 12.02%，DE Overlap 提高了 10.66%。总之，这些结果表明，推进虚拟细胞不仅需要更好的生成目标，还需要可扩展基础设施、稳定传输建模和生物学忠实评估的协同设计。

## Abstract
Virtual cell models aim to enable in silico experimentation by predicting how cells respond to genetic, chemical, or cytokine perturbations from single-cell measurements. In practice, however, large-scale perturbation prediction remains constrained by three coupled bottlenecks: inefficient training and inference pipelines, unstable modeling in high-dimensional sparse expression space, and evaluation protocols that overemphasize reconstruction-like accuracy while underestimating biological fidelity. In this work we present a specialized large-scale foundation model SCALE for virtual cell perturbation prediction that addresses the above limitations jointly. First, we build a BioNeMo-based training and inference framework that substantially improves data throughput, distributed scalability, and deployment efficiency, yielding 12.51* speedup on pretrain and 1.29* on inference over the prior SOTA pipeline under matched system settings. Second, we formulate perturbation prediction as conditional transport and implement it with a set-aware flow architecture that couples LLaMA-based cellular encoding with endpoint-oriented supervision. This design yields more stable training and stronger recovery of perturbation effects. Third, we evaluate the model on Tahoe-100M using a rigorous cell-level protocol centered on biologically meaningful metrics rather than reconstruction alone. On this benchmark, our model improves PDCorr by 12.02% and DE Overlap by 10.66% over STATE. Together, these results suggest that advancing virtual cells requires not only better generative objectives, but also the co-design of scalable infrastructure, stable transport modeling, and biologically faithful evaluation.

---

## 论文详细总结（自动生成）

### 论文总结：SCALE —— 用于虚拟细胞扰动预测的可扩展条件图谱级终点传输

#### 1. 核心问题与整体含义（研究动机和背景）
虚拟细胞模型的核心目标是通过单细胞测序数据，预测细胞在受到遗传（基因敲除）、化学（药物）或细胞因子干预后的状态变化。然而，现有模型面临三大瓶颈：
*   **效率低下**：在大规模图谱级数据上的训练和推理速度慢，难以扩展。
*   **建模不稳定**：单细胞数据具有高维、稀疏和异质性，直接在表达空间建模容易失败。
*   **评估偏差**：现有评估协议过度关注均方误差（MSE）等重建指标，而忽视了生物学上的真实性（如差异表达基因的恢复）。
**SCALE** 的提出旨在通过协同设计基础设施、传输建模和生物学评估体系，解决这些耦合的局限性。

#### 2. 方法论：核心思想与关键技术
SCALE 将扰动预测视为**条件传输（Conditional Transport）**问题，核心思想是在集合感知的潜空间中学习从对照组（Control）到扰动组（Perturbed）的状态转移。

*   **分层集合感知编码器（Hierarchical Set-Aware Encoder）**：
    *   **基因级编码**：采用 LLaMA 风格的注意力机制处理单细胞内的基因依赖关系。
    *   **细胞级聚合**：利用 DeepSets 结构捕捉细胞群体（Set）的分布特征，确保对细胞顺序的置换不变性。
*   **条件 JiT 流传输网络（Conditional JiT-based Velocity Field）**：
    *   **端点对齐监督**：不同于传统的扩散模型预测中间速度，SCALE 采用 JiT（Just-in-Time）参数化，直接预测扰动后的终点状态（x-pred/x-loss），这更符合单细胞数据“只有始末状态、无中间轨迹”的特性。
    *   **条件注入**：通过 Seed Attention 机制，将细胞类型、扰动类型和批次信息自适应地聚合到细胞表征中。
*   **损失函数**：结合了 MSE 重建损失和 MMD（最大均值差异）分布匹配损失，以平衡个体细胞的准确性和群体分布的一致性。

#### 3. 实验设计
*   **数据集**：
    *   **Tahoe-100M**：超大规模化学扰动图谱（>1亿细胞，1100+处理条件）。
    *   **PBMC**：细胞因子刺激响应数据集（90种扰动，18种细胞类型）。
    *   **Replogle-Nadig**：大规模遗传（CRISPRi）扰动数据集。
*   **对比方法（Benchmarks）**：包括 STATE（前 SOTA）、PerturbDiff、CPA、Linear（线性基准）、CellFlow、Squidiff 等。
*   **评估指标**：侧重生物学意义，如 PDCorr（扰动相关性）、DE Overlap（差异表达基因重叠度）、LFC Spearman（对数倍数变化相关性）等。

#### 4. 资源与算力
*   **框架支持**：模型构建在 NVIDIA **BioNeMo** 框架之上，利用分布式执行和高效的 I/O 处理（LMDB 分片存储）。
*   **算力表现**：论文提到在匹配的系统设置下，SCALE 实现了 **12.51 倍的预训练加速**和 **1.29 倍的推理加速**。
*   **具体硬件**：文中未明确列出具体的 GPU 型号（如 A100 或 H100）及具体卡数，但强调了其在处理 Tahoe-100M 这种亿级参数/数据规模时的扩展能力。

#### 5. 实验数量与充分性
*   **实验规模**：在三个具有代表性的生物学场景（化学、细胞因子、遗传）下进行了全面测试。
*   **消融实验**：非常充分，涵盖了条件融合方式（Mean vs. Seed Attention）、JiT 训练变体（x-pred vs. v-pred）、先验分布设计（Gaussian vs. Control-anchored）等多个维度。
*   **模型缩放（Scaling Study）**：对比了 184M 和 280M 参数规模的模型，探讨了性能饱和点。
*   **客观性**：通过严格的“Cell-Eval”协议进行评估，并指出了当前领域内评估代码实现细节对结果的影响，表现出较高的学术严谨性。

#### 6. 主要结论与发现
*   **性能领先**：在 Tahoe-100M 上，SCALE 比 STATE 在 PDCorr 上提升了 12.02%，在 DE Overlap 上提升了 10.66%。
*   **指标解耦**：模型在生物学指标（PDCorr）上表现优异，但在 MSE 上可能不如某些基准。作者认为这是因为 MSE 容易陷入“均值陷阱”，而 SCALE 更注重捕捉扰动诱导的异质性变化。
*   **端点监督更优**：在单细胞扰动场景下，直接预测终点（x-pred）比预测速度场（v-pred）更稳定且效果更好。
*   **规模饱和**：在当前数据制度下，模型性能在 184M 参数附近出现平台期，单纯增加参数量不一定能带来生物学指标的持续提升。

#### 7. 优点（亮点）
*   **端到端高效性**：通过 BioNeMo 解决了大规模单细胞数据的吞吐瓶颈。
*   **架构创新**：将 LLaMA 架构与 DeepSets 结合，既保留了基因关联建模能力，又尊重了细胞集合的对称性。
*   **任务适配**：针对单细胞数据“无配对轨迹”的特点，设计了端点对齐的传输机制，避免了不必要的中间状态模拟。

#### 8. 不足与局限
*   **重建指标表现一般**：由于不追求拟合所有背景基因的平均值，其在 MSE/MAE 等传统回归指标上可能不占优势。
*   **评估敏感性**：作者承认 Cell-Eval 协议对预处理和实现细节非常敏感，不同框架下的复现结果可能存在波动。
*   **因果性限制**：虽然模型在预测上很强，但对于扰动背后的深层基因调控网络（GRN）因果机制的显式解释仍有提升空间。

（完）
