---
title: Uncertainty-Aware Foundation Models for Clinical Data
title_zh: 临床数据的不确定性感知基础模型
authors: "Qian Zhou, Yuanyun Zhang, Shi Li"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04175v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 临床数据和电子健康记录的不确定性感知基础模型
tldr: 针对临床数据稀疏、不规则且不完整的特性，本文提出了一种不确定性感知的基础模型框架。该方法不再将患者表示为单一的点嵌入，而是将其建模为潜在状态的分布。通过结合多模态编码器和自监督学习目标（如对比对齐和分布正则化），模型能够显式编码认知不确定性。实验证明，该方法在多种临床任务中提升了预测性能、缺失数据下的鲁棒性以及不确定性校准能力，为医疗AI提供了更可靠的表示学习方案。
source: arxiv
selection_source: fresh_fetch
motivation: 传统的临床基础模型多采用确定性表示，难以处理医疗数据中固有的不完整性和生理状态测量的不确定性。
method: 提出一种将患者表示为潜在状态分布的框架，利用多模态编码器和自监督目标（重构、对比对齐、分布正则化）来捕捉认知不确定性。
result: 在多项临床任务中，该方法在预测性能、对缺失数据的鲁棒性以及不确定性校准方面均优于强基准模型。
conclusion: 建模“未观察到的信息”而非仅关注“已观察到的信息”，是构建医疗基础模型时至关重要的归纳偏置。
---

## 摘要
医疗基础模型在很大程度上遵循了自然语言处理和计算机视觉的范式，强调在异构临床数据上进行大规模预训练和确定性表示。然而，临床观察本质上是不完整的，反映了对潜在生理状态的稀疏、不规则且依赖于模态的测量。在这项工作中，我们提出了一个不确定性感知的基础建模框架，该框架不将每个患者表示为点嵌入，而是表示为合理潜在状态上的分布。通过学习集值表示并强制同一患者的部分视图之间保持一致性，该模型在显式编码认知不确定性的同时，捕捉了不变的可推断信息。我们将这一公式与多模态编码器和可扩展的自监督目标相结合，融合了重建、对比对齐和分布正则化。在多种临床任务中，与强基准模型相比，我们的方法提高了预测性能、缺失数据下的鲁棒性以及不确定性校准。这些结果表明，建模未观察到的内容而非仅仅是已观察到的内容，构成了医疗基础模型的一个关键归纳偏置。

## Abstract
Healthcare foundation models have largely followed paradigms from natural language processing and computer vision, emphasizing large scale pretraining and deterministic representations over heterogeneous clinical data. However, clinical observations are inherently incomplete, reflecting sparse, irregular, and modality dependent measurements of an underlying physiologic state. In this work, we propose a framework for uncertainty aware foundation modeling that represents each patient not as a point embedding, but as a distribution over plausible latent states. By learning set valued representations and enforcing consistency across partial views of the same patient, the model captures what is invariantly inferable while explicitly encoding epistemic uncertainty. We integrate this formulation with multimodal encoders and scalable self supervised objectives, combining reconstruction, contrastive alignment, and distributional regularization. Across diverse clinical tasks, our approach improves predictive performance, robustness under missing data, and uncertainty calibration relative to strong baselines. These results suggest that modeling what is not observed rather than only what is constitutes a critical inductive bias for healthcare foundation models.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对临床数据的不确定性感知基础模型框架，旨在解决医疗数据固有的稀疏性和不完整性问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的医疗基础模型（借鉴自NLP和CV领域）通常将临床数据视为确定性的标记序列，并学习单一的点嵌入（Point Embedding）。然而，临床观察（如化验、影像、病程记录）本质上是**结构性欠定**的——它们只是底层生理状态的稀疏、不规则且不完整的片段。
*   **研究动机**：将不完整的观察强行编码为确定性表示会混淆“认知不确定性”与“特征表示”。论文认为，医疗AI不应只建模“已观察到的”，更应具备推理“未观察到的但合理的生理状态空间”的能力。

### 2. 论文提出的方法论
*   **核心思想**：将每个患者表示为潜在状态空间上的**分布** $q_\theta(z|x)$（集值表示），而非单一向量。
*   **关键技术细节**：
    *   **分布参数化**：使用编码器输出潜在高斯分布的均值 $\mu$ 和协方差 $\Sigma$。
    *   **多模态融合**：采用模态特定的编码器（Transformer、ViT等），通过**掩码注意力池化（Masked Attention Pooling）**聚合不同模态，对缺失模态具有天然的鲁棒性。
    *   **部分视图一致性（Partial-View Consistency）**：强制同一患者的不同数据子集（如不同时间段或不同模态组合）生成的潜在分布保持一致，使用对称KL散度或Wasserstein距离作为损失函数。
    *   **自监督预训练目标**：结合了重构损失（Masked Modeling）、一致性损失（Consistency）、正则化（防止分布崩溃）和对比学习（InfoNCE），以增强患者间的区分度。
    *   **下游预测**：通过对潜在分布进行蒙特卡洛采样并边缘化处理，将表示的不确定性传播到最终的临床预测中。

### 3. 实验设计
*   **数据集**：基于 **MIMIC-III 和 MIMIC-IV** 数据库，涵盖了结构化EHR、临床文本、影像和生理波形（ECG/PPG）。
*   **实验场景**：
    1.  **二分类**：住院死亡率、再入院预测。
    2.  **多分类**：诊断预测。
    3.  **回归**：住院时长（LoS）。
    4.  **生存分析**：时间到事件风险估计。
*   **对比基准（Benchmarks）**：
    *   **MAE风格模型**：基于重构目标的掩码自编码器。
    *   **对比学习模型**：基于实例级对齐的方法。
    *   **自回归模型**：类似于GPT的序列建模方法。
    *   **消融版本**：本方法的确定性版本（Ours-deterministic）。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 AdamW 优化器、余弦学习率调度、混合精度训练（Mixed Precision）以及分布式数据并行（DDP）来处理大规模多模态数据。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 类主要临床任务，并使用了 AUROC、AUPRC、MSE 和 C-index 等多维度指标。
    *   **鲁棒性实验**：专门测试了在 0%、25%、50%、75% 模态缺失情况下的性能表现。
    *   **校准实验**：通过 ECE（期望校准误差）和 NLL 评估了模型预测的可靠性。
    *   **消融实验**：分别验证了一致性损失、分布表示和对比几何项的贡献。
*   **充分性评价**：实验设计较为全面，特别是针对“缺失数据”的鲁棒性测试和“不确定性校准”的评估，直接回应了论文的核心主张，逻辑闭环较好。

### 6. 主要结论与发现
*   **性能领先**：分布表示模型在所有任务上均优于确定性基准，尤其在 AUPRC（不平衡数据）上提升显著。
*   **鲁棒性极强**：随着数据缺失比例增加，该方法的性能下降速度远慢于传统模型，证明了其捕捉“不变生理特征”的能力。
*   **校准度高**：ECE 降低了近 40%，意味着模型在给出预测时能更准确地反映其“信心”，这对于临床决策至关重要。
*   **语义相关性**：分析发现，潜在分布的体积（协方差行列式）与预测难度呈正相关，具有明确的临床语义。

### 7. 优点
*   **归纳偏置创新**：将临床数据的“不完整性”从缺陷转变为建模原则，符合医学逻辑。
*   **不确定性量化**：通过协方差显式建模认知不确定性，为医疗AI的安全性提供了理论支撑。
*   **灵活的推理**：Product-of-Experts 公式允许模型随着新证据（新检查结果）的加入而自然地更新并收缩后验分布。

### 8. 不足与局限
*   **分布假设限制**：目前主要采用高斯分布，可能无法完全捕捉极其复杂或多峰的潜在生理状态。
*   **计算开销**：采样过程和散度计算增加了训练和推理的计算负担。
*   **数据单一性**：实验主要集中在 MIMIC 数据库，虽然数据量大，但仍需在更多跨机构、跨地域的数据集上验证泛化性。
*   **临床集成**：如何将这种抽象的“不确定性分数”转化为医生可理解、可操作的临床建议，仍是一个开放性挑战。

（完）
