---
title: "CDMT-EHR: A Continuous-Time Diffusion Framework for Generating Mixed-Type Time-Series Electronic Health Records"
title_zh: CDMT-EHR：一种用于生成混合类型时间序列电子健康档案的连续时间扩散框架
authors: "Shaonan Liu, Yuichiro Iwashita, Soichiro Nakako, Masakazu Iwamura, Koichi Kise"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23719v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 用于生成合成EHR数据的扩散框架
tldr: 针对电子健康记录（EHR）合成中的隐私保护和混合类型时间序列建模挑战，本文提出CDMT-EHR框架。该框架采用连续时间扩散模型，结合双向GRU捕捉时序依赖，并通过可学习嵌入统一处理数值与类别特征。此外，引入因子化噪声调度优化学习过程。实验证明，该方法在保持高保真度的同时，显著提升了采样效率，并支持条件化生成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.23719v1/fig-001.webp\", \"caption\": \"TABLE III FIDELITY METRICS (↓). BEST RESULTS PER DATASET ARE BOLDED.\", \"page\": 7, \"index\": 1, \"width\": 727, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2603.23719v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the proposed method. Categorical variables are mapped into a learnable continuous embedding space, and both numerical and categorical (embedded) features undergo unified Gaussian diffusion. A BiGRU backbone captures temporal dependencies. Factorized learnable noise schedules adapt to per-feature-per-timestep learning difficulties.\", \"page\": 4, \"index\": 2, \"width\": 1072, \"height\": 417}]"
motivation: 现有的离散时间扩散模型在处理混合类型EHR数据时存在近似误差大、采样效率低且难以有效建模跨特征关联的问题。
method: 提出一种基于连续时间扩散的框架，利用Bi-GRU处理时序性，通过可学习嵌入实现特征统一建模，并采用因子化噪声调度。
result: 在两个大型ICU数据集上，该方法在下游任务表现和数据保真度上均优于现有模型，且采样速度比基线方法提升了20倍。
conclusion: CDMT-EHR为高质量、高效率的混合类型医疗时序数据合成提供了一种有效的连续时间扩散解决方案。
---

## 摘要
电子健康档案（EHR）对临床研究具有极高的价值，但隐私问题严重限制了数据共享。合成数据生成提供了一个极具前景的解决方案，但 EHR 面临着独特的挑战：它们包含随时间演变的数值型和类别型特征。虽然扩散模型在 EHR 合成中表现出强大的性能，但现有方法主要依赖于离散时间表述，这会受到有限步逼近误差的影响，且训练与采样步数相互耦合。我们提出了一种用于生成混合类型时间序列 EHR 的连续时间扩散框架，具有三项贡献：（1）采用双向门控循环单元作为骨干网络的连续时间扩散，用于捕捉时间依赖性；（2）通过类别变量的可学习连续嵌入实现统一的高斯扩散，从而实现跨特征的联合建模；（3）一种分解的可学习噪声调度，能够适应每个特征在每个时间步的学习难度。在两个大规模重症监护室（ICU）数据集上的实验表明，我们的方法在下游任务性能、分布保真度和可区分性方面均优于现有方法，且仅需 50 个采样步，而基准方法则需要 1000 步。无分类器引导进一步实现了针对类别不平衡临床场景的有效条件生成。

## Abstract
Electronic health records (EHRs) are invaluable for clinical research, yet privacy concerns severely restrict data sharing. Synthetic data generation offers a promising solution, but EHRs present unique challenges: they contain both numerical and categorical features that evolve over time. While diffusion models have demonstrated strong performance in EHR synthesis, existing approaches predominantly rely on discrete-time formulations, which suffer from finite-step approximation errors and coupled training-sampling step counts. We propose a continuous-time diffusion framework for generating mixed-type time-series EHRs with three contributions: (1) continuous-time diffusion with a bidirectional gated recurrent unit backbone for capturing temporal dependencies, (2) unified Gaussian diffusion via learnable continuous embeddings for categorical variables, enabling joint cross-feature modeling, and (3) a factorized learnable noise schedule that adapts to per-feature-per-timestep learning difficulties. Experiments on two large-scale intensive care unit datasets demonstrate that our method outperforms existing approaches in downstream task performance, distribution fidelity, and discriminability, while requiring only 50 sampling steps compared to 1,000 for baseline methods. Classifier-free guidance further enables effective conditional generation for class-imbalanced clinical scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《CDMT-EHR: A Continuous-Time Diffusion Framework for Generating Mixed-Type Time-Series Electronic Health Records》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保护患者隐私的前提下，生成高质量、高效率且包含混合数据类型（数值型和类别型）的时间序列电子健康档案（EHR）。
*   **研究动机**：
    *   **隐私限制**：真实EHR数据共享受限，合成数据是理想替代方案。
    *   **现有模型缺陷**：现有的扩散模型（如TimeDiff）多基于离散时间框架（DDPM），存在有限步逼近误差、训练与采样步数耦合（导致采样慢）、以及无法针对不同特征自适应调整噪声调度等问题。
    *   **数据复杂性**：EHR包含随时间演变的异构特征，需要统一建模跨特征关联和时序依赖。

### 2. 方法论
CDMT-EHR 提出了一种连续时间扩散框架，其核心思想和技术细节包括：
*   **连续时间扩散与BiGRU骨干**：采用基于随机微分方程（SDE）的连续时间扩散模型，解耦了训练和采样步数。使用双向门控循环单元（BiGRU）作为去噪网络，以 $O(F \cdot L)$ 的复杂度高效捕捉时序依赖。
*   **统一高斯扩散（连续嵌入）**：不使用传统的掩码扩散处理类别变量，而是将类别特征映射到可学习的连续嵌入空间，并应用与数值特征相同的高斯扩散。这使得所有特征在统一的连续空间中建模，增强了跨特征关联的捕捉能力。
*   **因子化可学习噪声调度**：提出将噪声形状参数 $\rho$ 分解为全局、特征特定和时间特定的组件（$\rho_{f,l} = \rho_{global} + \rho_{feature}[f] + \rho_{time}[l]$）。这不仅能适应不同特征在不同时刻的学习难度，还将参数量从 $O(F \times L)$ 降低到 $O(F + L)$。
*   **无分类器引导（CFG）**：引入 CFG 技术支持条件生成，通过调节引导强度，在类别不平衡（如死亡率预测）的临床场景中实现有效的样本增强。

### 3. 实验设计
*   **数据集**：
    *   **MIMIC-III**：提取了7个生理变量，采样间隔1小时，序列长度24。
    *   **eICU**：提取了4个生理变量，采样间隔5分钟，序列长度272。
*   **基准模型（Benchmarks）**：主要对比了 **TimeDiff**（目前最先进的基于离散时间扩散的时序EHR生成模型），包括其 GRU 和 Transformer 两种变体。
*   **评估指标**：
    *   **下游任务性能（TSTR）**：在合成数据上训练分类器（LSTM、Transformer等），在真实数据上测试 AUC。
    *   **保真度（Fidelity）**：包括分布距离（MMD）、相关性误差（Corr MAE）、自相关误差（ACF MSE）和动态时间规整（DTW）。
    *   **可区分性（C2ST）**：训练判别器区分真假数据，AUC 越接近 0.5 越好。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA TITAN RTX** 或 **NVIDIA RTX A6000** GPU。
*   **训练配置**：使用 Adam 优化器，学习率 0.001，Batch Size 为 4096，训练 8000 个 Epoch。
*   **效率提升**：采样仅需 **50 步**（Euler 方法），而基线模型 TimeDiff 需要 1000 步，采样速度提升约 20 倍。

### 5. 实验数量与充分性
*   **实验规模**：在两个大型 ICU 数据集上进行了全面测试，涵盖了无条件生成和条件生成（CFG）模式。
*   **消融实验**：
    *   对比了“掩码扩散”与“连续嵌入”的效果，证明了统一高斯扩散的优越性。
    *   对比了“可学习噪声调度”与“固定调度”，验证了因子化设计的有效性。
    *   进行了数据增强实验，测试了不同比例合成数据对模型性能的提升。
*   **评价**：实验设计客观、公平，通过多种分类器和判别器取平均值的方式减少了随机误差，实验覆盖面较广。

### 6. 主要结论与发现
*   **性能领先**：CDMT-EHR 在下游任务 AUC 上显著优于 TimeDiff，在 MIMIC-III 上接近真实数据表现（0.751 vs 0.770）。
*   **高效采样**：在保持更高保真度的同时，采样步数缩减了 95%。
*   **时序保真度**：在自相关函数（ACF）和时间序列形状（DTW）指标上表现优异，证明其能精准捕捉临床动态。
*   **条件生成有效性**：CFG 在处理少数类（如死亡病例）样本增强时非常有效，显著提升了不平衡数据集下的预测性能。

### 7. 优点
*   **效率与质量并重**：通过连续时间框架解决了扩散模型采样慢的痛点。
*   **统一建模**：连续嵌入技术解决了混合类型数据难以联合优化的难题。
*   **参数高效**：因子化噪声调度以极小的参数代价实现了精细化的特征自适应。
*   **实用性强**：支持条件生成，直接针对医疗数据中常见的类别不平衡问题。

### 8. 不足与局限
*   **类别边际分布**：在类别特征的边际分布保真度（TVD 指标）上，有时略逊于基于 Transformer 的离散模型。
*   **非线性依赖**：判别器（MLP）仍能以约 0.6 的 AUC 识别出合成数据，说明在捕捉复杂的非线性跨特征依赖方面仍有提升空间。
*   **数据集覆盖**：虽然在两个 ICU 数据集上表现良好，但尚未在更长序列或更多样化的非 ICU 临床数据上进行验证。

（完）
