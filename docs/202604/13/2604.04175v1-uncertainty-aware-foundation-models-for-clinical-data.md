---
title: Uncertainty-Aware Foundation Models for Clinical Data
title_zh: 临床数据的不确定性感知基础模型
authors: "Qian Zhou, Yuanyun Zhang, Shi Li"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04175v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 临床数据的不确定性感知基础模型
tldr: 针对临床数据稀疏、不完整且具有高度不确定性的挑战，本文提出了一种不确定性感知的基础模型框架。该方法打破了传统的确定性表示范式，将患者建模为潜在状态的分布而非单一向量。通过整合多模态编码器与自监督学习目标，模型能够显式编码认知不确定性，在提升临床预测准确性的同时，显著增强了对缺失数据的鲁棒性和不确定性校准能力。
source: arxiv
selection_source: fresh_fetch
motivation: 传统的临床基础模型通常采用确定性表示，难以有效处理医疗数据中普遍存在的观测不完整、稀疏及不规则等固有特性。
method: 提出一种将患者表示为潜在状态分布的框架，结合多模态编码器，通过重构、对比对齐和分布正则化等自监督目标捕捉认知不确定性。
result: 实验表明，该方法在多项临床任务中的预测性能、对缺失数据的鲁棒性以及不确定性校准指标上均显著优于现有的强基准模型。
conclusion: 在医疗基础模型中，建模“未观测到的信息”而非仅关注“已观测到的信息”，是提升模型可靠性和性能的关键归纳偏置。
---

## 摘要
医疗基础模型在很大程度上遵循了自然语言处理和计算机视觉的范式，强调在异构临床数据上进行大规模预训练和确定性表示。然而，临床观测本质上是不完整的，反映了对潜在生理状态的稀疏、不规则且依赖于模态的测量。在这项工作中，我们提出了一个不确定性感知的基础建模框架，它不将每个患者表示为点嵌入，而是表示为合理潜在状态上的分布。通过学习集值表示并强制同一患者不同部分视图之间的一致性，该模型在捕获不变可推断信息的同时，显式地编码了认知不确定性。我们将这一公式与多模态编码器和可扩展的自监督目标相结合，融合了重建、对比对齐和分布正则化。在多种临床任务中，与强基准模型相比，我们的方法提高了预测性能、缺失数据下的鲁棒性以及不确定性校准。这些结果表明，对未观测到的内容（而非仅对已观测到的内容）进行建模，构成了医疗基础模型的一个关键归纳偏置。

## Abstract
Healthcare foundation models have largely followed paradigms from natural language processing and computer vision, emphasizing large scale pretraining and deterministic representations over heterogeneous clinical data. However, clinical observations are inherently incomplete, reflecting sparse, irregular, and modality dependent measurements of an underlying physiologic state. In this work, we propose a framework for uncertainty aware foundation modeling that represents each patient not as a point embedding, but as a distribution over plausible latent states. By learning set valued representations and enforcing consistency across partial views of the same patient, the model captures what is invariantly inferable while explicitly encoding epistemic uncertainty. We integrate this formulation with multimodal encoders and scalable self supervised objectives, combining reconstruction, contrastive alignment, and distributional regularization. Across diverse clinical tasks, our approach improves predictive performance, robustness under missing data, and uncertainty calibration relative to strong baselines. These results suggest that modeling what is not observed rather than only what is constitutes a critical inductive bias for healthcare foundation models.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对临床数据的不确定性感知基础模型框架，旨在解决医疗数据中普遍存在的稀疏性、不规则性和观测不完整性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的医疗基础模型（借鉴自 NLP 和 CV 领域）通常将异构临床数据映射为**确定性的点嵌入（Point Embedding）**。然而，临床观测本质上是“结构性欠定”的（如化验是间歇性的、影像是个断面的），单一的点表示无法反映由于数据缺失而产生的**认知不确定性（Epistemic Uncertainty）**。
*   **研究动机**：作者认为，医疗 AI 的核心挑战不在于模型容量，而在于如何处理观测的不完整性。模型不应只学习“已观测到的”数据，而应能够推理“未观测到但合理的”潜在生理状态空间。

### 2. 方法论：不确定性感知表示学习
*   **核心思想**：将每个患者表示为潜在状态空间上的**分布 $q_\theta(z|x)$**（即集值表示），而非单一向量。
*   **关键技术细节**：
    *   **分布参数化**：使用编码器输出潜在高斯分布的均值 $\mu$ 和协方差 $\Sigma$。协方差的大小直接反映了模型对当前观测证据的信心。
    *   **多模态融合**：采用模态特定的编码器（如 Transformer 处理 EHR、ViT 处理影像），通过**掩码注意力池化（Masked Attention Pooling）**进行聚合，能够自然处理任意模态缺失的情况。
    *   **部分视图一致性（Consistency Objective）**：这是核心约束。强制要求同一患者的不同部分视图（如不同时间段、不同模态子集）推导出的潜在分布在散度（如 KL 散度或 Wasserstein 距离）上保持一致。
    *   **自监督预训练目标**：结合了**重构损失**（预测掩码 Token）、**一致性损失**（对齐分布）和**对比损失**（InfoNCE，增强患者间的区分度）。
    *   **下游预测**：通过从潜在分布中进行蒙特卡洛采样并边缘化处理，将表示的不确定性传播到最终的临床预测中。

### 3. 实验设计
*   **数据集**：基于 **MIMIC 数据库**（MIMIC-III/IV），涵盖了结构化 EHR、临床文本、医学影像和生理波形（ECG/PPG）。
*   **评估任务**：
    *   分类：院内死亡率、再入院预测、多标签诊断。
    *   回归：住院时长（LOS）。
    *   生存分析：时间到事件风险估计。
*   **基准模型（Benchmarks）**：
    *   MAE 风格模型（基于重构）。
    *   对比学习模型（基于实例对齐）。
    *   自回归/序列模型（类似 GPT 架构）。
*   **评估指标**：AUROC、AUPRC、MSE、C-index，以及衡量不确定性质量的 **ECE（期望校准误差）**。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 AdamW 优化器、余弦学习率调度、混合精度训练（Mixed Precision）以及分布式数据并行（DDP）来管理大规模多模态训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了三大类数据集（EHR、多模态、生理信号）。
    *   进行了**缺失数据鲁棒性实验**（模拟 0% 到 75% 的模态缺失）。
    *   进行了多项**消融实验**，验证了一致性约束、分布表示和对比几何项的有效性。
*   **充分性评价**：实验设计较为全面，不仅关注预测准确率，还重点评估了在极端数据缺失情况下的性能退化速度和不确定性校准指标，这在医疗场景下非常客观且具有实际意义。

### 6. 主要结论与发现
*   **分布表示优于点表示**：即使在数据完整时，学习分布也能提供更好的预测性能（AUROC 0.861 vs 0.846）。
*   **极强的鲁棒性**：在缺失 75% 模态的情况下，该方法性能下降最慢，远优于确定性基准模型。
*   **校准度提升**：分布模型将 ECE 降低了约 40%，意味着模型给出的置信度与实际准确率更匹配，减少了“过度自信”的错误。
*   **语义关联**：协方差矩阵的行列式（分布体积）与预测难度呈正相关，证明了不确定性估计具有语义含义。

### 7. 优点与亮点
*   **归纳偏置创新**：将“缺失性”视为临床数据的内在属性而非噪声，通过建模“未观测空间”引入了更符合医疗逻辑的归纳偏置。
*   **优雅处理缺失值**：无需显式插补（Imputation），通过分布的熵增自然表达信息缺失。
*   **理论与实践结合**：将变分推理的思想与大规模基础模型预训练相结合，提供了严谨的数学框架。

### 8. 不足与局限
*   **分布假设限制**：目前主要采用高斯分布假设，可能无法捕捉极其复杂或多峰的潜在生理状态空间。
*   **计算开销**：采样过程和散度计算增加了推理和训练的计算负担。
*   **临床集成挑战**：虽然模型输出了不确定性，但如何将这些量化的不确定性无缝集成到医生的决策流中仍需进一步研究。
*   **硬件细节缺失**：缺乏具体的算力消耗数据，难以评估其在资源受限环境下的可行性。

（完）
