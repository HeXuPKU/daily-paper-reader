---
title: Asymptotic enumeration of admixed arrays and a different independence heuristic
title_zh: 混合数组的渐近计数与一种不同的独立性启发式
authors: Alan J. Aw
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08857v1"
tags: ["query:gwas"]
score: 6.0
evidence: 使用混合阵列分析大规模遗传数据的组合结构
tldr: 本文研究了源自遗传数据分析的“混合数组”（admixed arrays）这一成对二进制矩阵结构的渐近计数问题。研究者针对行和与成对列和约束，利用鞍点近似和概率技术推导了其在稠密状态下的渐近展开式。研究发现，在均匀边际和特定密度下，该模型的独立性启发式修正因子为 1/e^(1/4)，这与经典矩阵模型显著不同。此外，研究还提供了精确公式、信息论解释及开源枚举软件。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08857v1/fig-001.webp\", \"caption\": \"Figure 3: Visualizations for the proof of Theorem 3.7. A. Zoomed in graph of the binary entropy function H(x) = x log x+(1−x) log(1−x) restricted to x ∈ [0, 0.5]. B. Translation of the graph of the set of points (f0, f1) that represent ancestry-specific allele fraction constraints, where the parameterization x0 = f0 − 1/4 and x1 = f1 − 1/4 is used. The quantity c = (2 ln 2)ε+ ln 2(logN/N + logP/P ), as defined in the proof of Theorem 3.7. The ellipse and the region outside it bounded by the isosceles triangle are colored differently, as they depict two cases for which the volume of B (defined in Main Text) is to be bounded from above. The dashed closed curve overlaying the boundary of the ellipse depicts the actual region for one of the cases, which we approximate to second order with the ellipse.\", \"page\": 14, \"index\": 1, \"width\": 877, \"height\": 434}, {\"url\": \"assets/figures/arxiv/2604.08857v1/fig-002.webp\", \"caption\": \"Figure 1: An example of a two-way (ℓ = 2) admixed array for diploid individuals (k = 2) with N = 4 individuals (rows) and P = 5 loci (paired columns). The homologous haplotypes, which determine the pairing of columns, are annotated for clarity. Throughout this work, we assign one color to “Ancestry 0” and another to “Ancestry 1” (in this case, 0 = blue and 1 = red) so that global ancestries are determined with respect to Ancestry 1. In this example, the first individual’s global ancestry, A1·, is computed using only information in the local ancestry matrix A — specifically, by counting the fraction of red cells along the first row (marked by ▷), A1· = 7/10 = 0.7. On the other hand, ancestry-specific allele frequencies for the first locus, F1,0 and F1,1, are computed using information in both A and the allele dosage matrix X. Specifically, focusing on just the two columns corresponding to the first locus (these are marked by ↓), F1,0 = 1/2 = 0.5 and F1,1 = 3/6 = 0.5. The corresponding ancestry-specific allele dosages are Φ1,0 = 1 and Φ1,1 = 3. On the right, we further show the correspondence of [A,X] with a weighted edge coloring of a complete bipartite graph, where the black and white nodes correspond to the rows and columns of the admixed array. We omit edge weight labels to improve visualization.\", \"page\": 4, \"index\": 2, \"width\": 920, \"height\": 464}, {\"url\": \"assets/figures/arxiv/2604.08857v1/fig-003.webp\", \"caption\": \"Figure 2: Visualizing Corollary 3.6. A. Heat map of the fraction of predictions obtained from applying the entropy approximate criterion (Eq. (3.16)) that agree with the true classification obtained by direct calculation of |A1| and |A2|. Fractions range from 0.74 ((N,P ) = (10, 103)) to 0.99 ((N,P ) = (103, 103)). B. Heat map showing ranges of values of the pair of quantities (a, f) for which A1 is larger than A2, when N = P = 1000 and a and f are allowed to range within their discrete domains. To improve visualization, the roughly 2×106 choices of (a, f) are partitioned into 3, 600 square bins, with each bin reporting the fraction of (a, f) pairs — denoted P (|A1| > |A]|) — for which |A1| > |A2|. Black points identify bins for which the approximate bound (Eq. (3.16)) predicted the larger set inaccurately, with larger points indicating a greater fraction of inaccurate predictions within the bin.\", \"page\": 12, \"index\": 3, \"width\": 989, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2604.08857v1/fig-004.webp\", \"caption\": \"Table 2: Comparison of exact and approximate values of |A12|, in the semi-regular 1/2 case with N = P . Values in the saddle-point approximation column are computed using Eq. (4.4), including all terms except for the O(1/ √ m) quantity. Values in the right-most column are computed by subtracting (α1 + α2 − logD) from the quantity in the saddle-point approximation column. Exact values of α12 for N = P ∈ {102, 103, 104} were not computed owing to a prohibitively long runtime.\", \"page\": 27, \"index\": 4, \"width\": 1002, \"height\": 411}]"
motivation: 旨在解决大规模遗传数据分析中混合数组在边际约束下的计数难题，并探索其独特的组合数学特性。
method: 采用鞍点近似、概率论方法和熵不等式分析，对单约束和双约束下的数组规模进行精确推导与渐近估计。
result: 成功推导出双约束家族的对数计数渐近展开式，并发现了一个不同于经典模型的四阶矩贡献修正因子 1/e^(1/4)。
conclusion: 该研究揭示了混合数组独特的渐近行为，为遗传学中的组合结构分析提供了严谨的数学基础和高效的计算工具。
---

## 摘要
我们引入了一类称为“混合数组”（admixed arrays）的成对二进制矩阵，这类矩阵源于大规模遗传数据分析，并可被视为完全二分图的加权边着色。这种组合结构产生了两个自然的边际约束族：行和约束与成对列和约束，后者在矩阵对的条目之间引入了一个不等式。我们研究了在稠密情形下这些约束下的混合数组计数问题。首先，我们获得了由每个单独约束定义的集合大小的精确公式，并推导出一个有限尺寸准则，用以表征何时一个约束比另一个约束更具限制性。在高维极限下，这种比较简化为一个熵不等式，从而在半正则情形下提供了信息论解释和可量化的误差界。随后，我们分析了半正则设置下双重约束族的渐近计数。利用鞍点近似和概率方法，我们推导了计数对数的详细渐近展开，分离出了显式的四阶矩贡献，并实现了对高阶余项的定量控制。该分析的一个结果是发现了经典二进制和整数矩阵模型中不存在的现象：在 $N=Θ(P)$、具有均匀边际且密度远离零的情形下，这两个约束族遵循独立性启发式，但其修正因子为 $1/\sqrt[4]{e}$，而非人们熟悉的 $e^{\pm1/2}$。数值实验证实了这些解析近似，并且我们实现并扩展了 Miller 和 Harrison (2013) 的算法作为开源软件，用于对受限混合数组进行计数。

## Abstract
We introduce a class of paired binary matrices called admixed arrays, which arise in analyses of large-scale genetic data and can be viewed as weighted edge colorings of complete bipartite graphs. This combinatorial structure gives rise to two natural families of marginal constraints: a row-sum constraint and a paired column-sum constraint, the latter inducing an inequality among entries of the matrix pair. We study the enumeration of admixed arrays under these constraints in dense regimes. First, we obtain exact formulas for the sizes of the families defined by each constraint in isolation and derive a finite-size criterion characterizing when one constraint is more restrictive than the other. In the large-dimension limit, this comparison simplifies to an entropy inequality, yielding an information-theoretic interpretation and a quantifiable error bound in the semi-regular case. We then analyze the asymptotic enumeration of the doubly constrained family in a semi-regular setting. Using saddle-point approximation and probabilistic techniques, we derive a detailed asymptotic expansion for the logarithm of the count, isolating an explicit fourth-moment contribution and establishing quantitative control of the higher-order remainder. A consequence of this analysis is a phenomenon absent from classical binary and integer matrix models: in the regime $N=Θ(P)$ with uniform margins and density bounded away from zero, the two constraint families obey the independence heuristic with a correction factor $1/\sqrt[4]{e}$ rather than the familiar $e^{\pm1/2}$. Numerical experiments corroborate the analytical approximations, and we implement and extend an algorithm of Miller and Harrison (2013) as open-source software to enumerate constrained admixed arrays.