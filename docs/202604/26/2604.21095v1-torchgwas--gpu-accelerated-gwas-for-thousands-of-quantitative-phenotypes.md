---
title: "TorchGWAS : GPU-accelerated GWAS for thousands of quantitative phenotypes"
title_zh: TorchGWAS：针对数千种数量表型的GPU加速GWAS
authors: "Xingzhong Zhao, Ziqian Xie, Islam, Sheikh Muhammad Saiful, Tian Xia, Chen, Cheng, Degui Zhi"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.21095v1"
tags: ["query:gwas"]
score: 10.0
evidence: 用于高通量关联测试的GPU加速GWAS框架
tldr: TorchGWAS是一个基于GPU加速的GWAS分析框架，旨在解决现代生物信息学中成千上万种定量表型关联分析的计算瓶颈。它通过优化大规模表型面板与相同基因型矩阵的并行计算，支持多种输入格式并内置协变量调整。在基准测试中，其吞吐量比传统工具提升了300至1700倍，极大提高了大规模表型筛选的效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21095v1/fig-001.webp\", \"caption\": \"Figure 2. Validation and benchmarking of TorchGWAS. Left: Comparison of SNP association significance estimated by TorchGWAS and PLINK, showing near-perfect agreement for representative archived traits. Right: Runtime comparison of TorchGWAS and fastGWA for 2,048 and 20,480 phenotypes. Runtimes are shown on a log scale.\", \"page\": 3, \"index\": 1, \"width\": 620, \"height\": 253}]"
motivation: 现代生物信息学产生海量表型数据，传统GWAS工具在处理成千上万个表型时因无法有效复用基因型矩阵而成为计算瓶颈。
method: 开发了基于Python和GPU加速的TorchGWAS框架，支持线性GWAS和多变量筛选，并能自动处理数据对齐与协变量调整。
result: "在890万个标记和2.3万个样本的测试中，TorchGWAS在单块A100 GPU上仅用20分钟即可完成20,480个表型的分析，速度比传统工具提升了300至1700倍。"
conclusion: TorchGWAS为表型丰富的大规模基因组关联研究提供了高效实用的解决方案，显著提升了高通量筛选的计算可行性。
---

## 摘要
动机：现代生物信息学工作流，特别是在影像学和表示学习领域，可以从单一队列中生成数千到数万个数量表型。在这种背景下，逐个性状地运行全基因组关联分析（GWAS）迅速成为计算瓶颈。虽然现有的GWAS工具对单个性状非常有效，但它们并未针对表型丰富的筛选工作流进行优化，在这类工作流中，同一基因型矩阵会在大型表型面板中被重复使用。结果：我们提出了TorchGWAS，这是一个通过硬件加速对大型表型面板进行高通量关联测试的框架。当前的公开发布版本为线性GWAS和多变量表型筛选提供了稳定的Python和命令行工作流，支持NumPy、PLINK和BGEN基因型输入，通过样本标识符对齐表型和协变量表，并在内部执行协变量调整。在包含890万个标记和23,000个样本的基准测试中，fastGWA在AMD EPYC 7763 64核CPU上处理每个表型大约需要100秒，而TorchGWAS在单个NVIDIA A100 GPU上仅用10分钟就完成了2,048个表型，20分钟完成了20,480个表型，这相当于表型吞吐量提高了约300到1700倍。因此，TorchGWAS使得在需要高效评估数千个数量性状的表型丰富场景下，进行大规模GWAS筛选变得切实可行。可用性与实现：TorchGWAS采用Python实现，并作为带有文档的源码仓库发布在https://github.com/ZhiGroup/TorchGWAS。当前版本提供了命令行界面、打包的源代码、教程、基准测试脚本和示例工作流。

## Abstract
Motivation: Modern bioinformatics workflows, particularly in imaging and representation learning, can generate thousands to tens of thousands of quantitative phenotypes from a single cohort. In such settings, running genome-wide association analyses trait by trait rapidly becomes a computational bottleneck. While established GWAS tools are highly effective for individual traits, they are not optimized for phenotype-rich screening workflows in which the same genotype matrix is reused across a large phenotype panel. Results: We present TorchGWAS, a framework for high-throughput association testing of large phenotype panels through hardware acceleration. The current public release provides stable Python and command-line workflows for linear GWAS and multivariate phenotype screening, supports NumPy, PLINK, and BGEN genotype inputs, aligns phenotype and covariate tables by sample identifier, and performs covariate adjustment internally. In a benchmark with 8.9 million markers and 23,000 samples, fastGWA required approximately 100 second per phenotype on an AMD EPYC 7763 64-core CPU, whereas TorchGWAS completed 2,048 phenotypes in 10 minute and 20,480 phenotypes in 20 minutes on a single NVIDIA A100 GPU, corresponding to an approximately 300- to 1700-fold increase in phenotype throughput. TorchGWAS therefore makes large-scale GWAS screening practical in phenotype-rich settings where thousands of quantitative traits must be evaluated efficiently. Availability and implementation: TorchGWAS is implemented in Python and distributed as a documented source repository at https://github.com/ZhiGroup/TorchGWAS. The current release provides a command-line interface, packaged source code, tutorials, benchmark scripts, and example workflows.

---

## 论文详细总结（自动生成）

这是一份关于论文《TorchGWAS: GPU-accelerated GWAS for thousands of quantitative phenotypes》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在现代生物信息学（如影像遗传学、深度学习表示学习）中，研究者往往需要针对同一队列生成数千甚至数万个数量表型（Quantitative Phenotypes）。
*   **研究背景**：传统的GWAS工具（如fastGWA、PLINK）虽然对单个性状分析非常高效，但在处理“表型丰富”的场景时，由于需要逐个性状重复扫描基因型矩阵，计算效率极低，成为研究瓶颈。
*   **整体含义**：TorchGWAS 旨在通过 GPU 加速和矩阵运算优化，实现对大规模表型面板的高通量关联测试，将原本需要数周的计算缩短至分钟级。

### 2. 方法论
*   **核心思想**：将关联测试重新组织为“基因型批次”与“表型矩阵”之间的重复矩阵运算。通过一次性预处理表型数据，在全基因组扫描中复用该矩阵。
*   **关键技术细节**：
    1.  **表型预处理**：对表型矩阵 $Y$ 进行均值中心化，并利用协变量空间的规范正交基 $Q$ 进行残差化处理（公式：$Y_{res} = (I - QQ^T)(Y - \bar{Y})$），随后进行标准化。
    2.  **基因型处理**：将基因型分批次（Batch）加载并标准化。
    3.  **关联内核**：计算基因型与表型的相关系数矩阵 $R = G_s Y_s / N$。
    4.  **统计转换**：将相关系数 $R$ 直接转换为 t-统计量和 P 值。
*   **工作流**：支持 NumPy、PLINK (bed/bim/fam) 和 BGEN 格式输入，自动按样本 ID 对齐表型和协变量。

### 3. 实验设计
*   **数据集**：使用了包含 **890 万个遗传标记**和约 **23,000 个样本**的数据集。
*   **Benchmark（基准测试）**：
    *   **准确性对比**：将 TorchGWAS 的结果与 PLINK 的输出进行 Pearson 相关性分析。
    *   **性能对比**：对比了 **fastGWA**（基于 CPU 的领先工具）在处理不同数量表型时的耗时。
*   **对比场景**：分别测试了处理 2,048 个和 20,480 个表型的运行时间。

### 4. 资源与算力
*   **GPU 硬件**：使用了 **1 块 NVIDIA A100 GPU**。
*   **CPU 硬件**：对比组使用了 **AMD EPYC 7763 64 核处理器**。
*   **运行耗时**：
    *   处理 2,048 个表型：TorchGWAS 约 10 分钟（940秒）；fastGWA 约 57 小时（按 100s/性状估算）。
    *   处理 20,480 个表型：TorchGWAS 约 20 分钟（1108秒）。

### 5. 实验数量与充分性
*   **实验规模**：论文主要展示了在大规模真实遗传数据下的性能飞跃和准确性验证。
*   **充分性评价**：
    *   **性能验证充分**：通过对数级增长的表型数量展示了其非线性增长的优越性。
    *   **准确性客观**：与 PLINK 的相关性达到 0.999，证明了数值计算的可靠性。
    *   **局限性**：实验主要集中在数量性状的线性模型上，未涉及二元表型或更复杂的混合线性模型（LMM）对比。

### 6. 主要结论与发现
*   **吞吐量极大提升**：相比传统工具，TorchGWAS 实现了 **300 到 1700 倍**的吞吐量提升。
*   **扩展性极佳**：随着表型数量增加，运行时间增加非常缓慢，因为基因型读取和预处理的开销被大量表型平摊。
*   **实用性强**：TorchGWAS 能够让原本不可行的大规模表型筛选（如数万个影像特征）在单台工作站上快速完成。

### 7. 优点
*   **高效复用**：巧妙地通过矩阵化运算避免了基因型数据的重复加载和处理。
*   **易用性**：提供了 Python 库和命令行界面，兼容主流生物信息学文件格式（PLINK/BGEN）。
*   **硬件优化**：充分利用了 PyTorch 框架在 GPU 上的张量运算能力。

### 8. 不足与局限
*   **模型单一**：目前仅支持线性回归模型，不支持广义线性模型（GLM）或混合线性模型（LMM），无法直接处理复杂的群体结构（虽然支持预先排除亲缘关系样本）。
*   **硬件依赖**：性能高度依赖 GPU 显存大小和磁盘 I/O 带宽，在处理超大规模样本时可能受限。
*   **功能尚简**：作为早期版本，缺乏成熟工具包（如 SAIGE 或 fastGWA）中丰富的建模选项。

（完）
