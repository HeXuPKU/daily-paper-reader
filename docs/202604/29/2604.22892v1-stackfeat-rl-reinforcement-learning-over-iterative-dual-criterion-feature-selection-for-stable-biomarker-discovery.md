---
title: "StackFeat RL: Reinforcement Learning over Iterative Dual Criterion Feature Selection for Stable Biomarker Discovery"
title_zh: StackFeat RL：基于迭代双准则特征选择的强化学习，用于稳定的生物标志物发现
authors: "A. Yermekov, D. A. Herrera-Martí"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22892v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 强化学习用于基因组数据的特征选择和生物标志物发现
tldr: 针对高维基因组数据特征选择中稳定性与稀疏性难以兼顾的问题，本文提出StackFeat-RL框架。该框架采用元学习思路，利用REINFORCE策略梯度算法优化迭代双准则特征选择的超参数。通过结合系数一致性和选择频率的双重标准，该方法有效克服了单一准则的局限性。实验表明，在新冠及阿尔茨海默症数据集上，StackFeat-RL在保持最高预测准确率的同时，显著降低了特征维度，为稳定生物标志物发现提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22892v1/fig-001.webp\", \"caption\": \"Fig. 2. AUC vs. panel size (genes per fold). Each point represents one outer fold. Large translucent markers indicate method means. On all AD tasks, StackFeat-RL occupies the upper-left quadrant (high accuracy, few genes), demonstrating Pareto dominance over all baselines. mRMR (matched gene count) and SF-RL overlap on the x-axis but are clearly separated on AUC.\", \"page\": 6, \"index\": 1, \"width\": 1019, \"height\": 783}, {\"url\": \"assets/figures/arxiv/2604.22892v1/fig-002.webp\", \"caption\": \"Fig. 3. STRING protein interactions among StackFeat-RL Definite AD genes filtered by co-selection frequency (ψ). Edge labels show ψ values. Three functional modules emerge: haemoglobin/haeme biosynthesis (red), oxidative stress (blue), and autophagy (green). Node size proportional to degree.\", \"page\": 7, \"index\": 2, \"width\": 492, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2604.22892v1/fig-003.webp\", \"caption\": \"Fig. 1. Nested CV performance (AUC) across all methods and tasks. Average gene counts shown below each box. Diamond: mean; horizontal line: median; dots: individual outer folds. Brackets: paired t-test p-values vs. StackFeat-RL. StackFeat-RL achieves the highest mean AUC on all three AD tasks while using 3–4× fewer genes than ElasticNet and Boruta.\", \"page\": 5, \"index\": 3, \"width\": 1019, \"height\": 783}]"
motivation: 现有的高维基因组特征选择方法在处理数据扰动时稳定性不足，且往往需要手动设置阈值或忽略生物结构。
method: 提出一种基于强化学习的元学习框架，通过REINFORCE算法优化结合了系数一致性与选择频率的双准则迭代筛选算法。
result: 在新冠和阿尔茨海默症相关任务中，该方法在取得最高准确率的同时，所用特征数仅为ElasticNet、Boruta等主流方法的四分之一到三分之一。
conclusion: StackFeat-RL通过强化学习驱动的双准则优化，实现了高精度、高稀疏性且稳定的生物标志物识别。
---

## 摘要
高维基因组数据（$d \gg n$）中的特征选择要求方法同时具备准确性、稀疏性和稳定性。现有方法要么需要手动指定阈值（如 mRMR、稳定性选择），要么在数据扰动下产生不稳定的选择（如 Lasso、Boruta），或者完全忽略了生物结构。我们引入了 StackFeat-RL，这是一个元学习框架，通过 REINFORCE 策略梯度优化迭代双准则特征选择算法的超参数。该双准则要求系数一致性和选择频率，能够防范单准则方法容易忽略的两种失效模式，而迭代累积则通过大数定律提供收敛保证。在 COVID-19 miRNA 数据（GSE240888，332 个特征）和三项阿尔茨海默病分类任务（GSE84422，13237 个基因；正常 vs. 可能、极可能和确诊的 AD）上，StackFeat-RL 在所有评估方法（包括 ElasticNet、Boruta、mRMR 和稳定性选择）中实现了最高的预测准确率，同时所需的特征数量减少了 3-4 倍。关键词：特征选择，强化学习，REINFORCE，弹性网络，生物标志物发现，阿尔茨海默病，双准则选择，蛋白质相互作用网络

## Abstract
Feature selection in high-dimensional genomic data ($d \gg n$) demands methods that are simultaneously accurate, sparse, and stable. Existing approaches either require manual threshold specification (mRMR, stability selection), produce unstable selections under data perturbation (Lasso, Boruta), or ignore biological structure entirely. We introduce StackFeat-RL, a meta-learning framework that optimises the hyperparameters of an iterative dual-criterion feature selection algorithm via REINFORCE policy gradients. The dual criterion, requiring both coefficient consistency and selection frequency, guards against two failure modes missed by single-criterion methods, while iterative accumulation provides convergence guarantees via the law of large numbers.   On COVID-19 miRNA data (GSE240888, 332 features) and three Alzheimer's disease classification tasks (GSE84422, 13237 genes; Normal vs.\ Possible, Probable, and Definite AD), StackFeat-RL achieves the highest predictive accuracy among all evaluated methods, including ElasticNet, Boruta, mRMR, and stability selection, while requiring 3--4$\times$ fewer features.   Keywords: feature selection, reinforcement learning, REINFORCE, elastic net, biomarker discovery, Alzheimer's disease, dual-criterion selection, protein interaction networks

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **StackFeat-RL** 的新型特征选择框架，旨在解决高维基因组数据中生物标志物发现的准确性、稀疏性和稳定性三大挑战。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在高维基因组数据（特征数 $d$ 远大于样本数 $n$）中，现有的特征选择方法难以平衡**预测准确性**、**特征稀疏性**（便于临床验证）和**选择稳定性**（对数据扰动的鲁棒性）。
*   **研究背景**：
    *   L1 正则化（如 Lasso）在处理相关特征时具有随机性，导致结果不稳定。
    *   过滤法（如 mRMR）需要手动指定特征数量，且忽略了特征间的复杂交互。
    *   包装法（如 Boruta）生成的特征集往往过于冗余（特征数过多）。
    *   稳定性选择（Stability Selection）仅关注频率，可能误选系数正负交替（不一致）的噪声特征。

### 2. 方法论
StackFeat-RL 是一个基于强化学习（RL）的元学习框架，其核心思想是**通过策略梯度算法自动优化迭代双准则筛选过程的超参数**。
*   **双准则筛选（Dual-Criterion）**：同时要求特征满足“高选择频率”和“高系数一致性（符号一致且均值大）”。这能有效过滤掉符号波动的噪声和偶然出现的特征。
*   **强化学习优化（REINFORCE）**：
    *   **状态（State）**：包括特征的选择频率、平均系数、STRING 蛋白质相互作用网络中的连接度以及特征间的共同选择频率。
    *   **动作（Action）**：策略网络 $\pi_\theta$ 学习两个关键参数：(1) 每个基因的惩罚调制因子（调节 ElasticNet 的惩罚强度）；(2) 特征保留比例 $m_{frac}$。
    *   **奖励（Reward）**：基于验证集的 AUC 值减去稀疏性惩罚，促使模型在保持高精度的同时减少特征数。
*   **计算效率优化**：通过单次 `ElasticNetCV` 确定全局正则化强度 $\alpha$，替代了原版 StackFeat 昂贵的嵌套交叉验证，使计算速度提升了 10-17 倍。
*   **收敛保证**：利用大数定律（LLN），随着迭代次数增加，累积统计量会收敛于总体水平的重要性度量。

### 3. 实验设计
*   **数据集**：
    1.  **COVID-19 miRNA (GSE240888)**：332 个特征，122 个样本。
    2.  **阿尔茨海默病 (AD) 基因表达 (GSE84422)**：13,237 个基因，951 个样本。包含三个任务：正常 vs. 可能（Possible）、极可能（Probable）、确诊（Definite）AD。
*   **Benchmark 与对比方法**：
    *   **ElasticNet**（强基准，嵌入式方法）。
    *   **Boruta**（基于随机森林的包装法）。
    *   **mRMR**（过滤法，特征数设定为与 StackFeat-RL 一致以保证公平）。
    *   **Stability Selection**（稳定性选择）。
    *   **StackFeat**（原版迭代算法）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 或 CPU 型号及具体训练时长。
*   **效率提升**：文中强调了算法的并行性，指出 REINFORCE 的每个 Episode 是独立的，可以在多核硬件上实现近线性的加速。相比原版 StackFeat，其在处理 1.3 万维度的 AD 数据时表现出显著的计算可行性。

### 5. 实验数量与充分性
*   **实验规模**：采用了 **10 折嵌套交叉验证（10-fold nested CV）**，这是评估高维数据特征选择最严谨的方法之一，能有效防止过拟合和信息泄露。
*   **充分性**：实验覆盖了低维（miRNA）和高维（Microarray）两种场景，并针对 AD 的不同阶段进行了细分任务测试。
*   **客观性**：通过与 mRMR 在相同特征数量下的 AUC 对比，证明了其性能提升源于“选得准”而非“选得多”。

### 6. 主要结论与发现
*   **性能领先**：在所有 AD 任务中，StackFeat-RL 的 AUC 均最高。在“极可能 AD”任务中，其 AUC 显著优于 ElasticNet（0.882 vs 0.835），且使用的基因数量减少了 4 倍。
*   **高度稀疏**：相比 Boruta 和 ElasticNet 动辄产生 100-200 个基因的面板，StackFeat-RL 仅需 40-50 个基因即可达到更优性能。
*   **生物学一致性**：在确诊 AD 任务中识别出的基因富集在自噬（Autophagy）、NRF2 介导的氧化应激和 BDNF 信号通路，这些均是已知的 AD 病理机制。
*   **功能模块**：通过 STRING 网络过滤，识别出了血红蛋白合成、氧化应激和自噬三个功能模块。

### 7. 优点
*   **全自动化**：无需手动指定特征子集大小、正则化强度或停止准则，所有参数均由数据驱动学习。
*   **稳定性与可解释性结合**：双准则机制提供了坚实的统计学收敛保证，同时引入生物学先验（STRING 网络）增强了结果的生物学意义。
*   **帕累托最优**：在“准确性-稀疏性”前沿面上达到了最优平衡。

### 8. 不足与局限
*   **假设简化**：在梯度推导中假设基因选择是独立的 Bernoulli 过程，而实际上基因之间存在复杂的共表达相关性。
*   **验证范围**：虽然在 miRNA 和微阵列数据上表现良好，但尚未在 RNA-seq、单细胞数据或癌症亚型分类等更广泛的组学任务上进行验证。
*   **策略网络简单**：目前的策略参数 $\theta$ 较为简单，未来可能需要更复杂的神经网络（如带有注意力机制的架构）来捕捉更深层的基因交互。

（完）
