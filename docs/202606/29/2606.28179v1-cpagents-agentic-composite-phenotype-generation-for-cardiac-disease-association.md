---
title: "CPAgents: Agentic Composite Phenotype Generation for Cardiac Disease Association"
title_zh: CPAgents：用于心脏病关联的智能复合表型生成
authors: "Zuoou Li, Wenlong Zhao, Kelly Yu, Weitong Zhang, Paul M. Matthews, Wenjia Bai, Bernhard Kainz, Mengyun Qiao"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28179v1"
tags: ["query:med-ai"]
score: 8.0
evidence: 基于智能体的复合表型生成框架用于心脏疾病关联研究，与医疗领域强化学习与智能体应用相关
tldr: 现有表型关联研究依赖预定义单变量表型，难以捕获非线性效应和交互作用。提出CPAgents框架，通过协调分析、提议和验证三个智能体，自动构建可解释的复合表型。在大型心脏影像队列中，复合表型在56/72个分类器-疾病-指标组合中取得最佳判别性能，显著优于基线。实现了超越专家驱动的可解释复合表型自动发现，提升疾病关联强度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28179v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1245, \"height\": 416, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28179v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 379, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28179v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1267, \"height\": 737, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28179v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 752, \"height\": 299, \"label\": \"Table\"}]"
motivation: 当前PheWAS依赖预定义单变量表型，无法捕获非线性与交互效应，限制疾病关联发现。
method: CPAgents由Analyst、Proposer和Verifier三个智能体组成，自动生成并验证可解释的复合表型。
result: 在72个组合中56个取得最佳判别性能，所有九类疾病均获益，优于基线方法。
conclusion: 自动化复合表型生成框架能发现更强、可解释的疾病关联，超越专家特征选择。
---

## 摘要
识别心脏影像表型与临床疾病之间的稳健关联，是人群规模心血管研究和可靠风险分层的基础。然而，当前的全表型关联研究依赖于预定义的单一变量表型或专家设计的特征，这限制了其捕捉具有临床意义的非线性效应和跨表型交互的能力。为解决这一问题，我们提出了CPAgents，一种用于心血管全表型关联研究（PheWAS）的迭代表型组合框架，可从基础影像特征自动构建和验证可解释的复合表型（如多项式、比值和交互形式）。具体而言，我们的系统协调三个智能体：（i）分析师，识别统计异常并提出候选变换；（ii）提议者，在数值安全规则下生成受约束的、医学和统计学动机的表达式；（iii）验证者，使用多阶段标准评估候选者，并为接受的表型生成透明的证据轨迹。在一个群体规模的心脏影像队列上评估，所发现的复合表型显著改善了疾病区分能力：在72个分类器-疾病-指标组合中，我们的变体在56个案例中达到最高排名，而基线为18个，并且在所有九个临床疾病类别中都观察到了改进。我们的框架生成简洁、临床可解释的表型公式，并带有透明的证据轨迹，从而实现超越专家驱动特征选择的更强表型-疾病关联的可扩展发现。

## Abstract
Identifying robust associations between cardiac imaging phenotypes and clinical diseases is fundamental to population-scale cardiovascular research and reliable risk stratification. However, current phenome-wide association studies rely on pre-defined, single-variable phenotypes or expert-crafted features, which limits their ability to capture clinically meaningful non-linear effects and cross-phenotype interactions. To address this, we propose CPAgents, an iterative phenotype-Composition framework for cardiovascular Phenome-wide association study (PheWAS) that automatically constructs and validates interpretable composite phenotypes (e.g., polynomial, ratio, and interaction forms) from base imaging features. Specifically, our system coordinates three agents: (i) an Analyst that identifies statistical pathologies and nominates candidate transformations; (ii) a Proposer that generates constrained, medically and statistically motivated expressions under numerical safety rules; and (iii) a Verifier that evaluates candidates using multi-stage criteria and produces transparent evidence trails for accepted phenotypes. Evaluated on a population-scale cardiac imaging cohort, the discovered composite phenotypes markedly improve disease discrimination: across 72 classifier-disease-metric combinations, our variants achieve the top rank in 56 cases versus 18 for baselines, with gains observed across all nine clinical disease categories. Our framework yields compact, clinically interpretable phenotype formulas with transparent evidence trails, enabling scalable discovery of stronger phenotype-disease associations beyond expert-driven feature selection.

---

## 论文详细总结（自动生成）

# CPAgents：用于心脏病关联的智能复合表型生成 —— 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前全表型关联研究（PheWAS）普遍采用预定义的单一变量表型或专家手工设计的特征，导致无法捕捉具有临床意义的非线性效应（如多项式关系）以及跨表型的交互作用（如比值、乘积等）。这限制了从大规模心脏影像队列中发现强疾病关联的能力。
- **研究动机**：现代心血管影像队列（如UK Biobank）提供了丰富的定量表型（如心室容积、心肌质量、主动脉几何参数等），但高维度、冗余和复杂的依赖结构使得选择“哪些组合”进行检验、“如何捕捉非线性关系”以及“确保结果稳健且可解释”成为巨大的分析瓶颈。以往工作或依赖专家知识（线性假设、固定特征集），或采用自动化特征构造但缺乏鲁棒性校验。
- **整体含义**：提出CPAgents框架——一种基于多智能体迭代协作的复合表型自动发现系统，能够自动构建并验证可解释的复合影像表型（如多项式和交互形式），显著增强表型-疾病关联的信号强度，同时保持临床可解释性和透明性。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将表型发现形式化为**“分析-提议-验证”**的迭代闭环，由三个专门智能体协调完成：
  1. **Analyst（分析师）**：对当前特征集进行统计画像，识别非线性、分布偏态、交互潜力、组效应和冗余，输出结构化建议。
  2. **Proposer（提议者）**：基于Analyst的统计报告和LLM医学先验知识，生成受约束的复合表型候选表达式（抽象语法树AST形式），操作符空间严格限定（数学运算、交互、组归一化等），并强制执行数值安全规则（防零除）。
  3. **Verifier（验证者）**：通过多阶段过滤（边际效用检查、稳定性筛选、去重、SHAP重要性排序）和全局接受标准（跨4个分类器的集成提升），保留稳健的复合特征；失败则反馈给Proposer改进。

### 关键技术细节
- **复合表型空间**：通过AST表示，叶子节点是原始特征或域特定常数（由LLM内生生成），内部节点是受限算子集（`log1p`, `sqrt`, `add`, `sub`, `mul`, `div`, `groupby`等）。例如，系统可自主生成“性别标准化的左心室质量Z分数”，其中常数2.7（异速缩放因子）由LLM从医学知识中直接提取。
- **迭代搜索流程**：每轮迭代（最多T轮，早停P=3次连续拒绝）：
  1. Analyst计算ΔAIC（线性 vs 样条模型）、分布指标、互信息、ICC等，压缩为每个特征的推荐。
  2. Proposer按2:2:1比例生成三类候选：医学先验（40%）、统计变换（40%）、探索性数学（20%），每个表达式经AST解析器校验，失败时可重试两次。
  3. Verifier多阶段筛选：单特征ΔAUC、ElasticNet稳定性（50次bootstrap）、去重、LightGBM+SHAP排序。
  4. 全局接受：在验证集上，若增强的特征集在4个分类器（SVM, LDA, AdaBoost, MLP）中至少2个上提升复合指标S=0.5×AUC+0.5×Recall，则接受，否则丢弃并反馈。
- **可解释性**：最终输出的复合表型为封闭公式，带有构建轨迹和交叉验证证据摘要。

## 3. 实验设计：数据集、场景、基准与对比方法

- **数据集**：UK Biobank心脏影像队列，包含26,893名参与者。提取标准化CMR表型（LV, RV, LA, RA, AAo, DAo等），共K=9个疾病类别：高血压、高胆固醇、心脏病、周围血管疾病（PVD）、中风、哮喘、COPD、糖尿病、抑郁症。
- **数据划分**：训练:验证:测试 = 6:2:2。所有特征工程和选择仅在训练集上进行，验证集用于选择和调参，测试集最终评估一次。
- **基准方法**：
  - **Expert-defined features**：来自Bai et al. (Nature Medicine 2020) 的专家选择特征集。
  - **MESHAgents features**：基于多智能体对话的特征选择方法（Zhang et al., MICCAI 2025）。
  - **Ours (GPT-5.1)** 和 **Ours (DeepSeek V3.2)**：使用不同LLM后端作为Analyst/Proposer的CPAgents变体。
- **评估指标**：AUC（ROC曲线下面积）和Recall；额外用Silhouette分数衡量类分离性。
- **分类器**：SVM、LDA、AdaBoost、MLP。

## 4. 资源与算力

- **文中明确说明**：所有实验在**单张NVIDIA RTX 6000 GPU**上运行。每次搜索使用固定随机种子，所有指标在5次独立运行（不同随机种子）上取平均值以确保稳健性。
- **未说明训练时长**，但指出特征工程和选择限制在训练集，计算开销可控（每次迭代最多C_max=特征数，早停P=3）。

## 5. 实验数量与充分性

- **实验数量**：
  - **主实验**：4个分类器 × 9种疾病 × 2个指标（AUC, Recall）= 72个性能数字，与两个基线方法比较。排名分析覆盖完整72组合。
  - **消融实验**：对Analyst、Verifier、Feedback、SHAP Ranking四个模块分别去除，报告平均AUC和Recall。
  - **可解释性分析**：计算Silhouette分数（×10）对比原始特征、专家特征、MESHAgent、成分特征和复合特征。
  - **关联热图**：展示12个表型与9种疾病的log(Odds Ratio)。
- **充分性与客观性**：
  - 实验设计规范：数据划分防泄漏、多折交叉验证、独立测试集、多次重复取均值和标准差。
  - 对比基线包括领域强基线（专家特征）和另一个LLM驱动方法（MESHAgents）。
  - 消融实验验证各模块贡献，其中Verifier缺失性能下降最显著，表明过滤步骤关键。
  - 排名分析显示优势并非集中在少数任务，而是跨疾病一致提升。
- **潜在不足**：仅在一个数据集（UK Biobank）上评估，未进行外部验证；疾病类别定义依赖于UK Biobank编码；对比方法仅两个专家/LLM基线，未与更广泛的符号回归或自动特征工程方法（如FEAT）比较。

## 6. 论文的主要结论与发现

1. CP Agents发现的复合表型在所有9种疾病上均提升判别性能，在72个分类器-疾病-指标组合中，CPAgents变体在56个中取得最优排名（基线仅18个）。
2. 复合表型在可解释性上显著优于原始特征：Silhouette分数从0.029提升至0.041（相对提升41%），且大于专家特征（0.028）和MESHAgent（0.011）。
3. Verifier模块最关键，去除后AUC从0.686暴跌至0.590，表明多阶段稳健过滤对于防止过拟合和噪声特征至关重要。
4. 系统能自动生成临床有意义的复合指标（如性别分层的LV质量Z分数），无需人工介入领域常数。
5. 关联热图显示复合表型产生更集中、一致的关联模式，支持更好的聚类级可解释性。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：首次将多智能体闭环（分析-提议-验证）引入影像表型发现，结合LLM医学先验与统计学诊断，而不是盲目组合搜索。
- **可解释性与透明度**：生成封闭公式，带有构建轨迹和证据摘要，便于临床审计和下游复制。
- **鲁棒性保证**：采用多阶段筛选（边际AUC、ElasticNet稳定性、SHAP排序）、跨模型集成接受标准，有效控制过拟合。
- **临床实用性**：自动发现类似“Z-score of indexed LVM”等经典但需要领域知识的指标，降低人工专家成本。
- **实验设计严谨**：数据分离、多折交叉、多次重复、早停机制、防泄露。
- **消融全面**：各组件贡献明确，特别是Verifier的必要性清晰。

## 8. 不足与局限

- **外部验证缺失**：仅在UK Biobank单中心队列验证，未在独立外部数据集（如Sainsbury's Heart Study或Asian cohort）验证泛化性。
- **疾病标签定义**：疾病标签为UK Biobank自报或ICD编码，可能存在不准确或偏倚。
- **对比基线有限**：未与更通用的自动特征构造方法（如Symbolic Regression、AutoGluon-Tabular、DIFER）比较，MESHAgent本身也基于LLM，成本类似。
- **计算成本未量化**：虽说明单GPU，但未报告LLM调用次数及总体运行时间，可能对大样本队列有资源挑战。
- **复合表型逻辑可解释性**：虽然公式简洁，但如“RAESV / (Height^2.7)”可能对非专家仍较复杂；需更多临床验证其合理性。
- **潜在过拟合风险**：尽管有交叉验证和稳定性筛选，迭代过程中可能仍存在多次检验膨胀，文中未做多重比较校正（如Bonferroni）。
- **仅考虑横截面数据**：未利用纵向随访信息或时间序列变化。
- **LLM依赖**：生成质量依赖DeepSeek-V3.2/GPT-5.1；不同LLM版本或闭源模型可能影响可重复性。

（完）
