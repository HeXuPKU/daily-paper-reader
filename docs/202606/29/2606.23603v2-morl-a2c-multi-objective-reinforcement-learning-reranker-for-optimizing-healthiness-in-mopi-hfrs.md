---
title: "MORL-A2C: Multi-Objective Reinforcement Learning Reranker for Optimizing Healthiness in MOPI-HFRS"
title_zh: "MORL-A2C: 用于优化MOPI-HFRS中健康性的多目标强化学习重排序器"
authors: "Aarya Vasantlal, Joshua Zolla, Chuxu Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23603v2"
tags: ["query:med-ai"]
score: 8.0
evidence: 强化学习用于健康饮食推荐
tldr: 针对推荐系统忽略营养健康的问题，MOPI-HFRS通过帕累托优化联合优化偏好、健康和多样性，但采用静态单步折衷方案。本文提出MORL-A2C，将推荐建模为K步重排序问题，利用冻结GNN嵌入和优势演员-评论家算法，结合行为克隆预热策略，在召回率略有下降的情况下显著提升健康对齐分数。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23603v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 782, \"height\": 669}, {\"url\": \"assets/figures/arxiv/2606.23603v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1293, \"height\": 947}, {\"url\": \"assets/figures/arxiv/2606.23603v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1305, \"height\": 503}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23603v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 543, \"height\": 749}, {\"url\": \"assets/tables/arxiv/2606.23603v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 948, \"height\": 448}]"
motivation: 现有健康食品推荐系统MOPI-HFRS使用静态折衷，无法捕捉饮食决策的序列性，需要更有效的多目标优化方法。
method: MORL-A2C将推荐视为K步重排序问题，使用标量化相关/健康奖励的优势演员-评论家算法，并通过行为克隆从冻结嵌入的点积排序器初始化策略。
result: "在宏营养素基准上，Recall@20从25.64%降至23.61%，但H-Score@20从46.05%提升至69.57%。"
conclusion: 策略驱动的序列优化能有效平衡健康与偏好的权衡，在多目标食品推荐中具有潜力。
---

## 摘要
不健康的饮食行为仍然是美国一个持续存在的公共卫生问题，而推荐系统优先考虑用户偏好却忽视营养健康，加剧了这一问题。本文所扩展的多目标个性化可解释健康感知食物推荐系统（MOPI-HFRS）通过基于帕累托优化联合优化偏好、健康和多样性来解决这一问题。然而，这种方法依赖于静态的、每步权衡的解决方案，未能捕捉饮食决策的序列性质。我们引入MORL-A2C，这是MOPI-HFRS的一种面向健康-偏好轴的序列决策扩展。利用冻结的图神经网络嵌入，MORL-A2C将推荐形式化为一个使用优势演员-评论家算法及标量化相关性/健康奖励的K步重排序问题。通过从冻结嵌入导出的点积排序器进行行为克隆来热启动策略。我们还发现并纠正了MOPI-HFRS评估管道中一个非平凡的错误，该错误低估了基线性能；所有结果均针对修正后的基线报告。在宏量营养素基准上，MORL-A2C在排名质量上略有下降（Recall@20: 25.64%至23.61%，NDCG@20: 23.52%至20.64%），但换来了健康对齐的显著提升（H-Score@20: 46.05%至69.57%），在全营养素基准上也呈现一致趋势。这些发现验证了策略驱动的序列优化能够有效应对多目标食物推荐中的健康-偏好权衡。

## Abstract
Unhealthy dietary behavior continues to be a persistent public health issue in the United States, exacerbated by recommendation systems that prioritize user preference without considering nutritional health. The Multi-Objective Personalized Interpretable Health-aware Food Recommendation System (MOPI-HFRS), from which this work extends, addresses this by jointly optimizing preference, health, and diversity through Pareto-based optimization. However, this approach relies on static, per-step tradeoff solutions that fail to capture the sequential nature of dietary decision-making. We introduce MORL-A2C, a sequential decision-making extension to MOPI-HFRS targeting the health-preference axis. Leveraging frozen GNN embeddings, MORL-A2C formulates recommendation as a K-step reranking problem using an Advantage Actor-Critic algorithm with a scalarized relevance/health reward. The policy is warm-started via behavior cloning against a dot-product ranker derived from frozen embeddings. We also identify and correct a non-trivial bug in the MOPI-HFRS evaluation pipeline that understated baseline performance; all results are reported against the corrected baseline. On the macro-nutrient benchmark, MORL-A2C achieves a modest reduction in ranking quality (Recall@20: 25.64% to 23.61%, NDCG@20: 23.52% to 20.64%) in exchange for a substantial improvement in health alignment (H-Score@20: 46.05% to 69.57%), with consistent trends on the full-nutrient benchmark. These findings validate that policy-driven sequential optimization can effectively navigate the health-preference trade-off in multi-objective food recommendation.

---

## 论文详细总结（自动生成）

# 论文总结：MORL-A2C：多目标强化学习重排序器优化MOPI-HFRS中的健康性

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：在美国，不健康的饮食习惯是持续且加剧的公共卫生问题。2023年所有州成人肥胖率均高于20%，且仅有12.3%的成年人满足水果摄入推荐量、10.0%满足蔬菜摄入推荐量。现有食品推荐系统主要优化用户参与度和短期偏好，忽视长期健康后果。
- **现有工作局限**：前身系统MOPI-HFRS通过帕累托优化（基于多重梯度下降算法MGDA）联合优化用户偏好、个性化健康和营养多样性，但其采用**静态、每步独立的折衷解决方案**，无法捕捉推荐决策的序列性和累积性（例如，推荐某食物在位置t的价值取决于之前推荐了什么）。此外，它优化的是损失函数而非直接优化评价指标（如召回率、健康对齐分数）。
- **研究目标**：提出MORL-A2C，一种**序列决策扩展**，聚焦于健康-偏好权衡轴，利用强化学习将推荐建模为K步重排序问题，以更好地捕捉列表级健康目标。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想

将多目标食物推荐视为一个**马尔可夫决策过程（MDP）**，智能体为每个用户逐步构建Top-K推荐列表。使用**优势演员-评论家算法（A2C）** 学习策略，并通过**行为克隆（BC）** 从强点积排序器热启动，避免初始探索崩溃。

### 2.2 关键技术细节

- **冻结GNN编码器**：使用MOPI-HFRS训练的图神经网络（SGSL编码器）生成用户和食物嵌入（维度d=128），并冻结，仅训练轻量级策略网络（约271k参数，占全模型7%）。
- **状态空间设计**：状态 \( s_t = [h_u; \bar{e}_t; c_t; t/K] \in \mathbb{R}^{2d + |T| + 1} \)，包括冻结用户嵌入 \( h_u \)、已选物品嵌入均值 \( \bar{e}_t \)、累积健康标签覆盖向量 \( c_t \)、归一化时间步 \( t/K \)。
- **动作空间**：每一步从覆盖全部食物目录的候选集中选择一个未选物品（无重复选择）。
- **转移函数**：确定性更新：更新嵌入均值和覆盖向量，删除已选物品。
- **奖励函数**：二元组 \( r_t = (r_t^{\text{rel}}, r_t^{\text{health}}) \)，分别奖励匹配训练正交互物品（相关性）和共享至少一个健康标签（健康对齐）。通过固定系数 \( \beta \) 标量化：\( r_t = r_t^{\text{rel}} + \beta \cdot r_t^{\text{health}} \)。论文选择 \( \beta = 0.5 \)。
- **A2C训练**：使用优势函数 \( A_t = G_t - V_\phi(s_t) \)，联合优化策略损失、价值损失和熵正则项。策略网络由状态MLP和候选MLP组成，输出候选物品的logits。
- **行为克隆预训练**：在A2C之前，使用冻结嵌入的点积排序器（MOPI-HFRS的Top-1物品作为监督目标），通过采样softmax负对数似然损失预训练策略网络，使其复制参考排序器的行为，避免随机探索。

## 3. 实验设计

### 3.1 数据集与基准

- 使用**两个基准**：（1）**宏营养素（macro-nutrient）基准**；（2）**全营养素（full-nutrient）基准**。沿用MOPI-HFRS的数据划分、图编码器和标签方案。
- **评估指标**：Recall@20（相关性检索）、NDCG@20（排序感知相关性）、H-Score@20（个性化健康对齐）。论文指出两个多样性指标（AvgTags@20和%Foods@20）因重现不一致而未被报告。

### 3.2 对比方法

- **原始MOPI-HFRS（报告值）**：论文公开展示的指标。
- **MOPI-HFRS（bug修复）**：作者发现并修正了原评估管道中两个错误（用户嵌入索引错误、排除掩码构造错误），导致基线指标大幅提升（如宏营养素Recall@20从12.73%升至25.64%）。
- **MORL-A2C**：本文方法，使用冻结嵌入和A2C策略。

### 3.3 超参数

- BC预训练：宏营养素50个epoch，全营养素15个epoch，学习率1e-3。
- A2C训练：5000个epoch，学习率1e-4，批次大小64用户/epoch，健康权重 \( \beta = 0.5 \)，熵权重 \( \lambda_e = 1.5 \)（宏）/ 3.0（全），候选池大小=全目录。
- 架构：状态MLP隐藏维度256，2层；候选MLP1层；嵌入维度128。

## 4. 资源与算力

**论文未明确说明**使用的GPU型号、数量或训练时长。仅提到策略网络参数约271k，冻结GNN编码器约3.9M参数，训练成本较低。作者指出该设计适合资源受限场景。

## 5. 实验数量与充分性

- **实验数量**：在两个基准（宏营养素和全营养素）上各报告了三个主要指标，共6组对比结果。进行了 \( \beta \) 超参数扫描（0.1到20），以选择最优权衡。
- **充分性与公平性**：
  - 作者明确发现并修复了原MOPI-HFRS评估管道bug，并承诺所有RL结果基于修复后基线报告，避免了夸大性能。
  - 但论文**未进行消融实验**（例如，分离行为克隆、不同奖励组分的贡献、无RL的启发式重排序基线），也未比较其他MORL方法或非学习基线。
  - 缺少多样性的定量评估（因指标重现问题未能报告）。
  - 总体而言，实验初步验证了核心思想，但在全面性和对比基准丰富性上**不够充分**。

## 6. 主要结论与发现

- **健康对齐显著提升**：在宏营养素基准上，H-Score@20从46.05%提升至69.57%（绝对提升23.52个百分点）；在全营养素基准上，从64.85%升至90.48%（绝对提升25.63个百分点）。
- **排名质量小幅下降**：Recall@20和NDCG@20在宏营养素基准上分别下降约2个和3个百分点（25.64%→23.61%，23.52%→20.64%），全营养素基准上Recall@20从25.34%降至21.53%，NDCG@20从22.40%降至20.40%。
- **策略驱动的序列优化**能够有效导航健康-偏好权衡，但当前公式以一定排名牺牲换取健康提升。
- **修正基线的重要性**：原MOPI-HFRS报告值因评估bug被显著低估，对后续方法开发提供警示。

## 7. 优点

- **方法创新**：将静态帕累托优化扩展为序列决策MDP，直接优化列表级健康目标（如累积标签覆盖），而单次排序器无法表达。
- **模块化设计**：冻结GNN嵌入，仅训练轻量策略网络，实现低资源再利用。
- **行为克隆热启动**：有效避免RL在大动作空间中的早期训练崩溃，使策略从高质量初始化开始。
- **诚实报告**：主动发现并公开评估bug，并基于修正基线报告结果，提高了可信度。

## 8. 不足与局限

- **多样性缺失**：当前奖励不含多样性信号，初步检查表明策略将推荐集中在窄子集上。
- **固定标量化**：使用全局固定 \( \beta \)，无法适应不同用户或不同位置的需求。
- **离线评估**：奖励来自模拟环境，未考虑真实用户反馈。
- **实验不充分**：
  - 缺少消融实验（如无BC、不同 \( \beta \) 值、无RL的重排序基线）。
  - 仅对比MOPI-HFRS（修正版），未与其他MORL方法或传统重排序技术比较。
  - 两个多样性指标因重现问题未报告，阻碍了对整体性能的完整评估。
- **应用局限**：仅针对健康-偏好轴，未将多样性作为显式奖励组件（论文承认作为未来工作）。
- **可重复性风险**：依赖特定MOPI-HFRS实现和NHANES数据集，但未提供完整的实验重现细节（如数据预处理细节、种子等）。

（完）
