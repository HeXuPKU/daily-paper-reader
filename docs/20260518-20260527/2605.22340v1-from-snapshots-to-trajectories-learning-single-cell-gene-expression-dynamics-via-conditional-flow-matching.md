---
title: "From Snapshots to Trajectories: Learning Single-Cell Gene Expression Dynamics via Conditional Flow Matching"
title_zh: 从快照到轨迹：通过条件流匹配学习单细胞基因表达动态
authors: "Siyu Pu, Qingqing Long, Xiaohan Huang, Haotian Chen, Jiajia Wang, Meng Xiao, Xiao Luo, Hengshu Zhu, Yuanchun Zhou, Xuezhi Wang"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22340v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 单细胞基因表达动态的生成式建模
tldr: 单细胞RNA测序在离散时间点采集未配对快照数据，导致时序推断存在模糊性和长期预测分布漂移。本文提出scFM，一种基于条件流匹配的潜变量生成框架：利用熵正则化最优传输构建软流匹配目标，学习双向速度场并通过一致性细化耦合，结合分布对齐与潜在动态正则化缓解漂移。在真实数据集上，scFM在插值与外推任务中均取得更优的分布预测性能，轨迹重建更准确且时序可视化更连贯。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22340v1/fig-001.webp\", \"caption\": \"Figure 1: Given sparsely sampled scRNA-seq snapshots, scFM integrates OT-informed alignment and simulation-free flow matching to learn time-conditioned single-cell dynamics and generate in silico cells at unobserved time points.\", \"page\": 1, \"index\": 1, \"width\": 506, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2605.22340v1/fig-002.webp\", \"caption\": \"Figure 3: Averaged Wasserstein and ℓ2 distance between true and predicted expressions on unmeasured timepoints.\", \"page\": 6, \"index\": 2, \"width\": 484, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2605.22340v1/fig-003.webp\", \"caption\": \"Figure 2: scFM models continuous-time single-cell dynamics in a VAE latent space using a two-stage training framework. (1) A shared VAE is first pretrained to learn compact gene-expression representations. (2) Local OT-conditioned flow matching learns time-dependent velocity fields between adjacent snapshots using entropic optimal transport with a bidirectional fused cost and symmetric supervision. (3) During global trajectory anchoring, representation regularization and periodic global OT anchoring enforce reconstruction fidelity and long-horizon dynamic consistency.\", \"page\": 4, \"index\": 3, \"width\": 950, \"height\": 461}]"
motivation: 现有方法面临未配对快照导致的局部转换模糊和长期预测中误差累积引起的分布漂移问题。
method: scFM利用熵正则化最优传输耦合构造软流匹配目标，学习双向速度场并利用一致性细化耦合，辅以分布对齐与潜在动态正则化。
result: 在真实时间序列scRNA-seq数据集上，scFM在时序插值与外推的分布预测、轨迹重建和可视化连贯性上均优于现有方法。
conclusion: scFM通过耦合条件流匹配与正则化策略，更忠实地恢复潜在时间基因表达动态。
---

## 摘要
单细胞RNA测序（scRNA-seq）提供细胞状态的高维特征，使得基于数据驱动的细胞随时间动态建模成为可能。实践中，时间分辨的scRNA-seq仅在少数离散时间点以未配对的快照群体形式收集，留下了大量时间间隔。这促使在未测量时间点进行轨迹推断。现有方法主要遵循两个方向，最优传输（OT）对齐提供观测快照之间的分布级匹配，而连续时间生成模型通过学习动态支持预测。然而，仍存在两个挑战：（i）未配对的快照使得相邻时间点之间的局部转换模糊，导致监督不稳定；（ii）长期预测依赖于重复积分，其中小的建模误差会累积并导致分布漂移。为应对这些挑战，我们提出单细胞流匹配（scFM），一种基于耦合条件流匹配的潜在生成框架。首先，我们计算相邻快照之间熵正则化的OT耦合，并利用它们构建软加权的流匹配目标，以学习时间相关速度场。其次，我们学习双向速度场，并利用它们的一致性来优化耦合，提高稀疏监督下的时间连贯性。第三，我们引入分布级对齐和潜在动态正则化，以锚定长期展开并减轻漂移。在真实世界的时间序列scRNA-seq数据集上的实验表明，scFM在时间插值和外推方面持续改善了分布预测性能。此外，scFM在缺少中间时间点的情况下产生了更准确的轨迹重建和时间上连贯的可视化，表明对潜在的时序基因表达动态有了更忠实的恢复。

## Abstract
Single-cell RNA sequencing (scRNA-seq) provides high-dimensional profiles of cellular states, enabling data-driven modeling of cellular dynamics over time. In practice, time-resolved scRNA-seq is collected at only a few discrete time points as unpaired snapshot populations, leaving substantial temporal gaps. This motivates trajectory inference at unmeasured time points. Existing methods mainly follow two directions, optimal-transport (OT) alignment provides distribution-level matching between observed snapshots, while continuous-time generative models support forecasting via learned dynamics. However, two challenges remain: (i) unpaired snapshots render local transitions between adjacent time points ambiguous, leading to unstable supervision; and (ii) long-horizon prediction relies on repeated integration, where small modeling errors compound and cause distribution drift. To address these challenges, we propose single-cell Flow Matching (scFM), a latent generative framework based on coupling-conditioned flow matching. First, we compute entropically regularized OT couplings between adjacent snapshots and use them to construct soft, weighted flow-matching targets for learning time-dependent velocity fields. Second, we learn bidirectional velocity fields and leverage their consistency to refine couplings and improve temporal coherence under sparse supervision. Third, we introduce distribution-level alignment and latent dynamic regularization to anchor long rollouts and mitigate drift. Experiments on real-world time-series scRNA-seq datasets show that scFM consistently improves distributional prediction performance for both temporal interpolation and extrapolation. Moreover, scFM yields more accurate trajectory reconstruction and temporally coherent visualizations where intermediate time points are absent, indicating a more faithful recovery of underlying temporal gene expression dynamics.

---

## 论文详细总结（自动生成）

# 论文总结：From Snapshots to Trajectories: Learning Single-Cell Gene Expression Dynamics via Conditional Flow Matching

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：单细胞RNA测序（scRNA-seq）能够以单细胞分辨率测量基因表达，揭示细胞异质性和状态转变。然而，时间分辨的scRNA-seq只能提供少数离散时间点上的未配对快照（unpaired snapshots），即细胞之间没有对应关系，且时间间隔较大，中间状态缺失。
- **核心问题**：
  1. **未配对快照导致局部转换模糊**：相邻时间点之间缺乏细胞级对应关系，使得学习信号本质上是集合值（set-valued），导致监督不稳定，尤其在时间间隔大或轨迹分支/融合时。
  2. **长期预测中的误差累积与分布漂移**：连续时间模型通过反复积分学习速度场预测未来状态，但局部回归误差会随积分步数累积，导致预测分布逐渐偏离真实边缘分布。
- **整体含义**：需要一种生成式框架，能够从稀疏、未配对的快照中恢复连续的细胞发育动态，实现时间插值和外推，同时保持轨迹的忠实性和分布的稳定性。

## 2. 论文提出的方法论

### 核心思想
提出**scFM（Single-Cell Flow Matching）**，一种基于**耦合条件流匹配**的潜在生成框架。通过结合最优传输（OT）对齐与无模拟流匹配（simulation-free flow matching），学习时间相关的速度场，并引入双向速度场、分布级对齐和潜在动态正则化来缓解上述问题。

### 关键技术细节
1. **潜在空间表示**：先训练一个共享的变分自编码器（VAE），将高维基因表达映射到低维潜在空间，降低维度和稀疏性。
2. **双向速度场**：学习正向速度场 \( v^f_\psi(t, z) \) 和反向速度场 \( v^b_\psi(t, z) \)，分别从\( t_0 \)向未来驱动、从\( t_1 \)向过去驱动。通过一致性约束增强时间连贯性。
3. **OT条件化流匹配（OT-conditioned flow matching）**：
   - 在相邻时间点之间计算熵正则化的最优传输耦合 \( \pi \)（使用Sinkhorn算法），得到软配对分布。
   - 基于该耦合构造线性桥接轨迹：从\( z^a_i \)到\( z^b_j \)的线性插值，目标速度为常数 \( u_{ij} = (z^b_j - z^a_i)/(t_b - t_a) \)。
   - 训练速度场使其回归该目标速度，损失函数为 \( L_{FM} = L^f_{FM} + L^b_{FM} \)。采用Top-K近似减少计算量。
4. **全局分布匹配与潜在动态正则化**：
   - **全局Wasserstein分布匹配**（\( L_{OT} \)）：用熵正则化OT距离衡量预测细胞集与真实快照在基因空间中的差异，对所有训练时间点取平均。
   - **潜在动态正则化**（\( L_{dyn} \)）：对齐ODE预测的潜在表示与编码器编码的潜在表示，防止动态偏离VAE流形。
5. **联合训练目标**：\( L = L_{VAE} + \lambda_{FM} L_{FM} + \lambda_{OT} L_{OT} + \lambda_{dyn} L_{dyn} \)，采用周期调度（每 \( K_{OT} \) 步才计算一次全局损失）以减少计算开销。
6. **双向融合成本**（Bidirectional Fused Cost）：初始阶段使用欧氏距离成本，热身期后使用融合成本，结合正向和反向一步预测，增强耦合稳定性。

### 算法流程（文字说明）
- **阶段一**：VAE预训练，对所有快照的细胞集合最小化重构损失和KL散度。
- **阶段二**：动态学习。每步采样相邻时间点的小批量，编码为潜在表示，计算熵OT耦合（按阶段选择成本函数），然后构造桥接并计算流匹配损失。周期性地执行长程ODE展开，计算全局损失并反向传播。联合更新VAE和速度场参数。

## 3. 实验设计

### 数据集
使用三个真实时间序列scRNA-seq数据集，覆盖不同生物过程：
- **ZB**（斑马鱼胚胎）：38,731个细胞，12个时间点。
- **DR**（果蝇）：27,386个细胞，11个时间点。
- **SC**（Schiebinger2019，重编程）：236,285个细胞，19个时间点。

所有数据集统一预处理：文库大小归一化、log(1+x)转换、保留前2,000个高度可变基因（HVGs）。

### Benchmark设置
- **多留出协议**：训练时留出若干时间点，用于插值（中间时间点）和外推（最后时间点之后）。具体留出点见表1。
- **评估指标**：
  - 2-Wasserstein距离（Sinkhorn近似）
  - 平均成对ℓ2距离
  在每个留出时间点计算，然后平均跨所有留出点。

### 对比方法
- **WOT**：纯最优传输对齐（仅插值）。
- **scNODE**：VAE+潜在神经ODE。
- **PRESCIENT**：基于能量景观的连续时间模型。
- **VGFM**：流匹配方法。
- **Naive**：直接将最后一个观测时间点复制作为预测（非学习基线）。

## 4. 资源与算力

论文中**未明确说明**所使用的GPU型号、数量或训练时长。仅在实现细节中提到使用50维潜在空间、256隐藏单元残差网络、正弦时间嵌入（64维）、RK4积分器（步长0.1）。未提供算力资源细节。

## 5. 实验数量与充分性

- **主要实验**：在三个数据集上进行了插值和外推两种任务，每个任务多留出时间点。结果以柱状图（图3）和详细表格（表2）呈现，展示每个留出点上的Wasserstein和ℓ2距离。
- **轨迹恢复实验**（§5.3）：采用remove-recovery协议，用PAGA图计算Ipsen–Mikhailov (IM)距离，定性展示（图4）。
- **可视化**（§5.4）：UMAP投影定性展示插值效果（图5）。
- **消融分析**：论文未专门设计消融实验（如移除双向速度场、全局正则化等），但从方法设计上包含了多个组件。没有单独拆解每个组件的贡献。这是一个不足。
- **充分性评价**：实验覆盖了三种不同规模的数据集，对比了多个代表性基线，评估指标客观（Wasserstein和ℓ2），但缺乏严格的消融研究和统计显著性检验。总体较为充分但仍有提升空间。

## 6. 论文的主要结论与发现

- scFM在插值任务中，与最优传输基线（WOT）竞争，同时显著优于神经ODE基线（scNODE、PRESCIENT），表明连续时间动态学习不会牺牲中间时间点的重建质量。
- 在外推任务中，scFM保持鲁棒的长期预测精度，随预测时间步长增加性能退化远小于其他方法，体现更强的外推能力。
- 轨迹恢复实验中，scFM预测的细胞能更好地填补缺失时间点，恢复PAGA图的拓扑结构（更低IM距离）。
- UMAP可视化显示，scFM生成的插值轨迹保持了全局流形几何和时间梯度，与实际发育动态一致。

## 7. 优点

- **创新性结合**：将OT耦合与流匹配紧密集成，从分布级对齐转化为软细胞级监督，显著提升了速度场学习的信息量和稳定性。
- **双向速度场**：利用前后双向信息约束局部转换，增强耦合的鲁棒性和时间连贯性。
- **全局锚定机制**：通过分布匹配和潜在正则化有效抑制长期预测中的分布漂移，这是现有方法较少考虑的。
- **灵活性和泛化性**：支持任意时间点查询，单模型即可完成插值和外推，无需为每个留出点单独训练。
- **理论分析**：提供了关于ODE适定性、Wasserstein稳定性、泛化界和收敛性的理论推导（见附录），增强了方法的可信度。

## 8. 不足与局限

- **缺乏消融实验**：未量化每个组件（双向速度场、全局正则化、软OT耦合等）的单独贡献，难以确定哪些设计最关键。
- **计算效率未说明**：没有讨论训练和推理时间，对比方法的运行效率，以及大规模数据集的扩展性。全局损失（L_OT和L_dyn）需要每K步执行长程ODE卷展，可能带来额外计算负担。
- **假设与限制**：
  - 依赖VAE潜在空间的质量；如果VAE不能很好地保存拓扑结构，动态学习可能受损。
  - 需要预先指定时间点顺序和区间大小，对不规则采样或缺失时间点的鲁棒性未探讨。
  - 未考虑细胞增殖/死亡（不平衡质量传输），虽然OT耦合使用均匀边际，但实际发育可能涉及细胞数量变化。
- **评估指标单一**：仅使用Wasserstein和ℓ2距离，未采用生物学相关指标（如基因调控网络恢复、标记基因表达模式、细胞类型比例等）。
- **基线覆盖有限**：未与较新的方法如CellStream、DeepRUOT、MIOFlow等对比，仅对比了部分经典方法。
- **数据预处理限制**：仅使用2,000 HVGs，可能丢失低表达但重要的时序基因信息。

（完）
