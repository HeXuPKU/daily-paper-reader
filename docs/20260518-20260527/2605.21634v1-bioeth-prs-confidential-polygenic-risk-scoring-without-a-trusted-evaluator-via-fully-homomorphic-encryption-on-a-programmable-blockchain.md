---
title: "bioETH-PRS: Confidential Polygenic Risk Scoring without a Trusted Evaluator via Fully Homomorphic Encryption on a Programmable Blockchain"
title_zh: bioETH-PRS：通过可编程区块链上的全同态加密实现无需可信评估器的保密多基因风险评分
authors: "Kimon Antonios Provatas, Christos Galanopoulos, Ilias Georgakopoulos-Soares"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21634v1"
tags: ["query:gwas"]
score: 9.0
evidence: 使用同态加密的新型PRS计算算法
tldr: "针对多基因风险评分中基因型数据暴露及依赖可信评估器的问题，提出bioETH-PRS协议。利用支持全同态加密的区块链智能合约，采用TFHE方案在加密域内完成PRS点积计算，引入定点量化方案精准表示GWAS权重。四合约架构实现数据、模型、计算和输出的隔离，并设计流式路径降低37% gas消耗。链上噪声输出机制保护原始分数，原型评估显示线性扩展性及低部署成本潜力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21634v1/fig-001.webp\", \"caption\": \"Figure 4: Execution protocol comparison. The classic chunked path persists SNP handles in contract storage (SSTORE), enabling multi-party upload/compute separation. The streaming path uses EIP-1153 transient storage (TSTORE), eliminating two SSTORE operations per SNP and reducing total gas by 37%. Both paths finalize through the Result Oracle.\", \"page\": 6, \"index\": 1, \"width\": 517, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2605.21634v1/fig-002.webp\", \"caption\": \"Figure 1: Graphical Abstract. bioETH-PRS replaces the trusted evaluator of conventional homomorphic PRS pipelines with consensus-enforced smart contracts. Left: Traditional centralised approaches transmit raw genotypes to a cloud evaluator, requiring user trust in a designated third party. Right: bioETH-PRS keeps all genomic data and model weights encrypted throughout, with computation verified by blockchain consensus. Key results: 37% gas reduction via streaming execution; machine-epsilon quantisation accuracy on real GWAS fixtures; post-quantum security under TFHE/RLWE.\", \"page\": 2, \"index\": 2, \"width\": 1067, \"height\": 585}, {\"url\": \"assets/figures/arxiv/2605.21634v1/fig-003.webp\", \"caption\": \"Figure 5: Security threat model and defence layers. The TFHE/RLWE hardness assumption grounds the innermost layer. Successive layers—FHE coprocessor, smart contracts, blockchain consensus—add verifiable security properties. Adversary capabilities and corresponding protocol guarantees are enumerated at the threat-boundary interface.\", \"page\": 7, \"index\": 3, \"width\": 517, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2605.21634v1/fig-004.webp\", \"caption\": \"Figure 6: On-chain gas consumption versus SNP count for both execution paths. Gas scales linearly (O(N)) in both cases. The streaming path achieves 35.5–37.2% gas reduction over the classic chunked path by eliminating persistent SSTORE operations for intermediate SNP ciphertext handles.\", \"page\": 8, \"index\": 4, \"width\": 517, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2605.21634v1/fig-005.webp\", \"caption\": \"Table 1: Architectural comparison: HEPRS vs. bioETH-PRS\", \"page\": 4, \"index\": 5, \"width\": 517, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2605.21634v1/fig-006.webp\", \"caption\": \"Figure 2: System architecture. Four on-chain smart contracts form a linear pipeline from genomic data custody to privacypreserving risk output. The fhEVM coprocessor manages off-chain ciphertexts; only opaque 32-byte handles are stored on-chain. No plaintext genotype data or model weights are observable at any stage.\", \"page\": 4, \"index\": 6, \"width\": 517, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2605.21634v1/fig-007.webp\", \"caption\": \"Table 4: Per-SNP gas decomposition\", \"page\": 9, \"index\": 7, \"width\": 517, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2605.21634v1/fig-008.webp\", \"caption\": \"Figure 3: Three-step fixed-point quantisation scheme. Signed GWAS floats are (1) scaled by s, (2) shifted by the weight zeropoint zw to guarantee non-negative weights, and (3) shifted by the score zero-point zs to guarantee a non-negative encoded score. Decoding inverts all three steps after decryption.\", \"page\": 5, \"index\": 8, \"width\": 517, \"height\": 283}]"
motivation: 现有基因风险评分计算需暴露原始数据或依赖可信第三方，存在隐私和信任风险。
method: 基于fhEVM区块链的TFHE全同态加密，采用定点量化与四合约架构实现隐私保护点积计算。
result: "流式路径降低37% gas，量化误差达机器精度，线性gas扩展验证可行性。"
conclusion: bioETH-PRS在低gas环境中具备成本竞争力，为基因组隐私计算提供去信任化方案。
---

## 摘要
多基因风险评分（PRS）汇总遗传效应估计值以预测疾病易感性，但临床部署通常会将原始基因型数据暴露给第三方计算基础设施。先前的同态加密方法仍需要信任指定的评估器。我们提出 bioETH-PRS 协议，该协议用支持全同态加密（fhEVM）的区块链上的不可变智能合约取代了评估器角色。bioETH-PRS 采用整数精确 TFHE 方案，在加密域内完全计算 PRS 点积，在整个执行过程中对外部方隐藏基因型剂量向量和 GWAS 权重向量。我们引入了一种三步定点量化方案，将有符号的 GWAS 权重表示为无符号 64 位整数，在验证基准上实现了机器精度的重建精度。四合约架构分离了数据托管、模型发布、计算和输出发布，并支持经典的分块路径和流式路径，后者将模拟测量的燃料消耗降低了 37%。链上噪声输出预言机发出加密的噪声评分句柄和可公开解密的三元类别，减少了原始分数暴露和探测风险。在真实 GWAS 基准上的原型评估证实了线性燃料扩展，并表明该方法在低燃料部署环境中可能具有成本竞争力。

## Abstract
Polygenic risk scores (PRSs) aggregate genetic effect estimates to predict disease susceptibility, yet clinical deployment often exposes raw genotype data to third-party compute infrastructure. Prior homomorphic-encryption approaches, still require trust in a designated evaluator. We present bioETH-PRS, a protocol that replaces that evaluator role with immutable smart contracts on a blockchain supporting Fully Homomorphic Encryption (fhEVM). Using the integer-exact TFHE scheme, bioETH-PRS computes the PRS dot product entirely within the encrypted domain, keeping both genotype dosage vectors and GWAS weight vectors hidden from external parties throughout execution. We introduce a three-step fixed-point quantisation scheme for representing signed GWAS weights as unsigned 64-bit integers, achieving machine-epsilon reconstruction accuracy on validated fixtures. A four-contract architecture separates data custody, model publication, computation, and output release, and supports both a classic chunked path and a streaming path, with the latter reducing mock-measured gas by 37%. An on-chain noisy output oracle emits an encrypted noisy-score handle and a publicly decryptable ternary category, reducing raw score exposure and probing risk. Prototype evaluation on real GWAS fixtures confirms linear gas scaling and suggests that the approach may be cost-competitive in low-gas deployment environments.

---

## 论文详细总结（自动生成）

# bioETH-PRS 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多基因风险评分（PRS）的临床部署通常需要将患者的原始基因型数据暴露给第三方计算基础设施（如云端评估器），同时GWAS模型权重也面临泄露风险。已有的同态加密方案（如HEPRS）虽然保护了计算过程中的数据，但仍然依赖一个**可信的评估器**（第三方），无法防止评估器与客户端合谋泄露模型权重。
- **研究动机**：在精度医学中，PRS是预测疾病易感性的重要工具，但基因型数据具有永久性、可识别性，隐私保护要求极高。同时，模型权重源于受限研究队列，同样敏感。现有中心化方法无法同时保护患者基因型和模型权重。
- **整体含义**：提出一种**无需可信评估器**的PRS计算协议，将信任从单个评估器转移到**可审计的智能合约、区块链共识和FHE协处理器栈**上，真正实现端到端的加密计算与访问控制。

## 2. 论文提出的方法论：核心思想、关键技术细节
### 核心思想
- 用**支持全同态加密（fhEVM）的区块链**取代传统同态加密方案中的可信评估器。通过**四层智能合约**（数据注册表、模型市场、PRS计算引擎、结果预言机）分离数据托管、模型发布、计算执行和输出发布，各合约独立可审计。
- 使用**整数精确TFHE方案**（而非CKKS的近似算术），实现加密域内的PRS点积计算，所有中间值以32字节不透明句柄表示，纯文本仅存在于协处理器外部。

### 关键技术细节
1. **三步定点量化方案**：将带符号的浮点型GWAS权重转换为无符号64位整数，确保无符号整型不溢出。
   - Step 1：缩放 `qi = round(s * βi)`
   - Step 2：权重零点偏移 `zw = -min(qi)`，`ui = qi + zw`
   - Step 3：分数零点偏移 `zs = sum(2|qi| for qi<0)`，最终编码分数 `e = Σ(gi*ui) - zw*G + zs`（G为基因型剂量和）。解码时 `PRS = (e - zs)/s`。
   - 提供自动量化顾问选择最小可行缩放因子（推荐 `s≈10^6`），达到机器精度误差。
2. **四合约架构**：Genomic Registry（数据托管+ACL）、Model Marketplace（模型发布+元数据）、PRS Compute Engine（计算状态机）、Result Oracle（噪声输出+分类）。支持两种执行路径：
   - **经典分块路径**：将SNP句柄持久化到合约存储（SSTORE），支持多参与方上传/计算分离。
   - **流式路径**：使用EIP-1153瞬态存储（TSTORE），上传与计算合并为单个交易，消除两次SSTORE操作，燃料消耗降低37%。
3. **链上噪声输出预言机**：对最终编码分数添加均匀噪声 `ν ∼ Uniform(0, B)`，输出加密噪声分数句柄和可公开解密的三元风险类别（低/中/高）。阈值间距要求 `τH - τL ≥ B`，防止确定性映射。通过加密选择操作避免侧信道泄漏。
4. **抗探测速率限制**：每个模型、钱包、样本在W个区块窗口内最多R次查询（推荐R=3, W=1000），提取一个20-bit权重大约需要2800小时（12s/块）。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：使用HEPRS参考数据集中的真实GWAS基准（精神分裂症模型），包括**四个规模**：100、500、1000、5000个SNP。每个规模有50个个体。
- **基准（Benchmark）**：以浮点精确PRS计算结果作为真值，对比量化重建误差（MAE、机器精度）。在量化精度验证中，对所有200个个体-组合（4 × 50）进行检验。
- **对比方法**：主要对比**经典分块路径 vs 流式路径**的燃料消耗、延迟。没有与其他同态加密方案（如HEPRS）进行直接横向性能对比（因为fheVM环境与CKKS环境差异大），但进行了架构比较（表1）。
- **模拟环境**：使用Hardhat内存模拟协处理器，验证完整fhEVM协议（句柄、ACL、输入证明），但算术由纯文本执行。燃料数字预计与真实网络相差10-20%。

## 4. 资源与算力
- **计算资源**：实验在**Hardhat本地模拟协处理器**上运行，没有使用GPU。端到端延迟在单机模拟环境中测量（100 SNPs约386ms，5000 SNPs约14.5s）。文中未提及GPU型号、数量或训练时长，因为方案不涉及模型训练，仅进行加密推理。
- **实际网络成本**：基于模拟燃料消耗，估算了L1（30 gwei）、L2（0.05 gwei）和应用链（0.001 gwei）下的美元成本（假设ETH $3000）。作者指出L1成本过高（100 SNPs需$1035），L2或专用链才有经济可行性。

## 5. 实验数量与充分性
- **量化精度实验**：4个SNP规模 × 50个体 = 200个组合，全部验证通过（机器精度误差）。规模虽小但覆盖了典型PRS模型上限（5000 SNPs）。
- **燃料消耗实验**：每个规模分别测试经典和流式路径，记录总燃料和每个SNP的分解（表3、表4）。结果线性趋势明确，流式路径稳定降低37%左右。
- **协议正确性验证**：通过**134个自动化测试用例**，验证了ACL强制执行、状态机完整性、单次finalize保证、最小阈值间距、预言机必需模式、速率限制窗口行为、ACL撤销处理等安全性不变性。
- **HCU上限探测**：实验确定每个交易可执行60-74次FHE操作（20 SNPs chunk通过，25 SNPs失败）。
- **充分性评价**：
  - **优点**：覆盖了从100到5000 SNP的多级规模，测试了两种执行路径，完成了全面的安全属性和正确性验证。
  - **不足**：仅使用一个疾病模型（精神分裂症）的GWAS权重，未评估其他疾病或不同量化参数下的泛化能力。模拟环境而非真实网络，燃料数字可能偏差10-20%。没有对比现有中心化PRS计算（如PLINK）的精度损失或速度。速率限制的有效性分析仅为理论估算，未进行实际攻击模拟。

## 6. 论文的主要结论与发现
- **主要结论**：bioETH-PRS成功实现了无需可信评估器的加密PRS计算，在100–5000 SNP范围内技术上可行，在低燃料环境中（L2或应用链）可能具有成本竞争力（100–500 SNP分析预计$1.72–$7.95）。
- **关键发现**：
  - 三步量化方案在 `s ≈ 10^6` 时达到机器精度重建误差。
  - 流式路径相较于经典分块路径降低37%燃料消耗（消除两次SSTORE）。
  - 线性燃料扩展（O(N)）确认无隐藏二次开销。
  - 噪声输出+速率限制有效提升抗探测成本（提取单个权重需数千小时）。
- **局限性承认**：SNP数量上限受HCU预算限制（每个chunk 20 SNPs），无法扩展到全基因组（>50,000 SNPs）；数据源URI在链上可见；噪声引入向上偏差；L1燃料成本不可行。

## 7. 优点：方法或实验设计上的亮点
- **架构创新**：四合约分离数据、模型、计算和输出，各合约独立审计和升级，降低单点故障风险。
- **量化方案设计**：三步编码保证无符号整数不溢出，并给出形式化证明（4sMN ≤ 2^64-1），机器精度误差在真实数据上验证。
- **流式执行路径**：利用瞬态存储消除持久化开销，从工程角度显著降低链上成本，并保持计算确定性。
- **抗探测机制**：采用加密噪声 + 速率限制，且噪声来源为链上随机性（抗调用者操纵），强制噪声输出模式可防止原始分数旁路。
- **安全模型清晰**：形式化定义了五项不变性（密文不透明性、ACL门控解密、无原始分公开、预言机强制、单次finalize），并进行了全面自动化测试（134个用例）。
- **成本投影务实**：给出了多种gas定价场景下的美元成本估算，帮助读者理解实际经济限制。

## 8. 不足与局限：实验覆盖、偏差风险、应用限制
- **实验覆盖局限**：
  - 仅测试了一个疾病模型（精神分裂症）的四种SNP规模，未验证不同量化参数、不同权重分布下的稳定性。
  - 未在真实区块链网络（如Sepolia测试网）部署验证，模拟环境燃料数据可能偏离真实情况10-20%。
  - 未与现有同态加密PRS方案（如HEPRS）进行端到端性能对比（原因：环境异构，但可给出理论分析和延迟比较）。
- **偏差风险**：
  - 噪声输出机制为均匀噪声（单向），引入向上偏差（B/2），作者建议调整阈值但未消除系统性偏差。真正的零均值噪声需要有符号整数运算，当前fhEVM不支持。
  - 速率限制理论分析基于信息论，未模拟实际攻击（如自适应探测策略），低估可能。
- **应用限制**：
  - SNP数量上限（5,000）只能覆盖部分临床PRS模型，全基因组模型（>50,000 SNPs）仍依赖HEPRS。
  - 依赖fhEVM协处理器栈和区块链共识，系统安全性受限于RLWE假设、合约代码正确性和区块链活性。如果协处理器被攻破，整个隐私性失效。
  - 无法验证提交的加密SNP值是否真实对应注册样本（只验证ACL访问权限，不验证数据真实性），恶意请求者可能提交任意密文。
  - L1部署经济不可行（100 SNPs需$1035），实际部署需要专用L2或应用链，增加了基础设施依赖。

（完）
