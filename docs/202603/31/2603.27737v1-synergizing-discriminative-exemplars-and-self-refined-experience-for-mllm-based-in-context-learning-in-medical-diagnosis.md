---
title: Synergizing Discriminative Exemplars and Self-Refined Experience for MLLM-based In-Context Learning in Medical Diagnosis
title_zh: 协同判别性范例与自我优化经验，用于医学诊断中基于多模态大语言模型的上下文学习
authors: "Wenkai Zhao, Zipei Wang, Mengjie Fang, Di Dong, Jie Tian, Lingwei Zhang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27737v1"
tags: ["query:med-ai"]
score: 10.0
evidence: 基于多模态大模型的医疗诊断上下文学习
tldr: 针对通用多模态大模型在医疗诊断中难以捕捉领域细节且微调成本高的问题，本文提出一种模拟临床医生工作流的上下文学习框架。该框架结合了判别性样本核心集选择（DECS）和自我精炼经验总结（SRES），通过筛选视觉锚点案例和动态提炼诊断经验，在无需更新模型参数的情况下显著提升了诊断性能。在MedMNIST基准测试中，该方法超越了零样本模型，达到了与全监督模型相当的水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27737v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the proposed Clinician Mimetic Workflow. (a) The diagnostic cognitive process of human clinicians. (b) Discriminative Exemplar Coreset Selection, where data points of the same color belong to the same class. (c) Self-Refined Experience Summarization, dynamically updating the Experience Bank via reward-guided rollouts.\", \"page\": 3, \"index\": 1, \"width\": 723, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2603.27737v1/fig-002.webp\", \"caption\": \"Fig. 2. Illustration of the Self-Refined Experience Summarization. Texts highlighted in identical colors map the critical semantic discrepancies between distinct rollouts directly to the resulting synthesized Diagnostic Heuristics.\", \"page\": 5, \"index\": 2, \"width\": 723, \"height\": 484}, {\"url\": \"assets/figures/arxiv/2603.27737v1/fig-003.webp\", \"caption\": \"Table 1. Comparison of classification accuracy on the 12 MedMNIST 2D datasets. The best result in each column is highlighted in bold, and the second-best result is underlined.\", \"page\": 7, \"index\": 3, \"width\": 725, \"height\": 736}]"
motivation: 通用多模态大模型在医疗诊断中表现不足，且全量微调面临专家标注成本高和计算开销大的挑战。
method: 提出一种模拟临床医生工作流的框架，通过DECS筛选判别性视觉样本并利用SRES动态提炼文本经验库。
result: 在MedMNIST 2D的12个数据集上，该方法优于零样本通用及医疗大模型，并达到与全监督模型及微调模型相当的性能。
conclusion: 该研究为参数高效的医疗上下文学习设定了新基准，证明了结合视觉样本选择与经验总结在提升医疗诊断能力方面的有效性。
---

## 摘要
通用多模态大语言模型（MLLMs）在捕捉医学诊断中的领域特定细微差别方面通常表现不佳，落后于全监督基准。虽然微调提供了一种补救措施，但专家标注的高昂成本和巨大的计算开销限制了其可扩展性。为了在不更新 MLLM 预训练主干权重的情况下弥补这一差距，我们提出了一种临床医生模拟工作流（Clinician Mimetic Workflow）。这是一个新颖的上下文学习（ICL）框架，旨在协同判别性范例核心集选择（DECS）和自我优化经验总结（SRES）。具体而言，DECS 通过在计算层面从噪声数据中选择具有判别性的视觉核心集，模拟了临床医生参考“锚点病例”的能力；同时，SRES 通过将多样的 rollout 蒸馏到动态文本经验库中，模拟了临床诊断中的认知与反思。在 MedMNIST 2D 基准测试的所有 12 个数据集上进行的广泛评估表明，我们的方法优于零样本通用和医学 MLLM。同时，它达到了与全监督视觉模型和领域特定微调 MLLM 相当的性能水平，为参数高效的医学上下文学习树立了新的基准。我们的代码可在匿名仓库获取：https://anonymous.4open.science/r/Synergizing-Discriminative-Exemplars-and-Self-Refined-Experience-ED74。

## Abstract
General Multimodal Large Language Models (MLLMs) often underperform in capturing domain-specific nuances in medical diagnosis, trailing behind fully supervised baselines. Although fine-tuning provides a remedy, the high costs of expert annotation and massive computational overhead limit its scalability. To bridge this gap without updating the weights of the pre-trained backbone of the MLLM, we propose a Clinician Mimetic Workflow. This is a novel In-Context Learning (ICL) framework designed to synergize Discriminative Exemplar Coreset Selection (DECS) and Self-Refined Experience Summarization (SRES). Specifically, DECS simulates a clinician's ability to reference "anchor cases" by selecting discriminative visual coresets from noisy data at the computational level; meanwhile, SRES mimics the cognition and reflection in clinical diagnosis by distilling diverse rollouts into a dynamic textual Experience Bank. Extensive evaluation across all 12 datasets of the MedMNIST 2D benchmark demonstrates that our method outperforms zero-shot general and medical MLLMs. Simultaneously, it achieves performance levels comparable to fully supervised vision models and domain-specific fine-tuned MLLMs, setting a new benchmark for parameter-efficient medical in-context learning. Our code is available at an anonymous repository: https://anonymous.4open.science/r/Synergizing-Discriminative-Exemplars-and-Self-Refined-Experience-ED74.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为“临床医生模拟工作流”（Clinician Mimetic Workflow）的框架，旨在通过上下文学习（In-Context Learning, ICL）提升多模态大语言模型（MLLM）在医学影像诊断中的表现，而无需对模型进行参数微调。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：通用多模态大模型（如 GPT-4、Gemini）在特定医疗诊断任务中表现往往不如全监督的视觉模型。
*   **研究背景**：虽然微调（Fine-tuning）可以提升性能，但医学领域面临专家标注成本极高、计算资源消耗巨大的挑战。
*   **动机**：探索一种无需更新模型权重（Frozen Backbone）的方法，通过模拟临床医生的两种核心认知能力——**参考既往病例（视觉对比）**和**总结诊断经验（逻辑反思）**，来增强模型的领域适应能力。

### 2. 方法论
该框架由两个协同的核心组件组成：

*   **判别性范例核心集选择（DECS）**：
    *   **核心思想**：模拟医生参考“锚点病例”的行为，从带噪声的医疗数据中筛选出最具代表性的视觉样本。
    *   **技术细节**：使用视觉编码器提取特征，通过**类平衡初始化**构建核心集。采用**难样本聚合策略**，将查询图像分配给同类中最不相似的雏形（Prototype），并利用**指数移动平均（EMA）**更新特征中心。这种方法能有效过滤掉对比度、染色差异等无关噪声，强化诊断生物标志物的特征。
*   **自我精炼经验总结（SRES）**：
    *   **核心思想**：模拟医生的反思与规则更新过程，将诊断逻辑转化为动态的文本“经验库”。
    *   **技术细节**：
        1.  **奖励引导的 Rollout**：模型针对同一病例生成多个推理路径。
        2.  **对比总结**：利用 LLM 评估器根据正确性、一致性等临床标准对路径打分，分析高分和低分路径之间的语义差异。
        3.  **经验库更新**：通过“增加、删除、保留、修改”四种策略动态维护经验库，确保诊断启发式规则（Heuristics）的精准度和覆盖面。

### 3. 实验设计
*   **数据集**：采用了 **MedMNIST 2D** 基准测试中的全部 **12 个数据集**，涵盖了 X射线、CT、超声、皮肤镜、病理学等多种模态，涉及二分类、多分类和多标签分类任务。
*   **对比方法（Baselines）**：
    *   **全监督视觉模型**：ResNet50, ViT-B, Swin-B, ConvNeXt-B, SigLIP2-B。
    *   **通用 MLLM**：GPT-4o-mini, Gemini-1.5-flash。
    *   **医疗专用 MLLM**：LLaVA-Med, MedGemma。
    *   **微调后的医疗 MLLM**：BiomedGPT, Hulu-Med。

### 4. 资源与算力
*   **硬件**：实验在单张 **NVIDIA RTX 6000 GPU** 上完成。
*   **模型主干**：使用 **Qwen2-VL-8B** 作为冻结的 MLLM 主干。
*   **训练/优化时长**：文中未明确给出具体的总耗时，但提到 DECS 在全量训练集上优化，而 SRES 仅需 200 个随机训练样本即可高效进化经验库，体现了极高的参数效率。

### 5. 实验数量与充分性
*   **实验规模**：在 12 个不同模态的医学数据集上进行了系统评估，并进行了详细的消融实验（验证 DECS 和 SRES 各自及协同的效果）。
*   **客观性与公平性**：
    *   采用了统一的 N-shot（5-shot）设置。
    *   对比了从传统 CNN 到最新大模型的多种架构。
    *   通过多次独立运行取平均准确率（ACC）来确保统计可靠性。
    *   实验设计较为充分，覆盖了医学影像分类的主要场景。

### 6. 主要结论与发现
*   **性能卓越**：该方法在无需微调的情况下，平均准确率达到 **86.3%**，显著优于零样本的通用和医疗大模型（比 Gemini-1.5-flash 高出 29.2%）。
*   **比肩微调模型**：其表现与全监督视觉模型以及经过昂贵微调的专用医疗大模型（如 BiomedGPT）相当。
*   **协同效应**：消融实验证明，单独使用 DECS 或 SRES 虽有提升，但两者结合能产生最强的协同效应，证明了“视觉锚点+文本经验”双重驱动的有效性。

### 7. 优点
*   **参数高效**：完全不改变大模型权重，极大地降低了部署和适配成本。
*   **可解释性强**：通过模拟临床医生的认知流，生成的诊断启发式规则具有较好的可读性和逻辑性。
*   **鲁棒性**：DECS 算法能有效处理医疗影像中常见的噪声和类内差异问题。

### 8. 不足与局限
*   **多标签分类瓶颈**：在 ChestMNIST（多标签）任务上提升有限。原因是有限的上下文长度难以覆盖 14 种病理的组合证据空间。
*   **奖励偏差**：SRES 的评估器在处理严重类别不平衡的数据时，容易产生奖励偏差，倾向于高频标签，从而抑制了罕见病的经验提取。
*   **模态限制**：目前主要集中在 2D 影像，尚未扩展到 3D 影像（如 MRI 序列）或自动临床报告生成。

（完）
