---
title: Heterogeneous readmission prediction with hierarchical effect decomposition and regularization
title_zh: 基于层级效应分解与正则化的异质性再入院预测
authors: "Ziren Jiang, Lingfeng Huo, Jue Hou, Mary Vaughan-Sarrazin, Maureen A. Smith, Jared D. Huling"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19569v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 利用电子健康记录（EHR）进行再入院预测
tldr: 本研究针对电子健康记录中患者群体的高度异质性，提出了一种名为hierNest的层次建模框架，用于预测医院再入院风险。该方法利用主要诊断及其类别的层次结构，通过嵌套重新参数化和结构化正则化，实现了相关患者亚组间的信息共享，并保持了模型在不同层级的可解释性。在模拟研究和Medicare数据集上的应用证明，该方法在处理小样本亚组和异质效应方面具有显著的预测准确性优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.19569v1/fig-001.webp\", \"caption\": \"Figure 4: Worst AUROC across all DRG subgroups for different methods. The sample size n here represents the sample size for each DRG.\", \"page\": 21, \"index\": 1, \"width\": 1010, \"height\": 673}, {\"url\": \"assets/figures/arxiv/2603.19569v1/fig-002.webp\", \"caption\": \"Table 2: Number of observations and 30-day unplanned readmission or death rate by MDC index.\", \"page\": 30, \"index\": 2, \"width\": 879, \"height\": 1296}, {\"url\": \"assets/figures/arxiv/2603.19569v1/fig-003.webp\", \"caption\": \"Figure 1: Illustration of the hierarchical nested structure of the DRGs and MDCs and how our parameter decomposition aligns with this structure.\", \"page\": 7, \"index\": 3, \"width\": 1010, \"height\": 656}, {\"url\": \"assets/figures/arxiv/2603.19569v1/fig-004.webp\", \"caption\": \"Table 1: Main algorithm for solving the hierNest OGLasso problem.\", \"page\": 17, \"index\": 4, \"width\": 985, \"height\": 710}, {\"url\": \"assets/figures/arxiv/2603.19569v1/fig-005.webp\", \"caption\": \"Figure 5: Prediction performance within the different validation sets of external hospital observations (external validation) or the Year of 2020 observations (internal observations). We compare the MDCspecific AUPRC(top) and the AUROC(bot) for the compared methods. The black bar represents the median MDC-specific AUCs, and the red bar represents the mean MDC-specific AUCs. Each scatter point represents a single MDC-specific AUC for each MDC subgroup.\", \"page\": 31, \"index\": 5, \"width\": 1010, \"height\": 808}, {\"url\": \"assets/figures/arxiv/2603.19569v1/fig-006.webp\", \"caption\": \"Figure 3: Average AUROC across all DRG subgroups for different methods. The sample size n here represents the sample size for each DRG.\", \"page\": 20, \"index\": 6, \"width\": 1010, \"height\": 673}]"
motivation: 传统的再入院预测模型难以有效处理不同诊断类别下患者群体的高度异质性，且在小样本亚组上预测精度受限。
method: 提出hierNest框架，利用诊断信息的层次结构，结合嵌套重新参数化和结构化正则化技术实现跨亚组的信息共享。
result: 实验结果表明，该方法在模拟研究和真实Medicare数据集中均表现出优越的预测准确性，特别是在处理小样本亚组时优势明显。
conclusion: hierNest通过层次化效应分解有效提升了异质人群的再入院预测性能，为医疗资源分配提供了更精准且具可解释性的决策支持。
---

## 摘要
利用电子健康档案（EHR）准确预测医院再入院风险，对于有效的患者管理和医疗资源分配至关重要。医疗系统中的患者群体在不同主要诊断之间具有高度异质性，因此需要定制化且具有可解释性的预测模型。我们提出了一种结合了层级嵌套重参数化和结构化正则化方法的层级建模框架，称之为 hierNest。具体而言，我们的方法利用了主要诊断及其归类为主要诊断类别中固有的层级结构。我们的方法促进了相关患者子组之间的信息借用，并保留了不同层级水平上的可解释性。仿真研究表明，所提方法具有优越的预测准确性，特别是在子组样本量较小且层级效应程度各异的情况下。我们将该方法应用于一个包含联邦医疗保险（Medicare）患者的大型 EHR 数据集。

## Abstract
Accurately predicting hospital readmission risks using electronic health records (EHRs) is critical for effective patient management and healthcare resource allocation. Patient populations in health systems are highly heterogeneous across different primary diagnoses, necessitating tailored yet interpretable prediction models. We propose a hierarchical modeling framework incorporating hierarchical nested re-parameterization and structured regularization methods, which we call hierNest. Specifically, our approach leverages the inherent hierarchical structure present in primary diagnoses and groupings of these diagnoses into major diagnostic categories. Our methodology facilitates information borrowing across related patient subgroups and preserves interpretability at different hierarchical levels. Simulation studies demonstrate superior predictive accuracy of the proposed method, particularly with small subgroup sample sizes and varying degrees of hierarchical effects. We apply our methods to a large EHR dataset comprising Medicare patients.

---

## 论文详细总结（自动生成）

### 论文总结：基于层级效应分解与正则化的异质性再入院预测 (hierNest)

#### 1. 核心问题与研究动机
*   **核心问题**：如何利用高维且具有高度异质性的电子健康记录（EHR）数据，准确预测不同诊断类别患者的医院再入院风险。
*   **研究动机**：
    *   **患者异质性**：不同主要诊断（如心脏病与烧伤）的患者，其风险因素和临床路径截然不同。
    *   **数据稀疏性**：虽然总样本量大，但细分到具体的诊断相关组（DRG）时，样本量往往极小，导致独立建模效果差。
    *   **现有模型局限**：传统“一刀切”模型忽略了异质性；而黑盒机器学习模型（如深度学习）缺乏临床可解释性，且在小样本亚组上表现不佳。

#### 2. 方法论
*   **核心思想**：提出 **hierNest** 框架，利用医疗诊断中固有的层级结构（主要诊断类别 MDC -> 诊断相关组 DRG），将回归系数分解为不同层级的效应，并通过正则化实现跨组信息共享（Information Borrowing）。
*   **关键技术细节**：
    *   **效应分解公式**：对于特定 DRG $d$ 的系数 $\beta_d$，分解为：$\beta_d = \mu + \eta_{M(d)} + \delta_d$。其中 $\mu$ 是全局公共效应，$\eta_{M(d)}$ 是所属 MDC 的特定效应，$\delta_d$ 是该 DRG 的特有效应。
    *   **正则化策略**：
        1.  **hierNest-Lasso**：对分解后的各项进行独立 Lasso 惩罚，并根据样本量的四分之一次方进行权重调整，以平衡不同层级的收缩力度。
        2.  **hierNest-OGLasso (重叠组 Lasso)**：引入层级约束，确保只有在存在上级效应（如公共效应）时才允许存在下级效应（如 MDC 效应），从而避免过度拟合并增强可解释性。
    *   **算法流程**：
        *   通过构造特殊的稀疏设计矩阵将层级结构编码。
        *   开发了高效的**组间主化-最小化（MM）算法**，并结合**顺序强规则（Strong Rules）**来加速高维参数空间的优化过程。

#### 3. 实验设计
*   **数据集**：
    *   **模拟实验**：构建了包含 10 个 MDC、40 个 DRG 的合成数据，模拟了不同的样本量（30 到 200）、稀疏度和效应强度。
    *   **真实数据**：使用美国中西部某学术医疗系统的 Medicare 联动数据（2016-2020年），包含 66,885 条记录和 3,291 个预测变量。
*   **对比方法 (Benchmarks)**：
    *   标准 Lasso、组 Lasso (GLasso)、稀疏组 Lasso (SGLasso)。
    *   随机森林 (Random Forest)。
    *   按 MDC 独立训练的 Lasso 模型 (Lasso-MDCwise)。
*   **评估指标**：AUROC（曲线下面积）和 AUPRC（精准率-召回率曲线面积），重点关注所有亚组的平均表现及“最差亚组”的表现。

#### 4. 资源与算力
*   **算力说明**：文中未详细列出具体的 GPU/CPU 型号。
*   **训练时长**：作者提到，在处理超过 60,000 条观测数据和超过 100 万个参数（重参数化后）的模型时，使用 10 折交叉验证进行参数调优，整个过程在**几个小时内**即可完成。这得益于其针对稀疏矩阵优化的算法设计。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   模拟实验进行了 500 次重复采样，覆盖了多种异质性水平和样本量场景。
    *   真实数据验证采用了**外部验证**（独立医院数据）和**时间验证**（2020年数据）两种方式。
*   **充分性评价**：实验设计较为充分且客观。通过对比“平均表现”和“最差表现”，证明了该方法在提升整体精度的同时，有效保护了小样本亚组的预测质量，避免了模型在特定人群上的失效。

#### 6. 主要结论与发现
*   **性能优越**：hierNest-OGLasso 在几乎所有场景下均优于对比方法，尤其是在子组样本量较小时，其通过“信息借用”显著提升了预测准确度。
*   **结构优势**：层级正则化比简单的 Lasso 更有助于捕捉临床逻辑，减少了假阳性变量的选择。
*   **临床发现**：在真实数据中，模型识别出仅有少数变量（如 168 个）具有全局效应，而大多数效应是针对特定 MDC 或 DRG 的，这验证了医疗风险的异质性。

#### 7. 优点
*   **临床一致性**：模型结构直接对应医疗系统的 MDC-DRG 体系，结果易于被临床医生理解。
*   **数据效率**：通过参数共享，解决了小样本亚组无法有效建模的难题。
*   **计算高效**：提出的 MM 算法和强规则使得处理百万级参数的大规模 EHR 数据成为可能。

#### 8. 不足与局限
*   **依赖预定义结构**：模型高度依赖于 MDC-DRG 层级结构的准确性，如果该分类不能反映真实的临床风险分布，模型效果可能受限。
*   **变量缺失**：受限于 EHR 数据，模型无法纳入社会支持、交通便利性等对再入院有重大影响的社会决定因素（SDOH）。
*   **调参复杂度**：hierNest-OGLasso 涉及多个超参数（$\lambda, \alpha_1, \alpha_2$），在实际应用中需要较复杂的网格搜索或交叉验证。

（完）
