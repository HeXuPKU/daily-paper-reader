---
title: Instruction-Free Tuning of Large Vision Language Models for Medical Instruction Following
title_zh: 面向医疗指令遵循的大视觉语言模型无指令微调
authors: "Myeongkyun Kang, Soopil Kim, Xiaoxiao Li, Sang Hyun Park"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19482v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 医疗任务中大型视觉语言模型的免指令微调
tldr: 针对医疗领域缺乏高质量指令数据集的问题，本文提出一种无需指令的微调方法（Instruction-Free Tuning）。该方法仅利用图像-描述对，通过引入动量代理指令和响应打乱策略，在不依赖手工指令的情况下，使大视觉语言模型（LVLM）能够灵活响应领域特定指令。实验表明，该方法在多个医疗视觉问答任务上达到了SOTA水平，显著提升了微调效率。
selection_source: fresh_fetch
motivation: 医疗领域构建大规模高质量指令数据集需要专业专家知识，导致现有LVLM微调方法受限。
method: 提出使用动量代理指令替代手工指令，并结合响应打乱策略来减轻模型对前文的过度依赖。
result: 在SKINCON、WBCAtt、CBIS和MIMIC-CXR等多个医疗数据集的视觉问答任务中均取得了最先进的准确率。
conclusion: 该研究证明了仅利用图像-描述对即可实现高效的医疗LVLM微调，显著降低了对专家标注指令的依赖。
---

## 摘要
大视觉语言模型（LVLMs）在广泛的任务中展现出了令人印象深刻的性能。这些能力主要源于视觉指令微调，即在由精心策划的“图像-指令-输出”三元组组成的数据集上对模型进行微调。然而，在医疗领域，由于需要专门的专家知识，构建大规模、高质量的指令数据集尤其具有挑战性。为了解决这一问题，我们提出了一种无指令微调方法，该方法减少了对手工指令的依赖，仅利用“图像-描述”对进行微调。具体而言，我们引入了动量代理指令（momentum proxy instruction）来替代精心策划的文本指令，这在保留预训练 LVLM 指令遵循能力的同时，促进了在推理过程中保持有效的参数更新。因此，即使在微调过程中缺乏显式指令，微调后的 LVLM 也能灵活地响应特定领域的指令。此外，我们结合了响应打乱策略，以减轻模型对先前词汇的过度依赖，从而促进更有效的微调。我们的方法在 SKINCON、WBCAtt、CBIS 和 MIMIC-CXR 数据集的选择题视觉问答任务上达到了最先进的准确率，显著提升了 LVLMs 在医疗领域的微调效率。

## Abstract
Large vision language models (LVLMs) have demonstrated impressive performance across a wide range of tasks. These capabilities largely stem from visual instruction tuning, which fine-tunes models on datasets consisting of curated image-instruction-output triplets. However, in the medical domain, constructing large-scale, high-quality instruction datasets is particularly challenging due to the need for specialized expert knowledge. To address this issue, we propose an instruction-free tuning approach that reduces reliance on handcrafted instructions, leveraging only image-description pairs for fine-tuning. Specifically, we introduce a momentum proxy instruction as a replacement for curated text instructions, which preserves the instruction-following capability of the pre-trained LVLM while promoting updates to parameters that remain valid during inference. Consequently, the fine-tuned LVLM can flexibly respond to domain-specific instructions, even though explicit instructions are absent during fine-tuning. Additionally, we incorporate a response shuffling strategy to mitigate the model's over-reliance on previous words, facilitating more effective fine-tuning. Our approach achieves state-of-the-art accuracy on multiple-choice visual question answering tasks across SKINCON, WBCAtt, CBIS, and MIMIC-CXR datasets, significantly enhancing the fine-tuning efficiency of LVLMs in medical domains.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对医疗领域大视觉语言模型（LVLM）的“无指令微调”（Instruction-Free Tuning）框架，旨在解决医疗指令数据获取成本高、专家标注难的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的视觉指令微调依赖于大量的“图像-指令-输出”三元组。在医疗领域，构建高质量指令集需要极高的专家知识成本。
*   **研究背景**：现有的医疗LVLM通常利用LLM将医疗报告转化为对话格式，但这容易引入领域术语偏差、幻觉，且医疗报告中严重的句子间依赖（如从描述直接推导结论）会导致模型过度依赖文本上下文而忽略视觉特征。
*   **整体含义**：探索如何仅利用现有的“图像-描述（报告）”对，在不损失模型指令遵循能力的前提下，实现高效的领域适配。

### 2. 方法论
论文提出了 **InstFree** 框架，包含两个核心技术：
*   **动量代理指令（Momentum Proxy Instruction, MPI）**：
    *   在微调阶段，使用一组可学习的连续向量（代理指令）替代文本指令。
    *   **核心思想**：通过梯度下降优化这些向量以对齐图像和描述，同时更新视觉编码器。
    *   **动量更新**：为了防止模型对特定代理指令过拟合，引入指数移动平均（EMA）来更新代理指令（即动量代理指令），使其捕捉微调的整体趋势。
    *   **推理阶段**：丢弃代理指令，直接使用自然语言指令（如“描述这张图”），模型仍能保持预训练时的指令遵循能力。
*   **响应打乱策略（Response Shuffling, RS）**：
    *   针对医疗报告中结论往往能从前文预测的特点，随机打乱报告中句子的顺序。
    *   **目的**：强制模型减少对上文Token的依赖，转而更多地关注视觉特征，从而缓解“视觉忽略”问题。

### 3. 实验设计
*   **数据集**：
    *   **SKINCON**（皮肤病学）：48种临床属性标注。
    *   **WBCAtt**（白细胞）：11种形态属性标注。
    *   **CBIS**（乳腺钼靶）：病理和异常类型标注。
    *   **MIMIC-CXR**（胸部X光）：大规模放射科报告。
*   **Benchmark**：将上述数据集的测试集转化为选择题视觉问答（Multiple-choice VQA）任务。
*   **对比方法**：
    *   **零样本/预训练模型**：LLaMA-3.2-11B-Vision, Qwen2.5-VL-3B, MedGemma-4B, PubMedVision-7B。
    *   **微调基准**：使用固定文本指令（如“Describe this medical scan”）进行标准微调的上述模型。
    *   **其他**：BLIP-2。

### 4. 资源与算力
*   **模型基础**：基于 LLaMA-3.2-11B-Vision-Instruct。
*   **框架**：使用 PyTorch 的 `torchtune` 微调库。
*   **超参数**：输入分辨率 560x560，Batch Size 为 2，使用 AdamW 优化器。
*   **训练时长**：预热阶段 1 个 Epoch（约 3000 步），正式微调 5 个 Epoch。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量，但从模型规模（11B）和 Batch Size 来看，通常需要 8x A100 或更高规格的显卡集群。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同模态的医疗数据集上进行了验证，涵盖了皮肤、血液、乳腺和胸部影像，具有较好的领域覆盖度。
*   **消融实验**：针对动量系数 $\alpha$、代理指令向量数量 $N$、响应打乱策略的有效性、以及代理指令的不同更新方式（固定 vs 实时更新）做了详细对比。
*   **客观性**：每组实验运行 3 次并取平均值，对比了多个 SOTA 开源医疗模型，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **SOTA 性能**：InstFree 在所有四个数据集上均显著优于直接微调和现有的医疗专用模型（如 MedGemma）。
*   **解决指令依赖**：发现直接微调会导致模型产生“指令依赖”（仅对微调时的特定指令有效）或“图像依赖”（忽略指令，输出固定格式），而代理指令能有效保留模型的通用指令遵循能力。
*   **视觉特征增强**：响应打乱策略显著提升了模型在关键属性识别上的准确率，证明了减轻文本过拟合的必要性。

### 7. 优点
*   **高效性**：无需人工编写或 LLM 生成复杂的指令数据集，直接利用原始报告即可微调。
*   **通用性**：微调后的模型不仅能写报告，还能灵活处理未见过的 VQA 任务。
*   **创新性**：首次形式化定义了 LVLM 的“无指令微调”任务，并巧妙利用动量更新解决了代理指令的过拟合问题。

### 8. 不足与局限
*   **分隔符依赖**：响应打乱策略依赖于正确识别报告中的分隔符（如逗号、句号），对于结构极其混乱的原始文本可能效果受限。
*   **超参数敏感性**：动量系数和向量数量对结果有一定影响，不同数据集可能需要微调这些超参数。
*   **推理开销**：虽然微调效率高，但模型本身仍是 11B 规模，在边缘医疗设备上的部署成本依然较高。

（完）
