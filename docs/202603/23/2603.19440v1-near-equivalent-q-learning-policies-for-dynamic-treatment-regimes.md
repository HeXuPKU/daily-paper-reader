---
title: Near-Equivalent Q-learning Policies for Dynamic Treatment Regimes
title_zh: 动态治疗方案的近等效Q学习策略
authors: "Sophia Yazzourh, Erica E. M. Moodie"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19440v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 用于精准医疗动态治疗方案的Q学习
tldr: 针对精准医疗中动态治疗方案（DTR）通常只提供单一最优策略的局限性，本文扩展了Q-learning框架，引入容差参数ε来构建近等效策略集。该方法通过矩阵值表示法处理反向递归，能够识别治疗无差异区域，为临床医生提供多种性能相近的可选方案。在单阶段和多阶段肿瘤模拟实验中，该框架成功展示了在保证疗效的前提下提供决策灵活性的能力。
selection_source: fresh_fetch
motivation: 传统的动态治疗方案研究往往只关注单一最优策略，忽略了临床实践中可能存在多个效果相近且具有临床意义的替代方案。
method: 提出一种扩展的Q-learning框架，利用ε容差标准和矩阵值表示法，在反向递归过程中保留并识别所有性能接近最优的策略集。
result: 实验证明该方法能有效识别治疗无差异区域，并在模拟的肿瘤动力学模型中生成一系列在预设性能偏差内的近等效治疗策略。
conclusion: 该研究通过提供多样化的近等效策略，增强了临床决策的灵活性，使医生能够根据患者具体情况在多个有效方案中进行权衡。
---

## 摘要
精准医学旨在根据个体患者特征定制治疗决策。这一目标通常通过动态治疗方案（DTRs）来实现，该方案利用统计和机器学习方法，根据不断变化的临床信息推导序列决策规则。在大多数现有公式中，这些方法在每个阶段产生单一的最佳治疗方案，从而导致唯一的决策序列。然而，在许多临床场景中，多种治疗选择可能会产生相似的预期结果，而仅关注单一的最优策略可能会掩盖有意义的替代方案。我们通过引入由超参数 $\varepsilon$ 控制的最差值容差标准，扩展了针对回顾性数据的 Q 学习框架，该标准规定了与最优预期值的最大可接受偏差。该方法不是识别单一的最优策略，而是构建 $\varepsilon$-最优策略集，其性能保持在最优值的受控邻域内。这种公式将 Q 学习从向量值表示转变为矩阵值表示，允许在反向递归过程中共存多个可接受的价值函数。该方法产生了一系列近等效的治疗策略，并明确识别了治疗无差异区域，在这些区域中，多种决策可实现相当的结果。我们在两种设置中演示了该框架：一个是突出决策边界周围无差异区域的单阶段问题，另一个是基于描述肿瘤大小和治疗毒性动态的模拟肿瘤学模型的多阶段决策过程。

## Abstract
Precision medicine aims to tailor therapeutic decisions to individual patient characteristics. This objective is commonly formalized through dynamic treatment regimes, which use statistical and machine learning methods to derive sequential decision rules adapted to evolving clinical information. In most existing formulations, these approaches produce a single optimal treatment at each stage, leading to a unique decision sequence. However, in many clinical settings, several treatment options may yield similar expected outcomes, and focusing on a single optimal policy may conceal meaningful alternatives. We extend the Q-learning framework for retrospective data by introducing a worst-value tolerance criterion controlled by a hyperparameter $\varepsilon$, which specifies the maximum acceptable deviation from the optimal expected value. Rather than identifying a single optimal policy, the proposed approach constructs sets of $\varepsilon$-optimal policies whose performance remains within a controlled neighborhood of the optimum. This formulation shifts Q-learning from a vector-valued representation to a matrix-valued one, allowing multiple admissible value functions to coexist during backward recursion. The approach yields families of near-equivalent treatment strategies and explicitly identifies regions of treatment indifference where several decisions achieve comparable outcomes. We illustrate the framework in two settings: a single-stage problem highlighting indifference regions around the decision boundary, and a multi-stage decision process based on a simulated oncology model describing tumor size and treatment toxicity dynamics.

---

## 论文详细总结（自动生成）

### 论文总结：动态治疗方案的近等效 Q 学习策略

#### 1. 论文的核心问题与整体含义
在精准医疗中，**动态治疗方案（DTR）**的目标是根据患者随时间变化的特征提供序列化的决策建议。传统的 Q 学习（Q-learning）方法通常只输出一个“唯一最优”的治疗策略。然而，在临床实践中，往往存在多种治疗方案其预期效果极其接近（近等效）。
*   **研究动机**：强制推行单一最优策略会限制临床医生的自主权，并掩盖具有临床意义的替代方案。
*   **核心问题**：如何识别并量化那些与最优方案表现相当的“近等效”策略集，从而为临床决策提供更大的灵活性和容错空间。

#### 2. 论文提出的方法论
论文提出了一种**近等效 Q 学习（Near-Equivalent Q-learning）**框架，将传统的确定性决策扩展为集合值决策。
*   **核心思想**：引入一个容差超参数 $\epsilon \in [0, 1)$，定义一个 $\epsilon$-接纳标准。如果某个动作的预期 Q 值与最大 Q 值的差距在比例 $\epsilon$ 之内，则认为该动作是“近等效”的。
*   **关键技术细节**：
    *   **$\epsilon$-选择标准**：判定动作 $a$ 是否合格的公式为：$\hat{Q}_t(h_t, a) \geq \max_a \hat{Q}_t(h_t, a) - \epsilon |\max_a \hat{Q}_t(h_t, a)|$。
    *   **矩阵值表示法**：在反向递归过程中，由于每个阶段可能保留多个合格动作，传统的向量化伪结果（pseudo-outcome）演变为矩阵形式。
    *   **填充（Padding）机制**：为了保持递归过程中回归模型维度的统一，对于合格动作数少于最大值 $m$ 的个体，使用最优 Q 值进行填充，确保计算稳定性。
    *   **递归逻辑**：在倒数第二阶段引入 $\epsilon$-选择，随后在更早阶段并行传播这些近等效的价值函数。

#### 3. 实验设计
论文通过两个主要场景验证了该方法：
*   **场景一：单阶段个体化治疗规则（ITR）**
    *   **设置**：模拟具有 10 个协变量的二元治疗问题。
    *   **目标**：观察 $\epsilon$ 如何诱导决策边界周围的“治疗无差异区域”。
    *   **Benchmark**：标准的单阶段 Q 学习（即 $\epsilon=0$）。
*   **场景二：多阶段肿瘤模拟研究**
    *   **设置**：一个包含 6 个决策阶段的复杂模型，模拟肿瘤大小与治疗毒性之间的动态权衡。治疗剂量有 11 个离散选项（0 到 1，步长 0.1）。
    *   **对比方法**：标准反向 Q 学习（最优策略）以及各种固定剂量方案（Constant Regimes）。
    *   **模型工具**：使用带有径向基函数（RBF）内核的支持向量回归（SVR）作为基础学习器。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Apple M1 芯片**和 **16 GB RAM** 的笔记本电脑上完成。
*   **训练时长**：
    *   标准 Q 学习训练耗时约 **0.29 秒**。
    *   近等效 Q 学习（在特定 $\epsilon$ 下）训练耗时约 **2.1 秒**。
*   **软件实现**：使用 Python 编写，代码已在 GitHub 开源。

#### 5. 实验数量与充分性
*   **实验规模**：多阶段模拟中使用了 500 名患者作为训练集，5000 名患者作为测试集。
*   **实验维度**：针对 $\epsilon$ 取值（0.1, 0.3, 0.5, 0.9）进行了多组对比实验。
*   **充分性评价**：实验设计较为客观。通过单阶段实验清晰地展示了数学原理（决策边界的拓宽），通过多阶段实验验证了在复杂动态系统中的稳健性。虽然样本量不算巨大，但对于验证统计方法论而言已经足够。

#### 6. 论文的主要结论与发现
*   **识别无差异区**：该方法能有效识别出那些治疗效果差异不显著的患者区域。在这些区域，$\epsilon$ 越大，可选的近等效方案越多。
*   **性能保持**：即使在放宽了最优性要求后，生成的近等效策略集在测试集上的表现依然显著优于任何固定剂量方案，且其最优边界与标准 Q 学习高度重合。
*   **价值函数平坦性**：实验发现价值函数在最优值附近存在“平台期”，这意味着临床上确实存在多个性能几乎一致的决策序列。

#### 7. 优点
*   **临床实用性强**：打破了“算法给唯一指令”的僵化模式，允许医生根据药物可用性、成本或患者偏好在多个有效方案中选择。
*   **数学框架严谨**：将 $\epsilon$-贪婪的思想从在线学习引入到离线回归框架中，并解决了非正 Q 值的处理问题。
*   **可视化直观**：通过“白带区域”展示决策的不确定性，有助于增强医疗 AI 的可解释性和信任度。

#### 8. 不足与局限
*   **模拟环境简化**：肿瘤模型基于确定性的微分方程，缺乏真实世界临床数据中的高度随机性和噪声。
*   **超参数选择**：$\epsilon$ 的选择目前仍具有一定的主观性，缺乏一个自动化的、基于临床显著性差异的校准标准。
*   **状态空间维度**：实验中的协变量较少（仅肿瘤大小和毒性），在处理具有数百个特征的高维临床数据时，矩阵运算的计算开销可能会显著增加。
*   **非正则性风险**：在决策边界附近，统计推断可能面临非标准渐近分布的问题，论文未深入探讨其置信区间的构建。

（完）
