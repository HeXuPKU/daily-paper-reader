---
title: "Cross-Granularity Representations for Biological Sequences: Insights from ESM and BiGCARP"
title_zh: 生物序列的跨粒度表示：来自 ESM 和 BiGCARP 的启示
authors: "Hanlin Xiao, Rainer Breitling, Eriko Takano, Mauricio A. Álvarez"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20825v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 大型生物序列模型与基因组簇
tldr: 本研究探讨了生物序列模型中跨粒度知识的整合，以BiGCARP（结构域级）和ESM（氨基酸级）模型为案例。研究发现，简单的嵌入初始化效果不佳，而深层嵌入能捕捉更丰富的上下文信息。通过整合不同粒度的表示，可以互补生物学知识，显著提升中间层级预测任务的性能，为提高生物基础模型的性能和可解释性提供了新策略。
selection_source: fresh_fetch
motivation: 旨在解决生物序列多层级粒度（如氨基酸与结构域）在模型表示中的整合与互补性问题。
method: 通过表征分析和探测任务，对比分析了BiGCARP和ESM模型在不同层级的嵌入特征及其结合方式。
result: 发现深层嵌入比浅层更具代表性，且结合跨粒度表示能有效提升中间层级生物任务的预测准确率。
conclusion: 跨粒度表示整合是提升生物基础模型性能和可解释性的有效途径。
---

## 摘要
通用基础模型的最新进展推动了大型生物序列模型的发展。虽然自然语言表现出符号粒度（字符、词、句子），但生物序列呈现出层次化粒度，其各个层级（核苷酸、氨基酸、蛋白质结构域、基因）进一步编码了生物功能信息。在本文中，我们通过对 BiGCARP（一种针对生物合成基因簇的 Pfam 结构域级模型）和 ESM（一种氨基酸级蛋白质语言模型）的案例研究，探讨了模型间跨粒度知识的整合。利用表示分析工具和一系列探测任务，我们首先解释了为什么简单的跨模型嵌入初始化未能提升 BiGCARP 的下游性能，并表明深层嵌入捕捉到了模型所学知识中更具上下文相关性且更真实的表示。此外，我们证明了不同粒度的表示编码了互补的生物学知识，并且将它们结合起来可以在中间层级的预测任务中产生显著的性能提升。我们的研究结果强调，跨粒度整合是提高生物基础模型性能和可解释性的一种极具前景的策略。

## Abstract
Recent advances in general-purpose foundation models have stimulated the development of large biological sequence models. While natural language shows symbolic granularity (characters, words, sentences), biological sequences exhibit hierarchical granularity whose levels (nucleotides, amino acids, protein domains, genes) further encode biologically functional information. In this paper, we investigate the integration of cross-granularity knowledge from models through a case study of BiGCARP, a Pfam domain-level model for biosynthetic gene clusters, and ESM, an amino acid-level protein language model. Using representation analysis tools and a set of probe tasks, we first explain why a straightforward cross-model embedding initialization fails to improve downstream performance in BiGCARP, and show that deeper-layer embeddings capture a more contextual and faithful representation of the model's learned knowledge. Furthermore, we demonstrate that representations at different granularities encode complementary biological knowledge, and that combining them yields measurable performance gains in intermediate-level prediction tasks. Our findings highlight cross-granularity integration as a promising strategy for improving both the performance and interpretability of biological foundation models.

---

## 论文详细总结（自动生成）

以下是对论文《Cross-Granularity Representations for Biological Sequences: Insights from ESM and BiGCARP》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：生物序列具有天然的分层粒度（核苷酸 $\rightarrow$ 氨基酸 $\rightarrow$ 蛋白质结构域 $\rightarrow$ 基因/基因簇），不同于自然语言处理（NLP）中可逆的分词，生物学层级的转换是“有损”但具有“功能先验”的。目前的基础模型通常只专注于单一粒度，如何有效整合跨粒度知识以提升模型性能和可解释性是本文的核心。
*   **研究背景**：以氨基酸级模型 **ESM** 和结构域级模型 **BiGCARP** 为例。尽管 BiGCARP 尝试用 ESM 的表示来初始化其嵌入矩阵，但这种“跨粒度初始化”并未显著提升下游任务性能。本研究旨在探究其背后的原因，并寻找更有效的整合策略。

### 2. 方法论
*   **核心思想**：通过表示分析工具（UMAP、CKA）揭示模型内部特征演化，并对比不同层级嵌入（Embedder 层 vs. 最后一层）在不同尺度探测任务中的表现。
*   **关键技术细节**：
    *   **跨粒度初始化分析**：研究 BiGCARP 如何利用 ESM 处理后的 Pfam 结构域序列进行初始化。
    *   **表示提取策略**：对比“静态嵌入”（Embedder 层，每个结构域固定）与“上下文嵌入”（最后一层，受周围结构域影响）。
    *   **表示分析工具**：
        *   **UMAP**：用于降维可视化，观察结构域是否按功能或进化关系聚类。
        *   **CKA（中心核对齐）**：定量测量不同层级、不同初始化方案之间表示空间的相似性。
    *   **特征融合**：采用简单的特征拼接（Concatenation）方式整合 ESM（局部细粒度）和 BiGCARP（全局粗粒度）的表示。

### 3. 实验设计
*   **数据集**：
    *   **antiSMASH-DB**：用于 BiGCARP 的预训练（约 12.7 万个基因簇）。
    *   **MIBiG (v3.0)**：提供经过实验验证的基因簇（BGC）及其产物信息，用于下游探测任务。
    *   **Pfam**：提供蛋白质结构域的定义和 HMM 模型。
*   **探测任务（Benchmark）**：
    1.  **BGC 产物类别预测（全局任务）**：预测基因簇合成的化学物质类别（如聚酮、非核糖体肽等）。
    2.  **A-结构域底物预测（局部任务）**：预测非核糖体肽合成酶中 A-结构域的底物特异性。
    3.  **卤化产物预测（中间任务）**：预测含有特定卤化酶结构域的 BGC 是否产生卤化产物。
*   **对比方法**：随机初始化、ESM 初始化、单模型表示、跨模型拼接表示。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：提到了使用 ESM-2、ESM-1b（1280 维嵌入）和 BiGCARP（256 维嵌入）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 BGC 产物预测，采用了分层 5 折交叉验证，并重复 10 个随机种子以确保统计显著性。
    *   针对卤化产物预测（样本量仅 46 个），采用了留一法交叉验证（LOOCV）和 10,000 次 Bootstrap 重采样。
*   **充分性评价**：实验设计较为严谨，通过 CKA 分析解释了初始化失效的动力学原因，涵盖了从局部到全局的不同尺度任务，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **初始化失效原因**：CKA 分析显示，BiGCARP 的早期层（空洞卷积）会迅速重塑表示空间，导致预训练的 ESM 初始化信息在深层被“覆盖”或“重写”。
*   **深层表示更优**：模型的最后一层嵌入比初始 Embedder 层包含更丰富的上下文和功能信息，在下游任务中表现更好。
*   **粒度互补性**：ESM 在局部结构任务（如 A-结构域底物预测）中占优；BiGCARP 在全局背景任务中占优。
*   **整合收益**：在中间层级任务（如卤化预测）中，简单拼接两种粒度的表示可获得显著的性能提升（AUROC 提升），证明了跨粒度整合的价值。

### 7. 优点
*   **深入的机理分析**：不仅展示了结果，还通过 CKA 曲线解释了为什么简单的权重迁移在生物序列模型中可能失效。
*   **功能尺度发现**：通过 UMAP 聚类发现，模型学习到的结构域表示不仅按功能分类，还按“功能尺度”（全局调节 vs. 局部修饰）聚类，具有很强的生物学解释性。
*   **任务设计合理**：通过局部、中间、全局三个维度的任务全面评估了不同粒度模型的优劣。

### 8. 不足与局限
*   **融合策略简单**：目前仅采用了简单的特征拼接和 MLP 预测，未探索更先进的注意力机制融合或联合预训练。
*   **样本量限制**：部分探测任务（如卤化预测）样本量极小（N=46），虽然使用了统计学手段补偿，但结论的普适性仍需在大规模数据集上验证。
*   **应用范围**：研究目前仅局限于生物合成基因簇（BGC），尚未扩展到非编码区、全基因组等更广泛的生物学领域。

（完）
