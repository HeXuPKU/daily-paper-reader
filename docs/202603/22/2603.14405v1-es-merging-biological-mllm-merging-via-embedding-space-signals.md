---
title: "ES-Merging: Biological MLLM Merging via Embedding Space Signals"
title_zh: ES-Merging：基于嵌入空间信号的生物多模态大语言模型合并
authors: "Wonbin Lee, Dongki Kim, Sung Ju Hwang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14405v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 生物多模态大语言模型合并
tldr: 针对生物多模态大语言模型（MLLM）在处理跨模态科学问题时受限于单一模态的问题，本文提出 ES-Merging 框架。该方法摒弃了传统的参数空间启发式合并，转而利用嵌入空间信号，通过探测输入获取层级和元素级的响应来动态估计合并系数。实验证明，该方法在交互效应预测任务中表现优异，超越了现有合并技术及特定任务的微调模型，为跨模态模型融合提供了新路径。
selection_source: fresh_fetch
motivation: 现有的模型合并方法多依赖于与输入无关的参数空间启发式规则，难以准确捕捉生物模型中的模态特异性。
method: 通过设计包含不同模态 Token 的探测输入，从嵌入空间中提取层级和元素级的细粒度信号来联合估计合并系数。
result: 在交互效应预测基准上，ES-Merging 的性能不仅优于传统合并方法，还超越了经过专门微调的模型。
conclusion: 研究证明了嵌入空间信号是实现跨模态 MLLM 高效、稳健合并的有效基础。
---

## 摘要
生物多模态大语言模型（MLLMs）已成为科学发现的强大基础模型。然而，现有模型通常专注于单一模态，限制了它们解决固有跨模态科学问题的能力。虽然模型合并是将不同模态组合成统一 MLLM 的有效方法，但现有方法依赖于与输入无关的参数空间启发式算法，无法准确捕捉模态专业化。为了克服这一局限性，我们提出了一种表示感知的合并框架，该框架通过嵌入空间信号来估计合并系数。我们首先设计了一个由不同模态 token 组成的探测输入，并将其通过每个专业化的 MLLM，以获得反映模态特定表示变化的逐层嵌入响应。然后，我们从嵌入空间中估计两个粒度的互补合并系数：来自粗粒度信号的逐层系数和来自细粒度信号的逐元素系数，并将两者结合以进行鲁棒的系数估计。在相互作用效应预测基准上的实验表明，我们的方法优于现有的合并方法，甚至超过了特定任务的微调模型，证明了嵌入空间信号为跨模态 MLLM 合并提供了原则性且有效的基础。

## Abstract
Biological multimodal large language models (MLLMs) have emerged as powerful foundation models for scientific discovery. However, existing models are specialized to a single modality, limiting their ability to solve inherently cross-modal scientific problems. While model merging is an efficient method to combine the different modalities into a unified MLLM, existing methods rely on input-agnostic parameter space heuristics that fail to faithfully capture modality specialization. To overcome this limitation, we propose a representation-aware merging framework that estimates merging coefficients from embedding space signals. We first design a probe input that consists of different modality tokens and forward it through each specialized MLLM to obtain layer-wise embedding responses that reflect modality-specific representation changes. We then estimate complementary merging coefficients at two granularities from the embedding space: layer-wise coefficients from coarse-grained signals and element-wise coefficients from fine-grained signals, which are jointly combined for robust coefficient estimation. Experiments on interactive effect prediction benchmarks show that our method outperforms existing merging methods and even surpasses task-specific fine-tuned models, establishing that embedding space signals provide a principled and effective foundation for cross-modal MLLM merging.

---

## 论文详细总结（自动生成）

### 论文总结：ES-Merging —— 基于嵌入空间信号的生物多模态大语言模型合并

#### 1. 核心问题与整体含义（研究动机和背景）
在生物科学领域，现有的多模态大语言模型（MLLM）通常只针对单一模态（如分子、蛋白质或细胞）进行优化。然而，许多关键的科学问题（如药物-靶点相互作用、药物对特定细胞的效果）本质上是**跨模态**的。
*   **研究动机**：现有的模型合并方法（如 TIES-Merging）主要依赖于参数空间的启发式规则（如参数的大小、符号或方向），这些方法是“输入无关”的，无法准确捕捉模型在处理特定模态数据时的内部表示变化。
*   **核心问题**：如何有效地将多个单模态专家模型合并为一个统一的、具备跨模态推理能力的生物 MLLM，同时保留各专家的深层知识？

#### 2. 方法论：核心思想与关键技术
论文提出了 **ES-Merging** 框架，其核心思想是**利用嵌入空间（Embedding Space）的信号来动态估计合并系数**。

*   **探测输入（Probe Input）**：设计一个包含分子、蛋白质和细胞 Token 的组合 Prompt，将其输入到基础 LLM 和各个专业化 MLLM 中。
*   **层级全局系数（Layer-wise Global Coefficient, $\alpha$）**：
    *   计算专业化模型与基础模型在每一层输出的嵌入分布差异。
    *   使用**切片瓦瑟斯坦距离（Sliced Wasserstein Distance, SWD）**来量化这种分布偏移。偏移越大，说明该层对该模态的专业化程度越高。
*   **元素级局部系数（Element-wise Local Coefficient, $\beta$）**：
    *   通过计算嵌入空间距离对参数的梯度，识别出对表示偏移贡献最大的特定参数元素。
    *   这是一种细粒度的灵敏度分析，确保重要的权重获得更高的合并权重。
*   **系数集成**：将层级系数与元素级系数相乘并归一化，得到最终的合并权重 $\lambda$，用于对 LoRA 参数进行加权求和。

#### 3. 实验设计
*   **实验场景**：
    1.  **实例变化交互预测**：分子-蛋白质相互作用（BindingDB, BioSNAP, Human）、分子-细胞相互作用（DrugComb, GDSC2）。
    2.  **目标固定功能预测**：CYP 酶抑制预测（5 个子集）、CYP 酶底物预测（3 个子集）。
*   **基准模型（Benchmark）**：以 LLaMA-3.1-8B-Instruct 为基础，合并了三个专家模型：Mol-LLaMA（分子）、Prot2Text-V2（蛋白）、Cell-o1（细胞）。
*   **对比方法**：
    *   基础模型及单模态专家模型。
    *   传统合并方法：Average Merging, TIES-Merging, EMR-Merging, AdaMerging, PCB-Merging, Consensus Merging, LS-Merge。
    *   特定任务微调（Avg. Merging + FT）。

#### 4. 资源与算力
*   **模型规模**：使用了 8B 参数规模的 LLaMA-3.1。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率分析**：论文对比了计算成本（FLOPs），指出 ES-Merging 的计算开销比 AdaMerging 低 3.4 倍，比特定任务微调低 6.1 倍，因为它仅需一次梯度计算，而无需迭代更新。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 大类任务、10 多个子数据集，进行了大量的定量对比。
*   **消融实验**：针对层级系数、元素级系数及其组合进行了消融研究，并探讨了 SWD 投影维度的影响。
*   **定性分析**：提供了案例研究（Case Study），展示了合并模型在推理过程中的生物学解释能力，并进行了嵌入空间的可视化分析。
*   **评价**：实验设计全面，对比了当前主流的所有合并技术，验证了方法在不同生物任务上的稳健性，实验结果具有较高的说服力。

#### 6. 主要结论与发现
*   **性能卓越**：ES-Merging 在所有跨模态交互任务中均优于现有的合并方法，甚至在某些任务上超过了经过特定任务微调的模型。
*   **表示感知的优势**：实验证明，嵌入空间信号比参数空间信号更能反映模态专业化，能够更精准地定位“专家知识”所在的层和参数。
*   **知识保留**：合并后的模型不仅能做分类预测，还能保留专家模型的推理链条（Reasoning Chain），提供生物学解释。

#### 7. 优点与亮点
*   **创新视角**：将模型合并从“盲目”的参数操作转向“感知”的表示空间操作。
*   **多粒度融合**：结合了粗粒度的层级重要性和细粒度的参数灵敏度，使合并系数更加校准。
*   **高效性**：无需针对下游任务进行昂贵的微调，仅需少量探测样本即可完成高质量合并。

#### 8. 不足与局限
*   **领域局限性**：实验仅集中在生物化学领域，尚未在视觉、音频等通用多模态领域验证其普适性。
*   **单模态性能风险**：论文主要关注跨模态融合性能，未深入探讨合并后模型在原始单模态任务上是否存在性能退化（虽然可视化显示分布接近专家模型）。
*   **探测输入依赖**：合并效果可能受到探测输入（Probe Input）样本选择的影响，论文未详细讨论样本偏差带来的风险。

（完）
