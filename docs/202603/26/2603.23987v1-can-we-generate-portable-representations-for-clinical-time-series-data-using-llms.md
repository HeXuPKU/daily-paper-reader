---
title: Can we generate portable representations for clinical time series data using LLMs?
title_zh: 我们能否利用大语言模型（LLMs）为临床时间序列数据生成可移植的表示？
authors: "Zongliang Ji, Yifei Sun, Andre Amaral, Anna Goldenberg, Rahul G. Krishnan"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23987v1"
tags: ["query:med-ai"]
score: 10.0
evidence: 利用大语言模型从临床时间序列中创建可移植的患者嵌入
tldr: 本研究探讨了利用大语言模型（LLM）生成临床时间序列数据的可移植表示，以解决临床机器学习模型在跨医院部署时性能下降的问题。研究通过冻结的LLM将不规则的ICU时间序列转化为自然语言摘要，再利用文本嵌入模型生成固定长度向量。实验表明，该方法在多个数据集和任务上表现优异，不仅在分布内性能与主流模型相当，且在跨医院迁移时表现出更强的鲁棒性和更优的少样本学习能力，同时未增加隐私风险。
selection_source: fresh_fetch
motivation: 针对临床机器学习模型在不同医院间迁移时因分布偏移导致性能下降且部署成本高的问题，寻求一种更具可移植性的患者特征表示方法。
method: 利用冻结的大语言模型将复杂的ICU时间序列数据转化为自然语言摘要，随后通过文本嵌入模型将其转换为可供下游预测器使用的固定长度向量。
result: 该方法在MIMIC-IV等多个数据集上的预测任务中表现出色，跨医院迁移时的性能下降幅度小于传统基准模型，并显著提升了少样本学习效果。
conclusion: LLM生成的自然语言摘要表示能有效降低临床预测模型的工程开销，为实现可扩展且稳健的医疗AI部署提供了新路径。
---

## 摘要
部署临床机器学习（ML）既缓慢又脆弱：在一家医院运行良好的模型在另一家医院往往会因分布偏移而性能下降。在这项工作中，我们研究了一个简单的问题——大语言模型（LLMs）能否创建可移植的患者嵌入（即患者表示），使得在一家医院构建的下游预测器能够在其他地方使用，且只需极少甚至无需重新训练和微调。为此，我们使用冻结的 LLM 将不规则的 ICU 时间序列映射为简洁的自然语言摘要，然后使用冻结的文本嵌入模型对每个摘要进行嵌入，从而获得一个固定长度的向量，该向量可作为各种下游预测器的输入。在三个队列（MIMIC-IV、HIRID、PPICU）的多个临床预测和分类任务中，我们发现我们的方法简单易用，且在分布内性能上与网格插补（grid imputation）、自监督表示学习和时间序列基础模型相当，同时在迁移到新医院时表现出更小的相对性能下降。我们研究了不同提示词设计下的性能变化，发现结构化提示词对于在不改变平均准确率的情况下降低预测模型的方差至关重要。我们发现，使用这些可移植表示可以提高少样本学习能力，且相对于基准模型，并未增加年龄或性别等人口统计学信息的可恢复性，这表明几乎没有额外的隐私风险。我们的工作指出了 LLM 作为工具在通过减少工程开销来实现生产级预测模型的可扩展部署方面的潜力。

## Abstract
Deploying clinical ML is slow and brittle: models that work at one hospital often degrade under distribution shifts at the next. In this work, we study a simple question -- can large language models (LLMs) create portable patient embeddings i.e. representations of patients enable a downstream predictor built on one hospital to be used elsewhere with minimal-to-no retraining and fine-tuning. To do so, we map from irregular ICU time series onto concise natural language summaries using a frozen LLM, then embed each summary with a frozen text embedding model to obtain a fixed length vector capable of serving as input to a variety of downstream predictors. Across three cohorts (MIMIC-IV, HIRID, PPICU), on multiple clinically grounded forecasting and classification tasks, we find that our approach is simple, easy to use and competitive with in-distribution with grid imputation, self-supervised representation learning, and time series foundation models, while exhibiting smaller relative performance drops when transferring to new hospitals. We study the variation in performance across prompt design, with structured prompts being crucial to reducing the variance of the predictive models without altering mean accuracy. We find that using these portable representations improves few-shot learning and does not increase demographic recoverability of age or sex relative to baselines, suggesting little additional privacy risk. Our work points to the potential that LLMs hold as tools to enable the scalable deployment of production grade predictive models by reducing the engineering overhead.

---

## 论文详细总结（自动生成）

这篇论文探讨了利用大语言模型（LLM）解决临床机器学习模型在不同医疗机构间迁移困难的问题。以下是对该论文的详细总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：临床预测模型通常在特定医院的数据上训练，由于不同医院的传感器架构、采样频率、编码习惯和患者群体存在差异（即分布偏移），模型在跨机构部署时性能往往大幅下降，且需要高昂的重新校准和微调成本。
*   **研究动机**：作者提出“可移植的输入表示（Portable Representations）”这一概念。与其让模型去适应每个医院的原始数据，不如将异构的医疗记录映射到一个语义对齐、与站点无关的通用接口中。
*   **整体含义**：借鉴临床医生通过“交接班摘要”来抽象复杂病情的做法，研究 LLM 是否能将不规则的 ICU 时间序列转化为标准化的自然语言摘要，从而生成一种通用的患者嵌入向量，使下游预测器无需针对新医院进行大规模调整。

### 2. 论文提出的方法论：Record2Vec
*   **核心思想**：采用“先摘要、后嵌入（Summarize-then-Embed）”的流水线。
*   **关键技术细节**：
    *   **摘要阶段（Summarization）**：使用冻结（Frozen）的 LLM（如 Gemini-2.0 Flash、MedGemma、Llama-3.1）将 48 小时不规则的 ICU 记录（包括生命体征、实验室检查、治疗干预等）转化为简洁的临床摘要。
    *   **嵌入阶段（Embedding）**：使用冻结的文本嵌入模型（如 Qwen3-Embedding）将生成的文本摘要映射为固定长度的向量 $z_i$。
    *   **预测阶段**：该向量作为标准预测模型（如 PatchTSMixer、MLP、LSTM）的输入，用于执行下游任务。
*   **算法流程**：原始不规则记录 $\rightarrow$ 序列化文本 $\rightarrow$ LLM 摘要 $\rightarrow$ 文本嵌入向量 $\rightarrow$ 下游预测器 $\rightarrow$ 任务标签。

### 3. 实验设计
*   **数据集**：使用了三个大型 ICU 队列：**MIMIC-IV**（美国）、**HiRID**（瑞士）和 **PPICU**（外部独立数据集）。
*   **预测任务**：涵盖 7 个任务，包括多变量预测（未来 24h 指标）、剩余住院时间（LoS）、住院死亡率、未来 24h 药物使用（血管加压素/抗生素）以及实验室检查开单预测。
*   **对比方法（Benchmarks）**：
    *   **网格插补流水线**：均值填充、前向填充、线性插值。
    *   **自监督表示学习**：TSDE（时间序列扩散嵌入）。
    *   **时间序列基础模型**：TimesFM（谷歌开发的解码器架构模型）。
    *   **通用医疗预测框架**：GenHPF 的变体。
    *   **直接嵌入对照**：不经过 LLM 摘要，直接嵌入原始序列化文本。

### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA L40S GPU**（24GB 显存）上运行。
*   **算力消耗**：
    *   单机训练：大多数训练和推理任务可在单块 L40S 上完成。
    *   大规模推理：使用 4 块 L40S GPU 通过 vLLM 框架进行高效推理。
    *   成本分析：Gemini-2.0 Flash 的推理成本约为每 1000 个样本 0.7 美元，每名患者的延迟约为 0.26 秒。

### 5. 实验数量与充分性
*   **实验规模**：论文长达 61 页，包含了极其详尽的实验数据。
*   **充分性**：
    *   **多维度评估**：涵盖了分布内性能、跨站点迁移性能、少样本学习（16 个样本微调）以及隐私风险评估。
    *   **消融实验**：对比了不同 LLM 摘要器、不同提示词（Prompt）设计（Zero-shot, CoT, ICD 风格等）以及不同嵌入模型的表现。
    *   **客观性**：通过 4 个随机种子取平均值，并提供了详细的标准差，确保了结果的稳健性。

### 6. 论文的主要结论与发现
*   **性能优异**：Record2Vec 在分布内任务中赢得了 15 个任务中的 13 个；在跨站点迁移任务中，其表现显著优于所有基准模型，性能下降幅度最小。
*   **少样本能力强**：在仅有 16 个目标医院样本的情况下，Record2Vec 能够迅速恢复性能，接近分布内训练的上限。
*   **提示词敏感度**：结构化提示词（如 ICD 风格）能有效降低预测方差，但对平均准确率影响较小。
*   **隐私安全**：该方法并未增加年龄和性别等敏感信息的泄露风险，甚至在某些情况下降低了人口统计学信息的可恢复性。
*   **效率提升**：摘要过程将 Token 数量减少了约 25 倍，大幅降低了后续嵌入模型的计算开销。

### 7. 优点
*   **语义对齐**：通过自然语言中和了不同医院间单位、术语和采样频率的差异。
*   **高可移植性**：实现了“一次训练，多处部署”，显著减少了每家医院的工程开销。
*   **灵活性**：由于 LLM 和嵌入模型是冻结的，该方法可以轻松集成到现有的临床工作流中，无需复杂的端到端微调。

### 8. 不足与局限
*   **隐私合规风险**：目前依赖外部 LLM API（如 Gemini），在处理敏感患者数据时存在合规性挑战，未来需转向本地化部署。
*   **推理延迟与成本**：尽管比直接嵌入更高效，但相比纯数值模型，LLM 推理仍带来额外的计算延迟和 API 费用。
*   **理论深度不足**：研究主要基于经验性观察，缺乏关于“为什么自然语言摘要能保证站点不变性”的严格数学证明或因果推导。
*   **信息丢失风险**：摘要过程必然会丢弃部分原始数值细节，虽然实验显示这不影响主要任务，但在某些极端精细的预测场景下可能存在隐患。

（完）
