---
title: Scaling Recurrence-aware Foundation Models for Clinical Records via Next-Visit Prediction
title_zh: 通过下次就诊预测扩展临床记录的循环感知基础模型
authors: "Haresh Rengaraj Rajamohan, Xiang Gao, Weicheng Zhu, Shih-Lun Huang, Long Chen, Gabe Schulman, Huizhen Jin, Shengduo Li, Yixuan Wang, Huidi Yang, Kyunghyun Cho, Cem M. Deniz, Narges Razavian"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24562v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 针对序列化电子健康记录和临床事件的生成式预训练
tldr: RAVEN是一个针对结构化电子健康档案（EHR）的大规模预训练生成模型。它通过“复发感知”的下一次就诊事件预测策略，在超过百万患者的数据集上进行自回归训练。该研究引入了针对重复事件的正则化，揭示了评估中的指标虚高陷阱，并探讨了模型规模与数据量的扩展规律。RAVEN在零样本疾病预测任务中表现优异，且具备良好的跨机构泛化能力。
selection_source: fresh_fetch
motivation: 尽管大规模预训练在语言模型中取得成功，但在处理具有重复性特征的结构化电子健康档案（EHR）方面的潜力尚未得到充分挖掘。
method: 提出RAVEN模型，采用基于复发感知的下一次就诊事件预测策略进行自回归预训练，并引入针对重复事件预测的正则化机制。
result: RAVEN在零样本疾病发病率预测中媲美全微调模型，并揭示了在数据受限情况下单纯增加模型参数量的局限性。
conclusion: 通过结合复发感知机制和合理的扩展策略，生成式预训练模型能有效提升EHR数据的表征能力及跨医疗机构的泛化性能。
---

## 摘要
虽然大规模预训练彻底改变了语言建模，但在使用结构化电子健康档案 (EHR) 的医疗保健领域，其潜力仍未得到充分探索。我们提出了 RAVEN，这是一种基于循环感知下次就诊事件预测的时序 EHR 数据生成式预训练策略。利用超过一百万名独立个体的数据库，我们的模型学习在以患者历史记录为条件的情况下，自回归地生成下次就诊的标记化临床事件。我们引入了针对预测重复事件的正则化，并指出了基于 EHR 的基础模型评估中的一个关键陷阱：当新发病例与后续发生未加区分时，重复的事件标记会虚高性能指标。此外，我们实证研究了在数据受限、计算饱和状态下的扩展行为，表明在没有相应增加数据量的情况下，单纯增加模型规模并非最优。我们通过零样本预测评估了模型对多种疾病发病率的预测能力，其表现可与经过完全微调的基于表示的 Transformer 模型相媲美，并优于广泛使用的基于模拟的下个标记方法。最后，在无需额外参数更新的情况下，我们展示了 RAVEN 在存在有损临床代码映射和特征覆盖缺口的情况下，仍能泛化到外部患者队列。

## Abstract
While large-scale pretraining has revolutionized language modeling, its potential remains underexplored in healthcare with structured electronic health records (EHRs). We present RAVEN, a novel generative pretraining strategy for sequential EHR data based on Recurrence-Aware next-Visit EveNt prediction. Leveraging a dataset of over one million unique individuals, our model learns to autoregressively generate tokenized clinical events for the next visit conditioned on patient history. We introduce regularization on predicting repeated events and highlight a key pitfall in EHR-based foundation model evaluations: repeated event tokens can inflate performance metrics when new onsets are not distinguished from subsequent occurrences. Furthermore, we empirically investigate the scaling behaviors in a data-constrained, compute-saturated regime, showing that simply increasing model size is suboptimal without commensurate increases in data volume. We evaluate our model via zero-shot prediction for forecasting the incidence of a diverse set of diseases, where it rivals fully fine-tuned representation-based Transformer models and outperforms widely used simulation-based next-token approaches. Finally, without additional parameter updates, we show that RAVEN can generalize to an external patient cohort under lossy clinical code mappings and feature coverage gaps.

---

## 论文详细总结（自动生成）

这是一份关于论文《Scaling Recurrence-aware Foundation Models for Clinical Records via Next-Visit Prediction》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：尽管大规模预训练在自然语言处理（NLP）领域取得了巨大成功，但在医疗电子健康档案（EHR）领域的应用仍面临挑战。传统的临床预测模型多为针对特定疾病、特定时间窗口的判别式模型，难以扩展且数据利用率低。
*   **核心问题**：
    1.  **院内事件无序性**：单次就诊内的临床事件（诊断、用药、化验）通常没有严格的时间先后顺序，传统的“下一标记预测（Next-token Prediction）”强制施加顺序并不合理。
    2.  **慢性病重复偏见**：EHR中慢性病诊断会反复出现，模型容易通过简单“复读”历史记录来获得虚高的准确率，而忽略了临床上更关键的“新发病（New Onset）”预测。
    3.  **数据受限下的扩展性**：医疗数据量远小于互联网文本，如何在有限数据和充足算力下寻找最优的模型规模（Scaling Law）尚不明确。

### 2. 方法论：RAVEN 模型
*   **核心思想**：提出**RAVEN（Recurrence-Aware next-Visit EveNt prediction）**框架，将预训练目标从预测“下一个标记”改为预测“下一次就诊的完整事件集”。
*   **关键技术细节**：
    *   **下次就诊多标签预测**：利用 Transformer 解码器，通过一个带有时间编码的特殊分隔符 `[SEP]`，在一次前向传播中预测下一次就诊的所有临床事件（多标签分类）。
    *   **循环感知正则化（Recurrence-Aware Regularization）**：引入历史相关的损失权重。对于目标事件 $k$，根据其在患者历史中出现的次数 $c$ 进行降权，权重 $w = \lambda^c$（$\lambda$ 为衰减因子）。这迫使模型减少对重复信息的依赖，专注于预测新发生的临床变化。
    *   **时间感知推理**：通过在输入序列末尾添加带有未来时间戳（如 $t+2$ 年）的 `[SEP]` 标记，实现对任意未来时间窗口的零样本（Zero-shot）风险评估。

### 3. 实验设计
*   **数据集**：
    *   **内部数据**：NYU Langone Health 数据库，包含 129 万名患者、十年的纵向记录、4.2 万个唯一临床标记。
    *   **外部验证**：斯坦福大学的 **EHRSHOT** 公开基准数据集（6,739 名患者），用于测试跨机构的泛化能力。
*   **任务场景**：针对 7 种多样化疾病（痴呆症、膝骨关节炎、胰腺癌、前列腺癌、急性心肌梗死、充血性心力衰竭、COPD）进行 2 年和 5 年的发病预测。
*   **对比方法（Benchmarks）**：
    *   **生成式基准**：标准下一标记交叉熵模型（Multiclass）、集合损失模型（SeqLoss）、联合间隙与事件生成模型（EGE）。
    *   **判别式基准**：经过全监督微调的 BERT 基础模型（BERT-FT）。
    *   **少样本基准**：在 EHRSHOT 上对比了 CLMBR-T、GBM、逻辑回归和随机森林。

### 4. 资源与算力
*   **算力投入**：论文明确提到在“计算饱和（Compute-saturated）”状态下进行研究。
*   **实验规模**：作者训练了**超过 80 个不同配置的模型**，参数量跨度从 **0.68M 到 848M**。
*   **具体细节**：文中未明确给出具体的 GPU 型号（如 A100 数量）或总训练时长，但提到采用了早期停止（Early Stopping）策略，且模型训练持续 10 到 20 个 Epoch 直至验证集损失收敛。

### 5. 实验数量与充分性
*   **实验组数**：非常充分。涵盖了 4 种数据预算（10%、25%、50%、100%）与 8 种模型规模的交叉实验，用以推导 EHR 领域的 Scaling Law。
*   **客观性与公平性**：
    *   采用了严格的患者级划分（70/15/15），防止信息泄露。
    *   在外部迁移实验中，面对代码映射有损（仅能利用 25% 的数据行）的极端不利条件下进行测试，体现了实验的客观性。
    *   引入了“准时率（On-time Rate）”这一新指标，专门评估模型预测新发病的能力，而非仅仅看传统的 AUROC。

### 6. 主要结论与发现
*   **零样本能力强**：RAVEN 在零样本预测下的表现优于所有基于模拟（Rollout）的自回归基准，且在多个疾病上媲美甚至超过了全微调的 BERT 模型。
*   **正则化的必要性**：引入循环感知正则化显著提升了模型对“新发病”的预测能力（准时率翻倍），尽管这可能会略微降低整体的 AUPRC（因为牺牲了对重复慢性病的预测精度）。
*   **EHR 扩展规律**：在数据受限的情况下，模型性能随规模呈 **U 型曲线**。盲目增加参数量会导致严重的过拟合，模型容量必须与可用患者队列规模相匹配。
*   **跨机构泛化**：尽管存在代码系统差异和特征缺失，RAVEN 在外部数据集上仍展现出极强的零样本迁移能力，表现优于许多经过少样本训练的本地模型。

### 7. 优点与亮点
*   **效率极高**：相比于需要进行数百次蒙特卡洛模拟（Rollout）的传统生成模型，RAVEN 仅需一次前向传播即可预测未来风险。
*   **解决行业痛点**：敏锐捕捉到了 EHR 评估中“重复诊断导致指标虚高”的陷阱，并给出了有效的正则化解决方案。
*   **理论深度**：对数据受限状态下的 Scaling Law 进行了深入探讨，为医疗 AI 领域如何分配计算资源提供了实证指导。

### 8. 不足与局限
*   **代码映射损失**：在跨机构迁移时，由于不同医院编码体系（如 ICD-10 与 SNOMED）的差异，导致大量信息在转换中丢失。
*   **特征覆盖有限**：目前模型主要处理结构化代码，尚未整合临床文本（医生笔记）或医学影像数据。
*   **静态预测偏见**：模型学习的是现有的临床实践，可能会继承医疗系统中的既有偏见（如某些人群的诊断延迟）。
*   **应用限制**：目前仅验证了二分类的发病预测，尚未扩展到疾病进展建模或干预效果模拟。

（完）
