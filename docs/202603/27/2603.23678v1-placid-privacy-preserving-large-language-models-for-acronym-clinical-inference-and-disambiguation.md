---
title: "PLACID: Privacy-preserving Large language models for Acronym Clinical Inference and Disambiguation"
title_zh: PLACID：用于临床首字母缩略词推理与消歧的隐私保护大语言模型
authors: "Manjushree B. Aithal, Ph. D., Alexander Kotz, James Mitchell, Ph. D"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23678v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 用于临床推理的隐私保护大语言模型
tldr: 本研究针对医疗领域临床缩写歧义及隐私保护挑战，提出了PLACID框架。该框架采用端侧部署的小参数模型（2B-10B），通过级联流水线实现隐私保护：先利用通用本地模型检测缩写，再由领域特定生物医学模型进行上下文相关的扩展。实验证明，该方法在确保数据不出本地的前提下，显著提升了缩写扩展的准确率，为医疗AI应用提供了兼顾隐私与性能的解决方案。
selection_source: fresh_fetch
motivation: 临床文本中缩写歧义可能导致严重医疗错误，而云端大模型处理敏感医疗数据存在隐私泄露风险。
method: 提出一种级联流水线，利用本地通用模型检测缩写，并将其路由至领域特定模型进行上下文消歧。
result: 级联方法将缩写扩展的准确率从通用模型的0.655提升至0.81，同时保持了极高的检测准确率（0.988）。
conclusion: 2B-10B规模的端侧模型能够有效实现高保真度的临床缩写消歧，是平衡医疗数据隐私与模型性能的可行方案。
---

## 摘要
大语言模型（LLMs）在许多领域提供了变革性的解决方案，但其在医疗领域的集成受到严格数据隐私限制的阻碍。临床叙述中充斥着具有歧义的首字母缩略词，误解这些缩写可能会导致严重的后果，例如危及生命的用药错误。虽然依赖云端的 LLMs 在首字母缩略词消歧（Acronym Disambiguation）方面表现出色，但将受保护健康信息（PHI）传输至外部服务器违反了隐私框架。为了弥补这一差距，本研究率先评估了完全部署在设备端的小参数模型，以确保隐私保护。我们引入了一种隐私保护的级联流水线，利用通用本地模型检测临床首字母缩略词，并将其路由至特定领域的生物医学模型以进行上下文相关的扩展。结果显示，虽然通用指令遵循模型实现了极高的检测准确率（~0.988），但其扩展能力显著下降（~0.655）。我们的级联方法利用领域特定的医学模型将扩展准确率提升至（~0.81）。这项创新工作证明了隐私保护的设备端（2B-10B）模型能够提供高保真的临床首字母缩略词消歧支持。

## Abstract
Large Language Models (LLMs) offer transformative solutions across many domains, but healthcare integration is hindered by strict data privacy constraints. Clinical narratives are dense with ambiguous acronyms, misinterpretation these abbreviations can precipitate severe outcomes like life-threatening medication errors. While cloud-dependent LLMs excel at Acronym Disambiguation, transmitting Protected Health Information to external servers violates privacy frameworks. To bridge this gap, this study pioneers the evaluation of small-parameter models deployed entirely on-device to ensure privacy preservation. We introduce a privacy-preserving cascaded pipeline leveraging general-purpose local models to detect clinical acronyms, routing them to domain-specific biomedical models for context-relevant expansions. Results reveal that while general instruction-following models achieve high detection accuracy (~0.988), their expansion capabilities plummet (~0.655). Our cascaded approach utilizes domain-specific medical models to increase expansion accuracy to (~0.81). This novel work demonstrates that privacy-preserving, on-device (2B-10B) models deliver high-fidelity clinical acronym disambiguation support.

---

## 论文详细总结（自动生成）

### PLACID：用于临床首字母缩略词推理与消歧的隐私保护大语言模型论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：临床文档中存在大量具有歧义的首字母缩略词（如“PT”可指物理治疗或私人教练），误解这些缩写可能导致严重的医疗错误。
*   **研究背景**：虽然云端大语言模型（LLMs）在缩写消歧（AD）方面表现优异，但将包含受保护健康信息（PHI）的临床文本传输至云端违反了 HIPAA 和 GDPR 等隐私法规。
*   **研究意义**：本研究旨在探索如何在完全本地化、隐私保护的环境下，利用小参数（2B-10B）模型实现高精度的临床缩写检测与扩展。

#### 2. 论文提出的方法论
*   **核心思想**：提出了 **PLACID** 框架，通过本地部署的小模型实现隐私保护。
*   **关键技术细节**：
    *   **单阶段模式（Single-pass）**：单个模型同时执行缩写检测和上下文扩展，适用于对延迟要求极高的场景。
    *   **级联模式（Cascaded）**：采用“分而治之”策略。首先由通用小模型（如 Gemma2）负责检测文本中的缩写；随后将检测到的缩写与原始上下文路由至领域特定模型（如 MedGemma）进行专业扩展。
    *   **零样本提示（Zero-shot Prompting）**：不依赖少样本示例，强制模型利用其内在知识库进行逻辑推理。
    *   **输出增强**：模型不仅输出扩展词，还提供扩展理由（Rationale）和置信度评分（Confidence Score），增强了临床决策的可解释性。

#### 3. 实验设计
*   **数据集**：
    *   **GLADIS Benchmark**：主要使用其生物医学子集（源自 PubMed 摘要），包含约 1.2 万个样本。
    *   **测试集划分**：级联模式使用 6,299 个实例；单阶段模式使用经过严格过滤的 2,026 个单缩写实例；另有 1,000 个通用领域数据用于基准对比。
*   **评估指标**：
    *   **检测准确率**：精确字符串匹配。
    *   **扩展准确率**：使用 BLEU、ROUGE-L 和 METEOR 指标，并区分“原始文本（Raw）”和“清洗后文本（Clean）”两种评估管道。
*   **对比方法**：
    *   **通用模型**：Gemma2:2B, Llama3.2:3B, Mistral:7B, Apple Foundation Model。
    *   **医学微调模型**：MedGemma1.5:4B, BioMistral:7B。

#### 4. 资源与算力
*   **硬件平台**：实验完全在本地设备端运行，使用的是搭载 **Apple M4-Max GPU** 的设备。
*   **软件框架**：利用 Apple 的 **MLX 框架** 进行 GPU 推理优化。
*   **模型配置**：
    *   2B-4B 模型使用全精度运行。
    *   7B 模型（Mistral/BioMistral）为解决内存瓶颈（OOM），采用了 **4-bit 量化**。
*   **训练时长**：文中未提及训练时长，因为研究重点在于现成（off-the-shelf）模型的**零样本推理**性能评估。

#### 5. 实验数量与充分性
*   **实验规模**：针对 6,000+ 临床实例进行了测试，每组实验重复 5 次迭代以确保生成稳定性并计算标准差。
*   **充分性评价**：实验设计较为充分。作者不仅对比了不同参数规模的模型，还对比了通用模型与医学专用模型的性能差异，并引入了“遮蔽率（Overshadow Ratio）”来衡量数据集难度（即正确扩展词并非最常见含义的情况），确保了评估的客观性。

#### 6. 论文的主要结论与发现
*   **检测与扩展的脱节**：通用模型在缩写检测上表现极佳（准确率达 0.988），但在临床扩展生成上表现较差（约 0.655）。
*   **领域知识的重要性**：医学微调模型在扩展任务中显著优于通用模型，级联模式下的 ROUGE-L 准确率提升至 **0.81**。
*   **本地化可行性**：证明了 2B-10B 规模的端侧模型在处理敏感临床叙述时，既能保证隐私，又能提供高保真的消歧支持。
*   **模型行为**：医学模型（如 MedGemma）能够根据上下文进行复杂的逻辑推理，而非简单地选择最常见的定义。

#### 7. 优点
*   **隐私保护**：完全本地化部署，解决了医疗数据上云的合规性痛点。
*   **级联架构创新**：巧妙结合了通用模型强大的指令遵循能力和专用模型深厚的领域知识。
*   **可解释性**：通过输出推理过程，满足了医疗场景对 AI 透明度的要求。
*   **高效推理**：利用 MLX 框架在移动端/桌面端芯片上实现了高效推理。

#### 8. 不足与局限
*   **模型过自信**：研究发现模型在自我评估置信度时经常出现“过度自信”现象（如对错误答案给出 98% 的置信度）。
*   **硬件限制**：7B 模型仍需量化才能在端侧运行，可能存在潜在的精度损失（尽管初步验证显示影响较小）。
*   **语言与领域局限**：实验主要集中在英语 PubMed 数据，尚未验证在多语言或非结构化手写临床笔记中的表现。
*   **幻觉风险**：尽管使用了医学模型，但在极度缺乏上下文的情况下，模型仍可能产生错误的推理。

（完）
