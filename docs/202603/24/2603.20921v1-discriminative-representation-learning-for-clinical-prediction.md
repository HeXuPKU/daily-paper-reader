---
title: Discriminative Representation Learning for Clinical Prediction
title_zh: 临床预测中的判别式表示学习
authors: "Yang Zhang, Li Fan, Samuel Lawrence, Shi Li"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20921v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 使用纵向电子健康记录数据进行临床预测
tldr: 本研究挑战了医疗大模型依赖自监督预训练的现状，提出一种针对临床预测的判别式表示学习框架。该方法在拥有高质量监督信号时，通过最大化类间分离度并压缩类内方差来优化模型表示。在多项电子健康档案任务中，该方法在判别性能、校准度和样本效率上均优于掩码、自回归及对比学习等预训练基准，证明了直接监督在结局驱动领域是更有效的表示学习驱动力。
selection_source: fresh_fetch
motivation: 探讨在拥有高质量标签的临床预测场景下，直接的结局对齐是否比通用的自监督预训练更具归纳偏置优势。
method: 提出一种监督学习框架，通过最大化类间分离度与类内方差的比值来显式塑造表示空间的几何结构。
result: 在死亡率和再入院等多种临床预测任务中，该方法在相同模型容量下全面超越了掩码、自回归和对比预训练基准。
conclusion: 在结局驱动的医疗领域，直接监督学习是比大规模自监督预训练更优的表示学习驱动方式，且能显著简化训练流程。
---

## 摘要
医疗保健领域的基座模型在很大程度上采用了继承自自然语言处理和计算机视觉的自监督预训练目标，在下游适配之前强调重构和大规模表示学习。我们在以结果为中心的临床预测场景中重新审视了这一范式，并认为当高质量的监督信息可用时，直接的结果对齐可能比生成式预训练提供更强的归纳偏置。我们提出了一种监督深度学习框架，通过最大化类间分离度（相对于类内方差）来显式地塑造表示几何结构，从而将模型容量集中在具有临床意义的轴向上。在包括死亡率和再入院预测在内的多项纵向电子健康档案任务中，我们的方法在模型容量匹配的情况下，始终优于掩码、自回归和对比预训练基准。所提出的方法提高了判别力、校准度和样本效率，同时将训练流程简化为单阶段优化。这些发现表明，在低熵、结果驱动的医疗保健领域，监督可以作为表示学习在统计上的最优驱动力，挑战了大规模自监督预训练是实现强大临床性能的前提这一假设。

## Abstract
Foundation models in healthcare have largely adopted self supervised pretraining objectives inherited from natural language processing and computer vision, emphasizing reconstruction and large scale representation learning prior to downstream adaptation. We revisit this paradigm in outcome centric clinical prediction settings and argue that, when high quality supervision is available, direct outcome alignment may provide a stronger inductive bias than generative pretraining. We propose a supervised deep learning framework that explicitly shapes representation geometry by maximizing inter class separation relative to within class variance, thereby concentrating model capacity along clinically meaningful axes. Across multiple longitudinal electronic health record tasks, including mortality and readmission prediction, our approach consistently outperforms masked, autoregressive, and contrastive pretraining baselines under matched model capacity. The proposed method improves discrimination, calibration, and sample efficiency, while simplifying the training pipeline to a single stage optimization. These findings suggest that in low entropy, outcome driven healthcare domains, supervision can act as the statistically optimal driver of representation learning, challenging the assumption that large scale self supervised pretraining is a prerequisite for strong clinical performance.

---

## 论文详细总结（自动生成）

以下是对论文《Discriminative Representation Learning for Clinical Prediction》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：论文探讨了在医疗临床预测领域，是否必须遵循自然语言处理（NLP）和计算机视觉（CV）中主流的“大规模自监督预训练 + 下游微调”范式。
*   **研究动机**：
    *   **范式反思**：目前的医疗基座模型大多采用掩码（Masked）或自回归（Autoregressive）等重构性目标。作者认为这些目标旨在还原输入数据的全部变异性，但在临床场景中，许多变异属于与结局无关的“噪声”。
    *   **领域特性**：临床数据具有低熵、分布受限且显式受结局驱动（Outcome-driven）的特点。
    *   **核心假设**：在高质量监督信号可用时，直接进行结局对齐（Outcome Alignment）比通用的生成式预训练能提供更强的归纳偏置，使模型容量集中在具有病理生理意义的判别轴向上。

### 2. 方法论
*   **核心思想**：提出一种“结局对齐的表示学习”框架，通过显式塑造表示空间的几何结构，最大化类间分离度并压缩类内方差。
*   **关键技术细节**：
    *   **表示编码**：使用深度神经网络（如 Transformer）将患者纵向轨迹 $x$ 映射为高维嵌入 $h_\theta(x)$。
    *   **判别式正则化**：引入基于 Fisher 判别分析思想的 **Rayleigh 商（Rayleigh quotient）** 作为正则项 $R_{disc}$。
    *   **公式实现**：
        *   计算类间均值差异：$\mu_1 - \mu_0$。
        *   计算类内协方差之和：$\Sigma_w$。
        *   优化目标：$L_{total} = L_{sup} - \lambda R_{disc}$，其中 $L_{sup}$ 是标准的交叉熵损失，$R_{disc}$ 鼓励模型在嵌入空间中拉开不同结局患者的距离，同时让同类患者更聚集。
*   **统计解释**：从信息论角度看，该方法旨在增加表示与标签之间的互信息 $I(h(X); Y)$，而非仅仅增加与输入的互信息 $I(h(X); X)$。

### 3. 实验设计
*   **数据集与场景**：基于结构化电子健康档案（EHR）的三个纵向预测任务：
    1.  **住院死亡率预测**。
    2.  **30天再入院预测**。
    3.  **48小时内急性失代偿预测**。
*   **对比基准（Benchmarks）**：
    *   **Masked EHR Transformer**：类似于 CEHR-BERT 的掩码预训练。
    *   **Autoregressive EHR Model**：类似于 CEHR-GPT 的自回归预训练。
    *   **Contrastive Pretraining**：对比学习预训练。
    *   **Supervised Transformer**：无几何正则化的标准监督学习。
*   **评估指标**：AUROC、AUPRC、Brier 分数（校准度）、ECE（预期校准误差）。

### 4. 资源与算力
*   **模型规模**：所有对比模型的参数量统一控制在约 **35M**，以确保公平比较。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。仅提到该方法由于是单阶段优化，且正则项计算开销极小（仅涉及 batch 级别的均值和方差计算），因此计算成本与标准监督学习相当。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了三个核心临床任务。
    *   进行了**样本效率实验**：测试在 25%、50%、75% 等不同数据比例下的表现。
    *   进行了**跨机构泛化实验**：在未经额外预训练的情况下，直接在外部医院系统验证模型鲁棒性。
    *   进行了**几何结构分析**：定量计算了不同模型的 Rayleigh 商。
*   **充分性评价**：实验设计较为全面，通过控制变量（相同参数量、相同优化设置）确保了对比的客观性。跨机构验证增强了结论的说服力。

### 6. 主要结论与发现
*   **性能全面领先**：所提方法在所有任务中均优于掩码、自回归和对比预训练基准。AUROC 提升了 1.9 至 3.6 个百分点，AUPRC 提升尤为显著。
*   **校准度更高**：Brier 分数和 ECE 的下降表明模型给出的风险概率更准确。
*   **样本效率极佳**：该方法仅需 60% 的标注数据即可达到掩码预训练在 100% 数据下的性能。
*   **几何结构与性能正相关**：实验证实，嵌入空间的类间分离度（Rayleigh 商）与最终的预测准确率存在强正相关（r=0.91）。

### 7. 优点
*   **流程简化**：将复杂的“预训练-微调”两阶段流程简化为单阶段优化，降低了工程复杂度。
*   **统计学支撑**：利用 Fisher 信息矩阵和数据处理不等式为监督学习的优越性提供了理论解释。
*   **针对性强**：有效解决了生成式目标在临床预测中可能分配过多容量给“无关变异”的问题。

### 8. 不足与局限
*   **任务局限性**：目前仅验证了结构化 EHR 数据的二分类预测任务。对于标签极度稀疏或需要开放式生成的任务（如医疗报告生成），自监督预训练可能仍具优势。
*   **规模效应未知**：实验模型规模较小（35M），尚未探讨在百亿级（Billion-scale）参数下，判别式正则化是否依然能显著超越隐式学习到判别结构的超大型生成模型。
*   **Batch 依赖性**：判别正则项依赖于 batch 内的统计量，在极小 batch size 或类别极度不平衡时，统计估计的稳定性可能受到影响。

（完）
