---
title: "CACSurv: Concordance-Aligned Comparative Learning with Large Language Models for Cancer Survival Prediction"
title_zh: CACSurv：基于一致性对齐的大语言模型比较学习用于癌症生存预测
authors: "Tianqi Xiang, Qixiang Zhang, Xinpeng Ding, Yi Li, Xiaomeng Li"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16594v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 基于大语言模型的报告中心癌症生存预测，是医学大语言模型的核心应用
tldr: 癌症生存预测通常依赖结构化数据或病理图像，患者报告未被充分利用。现有大模型直接回归生存时间，既未利用生存分析中的排序性质，也无法处理右删失数据。本文提出CACSurv框架，将生存预测重构为小队列对比推理，利用一致性对齐奖励从删失数据中学习相对预后排序，推理时通过蒙特卡洛参考聚合生成最终排名。在六个TCGA癌症队列上平均C-index达0.722，显著优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.16594v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1765, \"height\": 493}, {\"url\": \"assets/figures/arxiv/2608.16594v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1816, \"height\": 863}, {\"url\": \"assets/figures/arxiv/2608.16594v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 871, \"height\": 800}, {\"url\": \"assets/figures/arxiv/2608.16594v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 814, \"height\": 886}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.16594v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1839, \"height\": 951}, {\"url\": \"assets/tables/arxiv/2608.16594v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 754, \"height\": 182}, {\"url\": \"assets/tables/arxiv/2608.16594v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 852, \"height\": 181}, {\"url\": \"assets/tables/arxiv/2608.16594v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 209}]"
motivation: 现有LLM直接回归生存时间，忽视生存评估的排序本质与右删失数据，导致公式和监督两重不匹配。
method: 将生存建模转为小队列对比推理，用一致性对齐奖励利用删失数据提供排序监督，推理时通过蒙特卡洛参考聚合得到队列排名。
result: 在TCGA六个癌症队列上C-index全部最高，平均0.722，比最强已发表模型高6.5个百分点，比最强LLM回归基线高4.2个百分点。
conclusion: CACSurv证明基于报告的比较式学习能有效提升癌症生存预测，并提供了TCGA-SurvReport基准与开源资源。
---

## 摘要
癌症生存预测支持治疗计划、风险分层和随访管理。现有方法使用结构化临床变量、全切片图像、基因组图谱或多模态输入，而患者报告仍未得到充分探索。我们研究以报告为中心的生存预测，使用组织病理学、临床和分子证据的报告。大语言模型（LLMs）能够对这些报告进行推理，但逐案例的时间回归引入了两种不匹配。首先，公式化不匹配源于生存评估依赖于对可比患者进行排序，而独立的时间预测并不强制排序一致性。其次，监督不匹配源于删失患者的观察时间仅表示其生存超过该时间点，不能作为精确的回归目标，尽管它仍暗示了与较早死亡患者的相对排序。为解决这些不匹配，我们提出了CACSurv，一种以报告为中心的生存预测的一致性对齐比较框架。CACSurv将生存建模重新构建为小队列比较推理，其中LLM预测相对预后排序。我们引入了从右删失下的可比关系中推导出的一致性对齐奖励，使删失结果能够在没有精确事件时间目标的情况下提供排序监督。在推理时，蒙特卡洛参考聚合将每个患者与采样参考进行比较，并将位置聚合为队列级排序。我们建立了TCGA-SurvReport基准，涵盖六个TCGA癌症队列。CACSurv在所有六个队列上均取得最高C指数，平均C指数为0.722，比最强已发表生存模型高出6.5个百分点，比最强LLM时间回归基线高出4.2个百分点。我们的代码、模型和数据集将在https://github.com/xmed-lab/CACSurv上提供。

## Abstract
Cancer survival prediction supports treatment planning, risk stratification, and follow-up management. Existing methods use structured clinical variables, whole-slide images, genomic profiles, or multimodal inputs, while patient reports remain underexplored. We study report-centric survival prediction using reports that organize pathological, clinical, and molecular evidence. Large language models (LLMs) can reason over such reports, but case-wise time regression introduces two mismatches. First, a formulation mismatch arises because survival evaluation depends on ordering comparable patients, whereas independent time predictions do not enforce ranking consistency. Second, a supervision mismatch arises because a censored patient's observed time indicates survival beyond that point and cannot serve as an exact regression target, although it still implies orderings relative to patients who died earlier. To address these mismatches, we propose CACSurv, a Concordance-Aligned Comparative framework for report-centric survival prediction. CACSurv reformulates survival modeling as mini-cohort comparative reasoning, where an LLM predicts relative prognostic orderings. We introduce concordance-aligned rewards derived from comparable relations under right censoring, enabling censored outcomes to provide ranking supervision without exact event-time targets. At inference, Monte Carlo Reference Aggregation compares each patient with sampled references and aggregates positions into a cohort-level ranking. We establish TCGA-SurvReport, a benchmark covering six TCGA cancer cohorts. CACSurv achieves the highest C-index on all six cohorts and an average C-index of 0.722, outperforming the strongest published survival model by 6.5 percentage points and the strongest LLM time-regression baseline by 4.2 percentage points. Our code, models, and dataset will be available at https://github.com/xmed-lab/CACSurv.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

### 研究动机与背景

- 癌症生存预测在治疗计划、风险分层和随访管理中发挥重要作用，但现有方法主要依赖结构化临床变量、全切片图像（WSIs）、基因组图谱或多模态组合，**患者报告作为主要输入尚未被充分探索**。
- 患者报告将病理、临床和分子证据组织为连贯文本，信息密集。随着大语言模型（LLMs）在长文本处理和多源信息整合方面的进步，利用 LLM 直接基于患者报告进行生存预测成为一种自然且可行的方向。
- 然而，论文指出，直接将 LLM 用于**逐案例（case-wise）生存时间回归**存在两个根本性不匹配：
  - **公式化不匹配**：生存评估的核心指标 C-index 衡量的是可比患者的排序正确性，而独立的时间回归不强制患者间排序一致性；
  - **监督不匹配**：右删失（right-censored）患者的观察时间仅表示其存活超过该时间点，不能作为精确回归目标，但相对较早死亡患者仍提供有效的排序关系。
- 这两个不匹配的共因在于：**生存评估和删失监督本质上是关系性的（relational）**，而绝对时间回归则将生存预测当作独立的数值预测问题。

## 2. 论文提出的方法论

### 核心思想

CACSurv（Concordance-Aligned Comparative Survival framework）将生存预测从独立绝对时间生成重构为**小队列（mini-cohort）内的相对预后排序**：

- **训练阶段**：以成对或三元组患者为基本预测单元，让 LLM 直接输出患者间生存长短的排序，并使用一致性对齐奖励（concordance-aligned rewards）进行 GRPO 优化；
- **推理阶段**：通过**蒙特卡洛参考聚合（Monte Carlo Reference Aggregation, MCRA）**，将每个评估患者与随机采样的参考患者子集反复比较，聚合归一化排名位置，得到队列级生存排名。

### 关键技术细节

1. **小队列构建（Mini-cohort Construction）**
   - 训练时从同癌种训练集中采样规模为 \( m_k \) 的患者子集（论文中 \( m_k = 2 \) 或 \( 3 \)），每个患者仅出现一次；
   - 推理时为每个评估患者 \( p_j^* \) 构建 \( K \) 个评估小队列：将 \( p_j^* \) 与从癌种特定参考库 \( \psi \) 中采样得到的参考患者子集 \( R_{j,l} \) 组合。

2. **训练：GRPO + 一致性对齐奖励**
   - 采用 Group Relative Policy Optimization（GRPO）优化策略，奖励分为两种：
     - **Pair 正确性奖励（\( |C| = 2 \)）**：仅对可比对构造，若模型预测的排序方向与真实生存长短一致则奖励为 1，否则为 0：
       \[
       R_2(C, \hat{y}) = \mathbb{1}[t_i < t_j]\cdot\mathbb{1}[p_j \succ_{\hat{y}} p_i] + \mathbb{1}[t_j < t_i]\cdot\mathbb{1}[p_i \succ_{\hat{y}} p_j]
       \]
     - **Triplet 一致性奖励（\( |C| \ge 3 \)）**：在所有可比有向关系 \( V(C) \) 上计算排序一致率的平均值：
       \[
       R_3(C, \hat{y}) = \frac{1}{|V(C)|}\sum_{(p_u, p_v) \in V(C)} \mathbb{1}[p_u \succ_{\hat{y}} p_v]
       \]
       其中 \( V(C) = \{(p_u, p_v) : u \neq v,\ t_v < t_u,\ \delta_v = 1\} \)。
   - 该奖励设计允许删失患者为训练提供有效排序监督，无需将其随访时间当作精确死亡时间。

3. **推理：蒙特卡洛参考聚合（MCRA）**
   - 对每个评估患者 \( p_j^* \)，在 \( K \) 个小队列中计算其在每个队列中的归一化排名位置 \( s(p_j^*; C_{j,l}^*, \hat{y}_{j,l}^*) \)，然后取平均得到参考相对生存得分：
     \[
     S(p_j^*) = \frac{1}{K}\sum_{l=1}^{K} s(p_j^*; C_{j,l}^*, \hat{y}_{j,l}^*)
     \]
   - 当 \( K \) 增大时，\( S(p_j^*) \) 收敛到该患者相对参考库的预期位置（即预测比该患者存活时间更短的参考患者比例），据此对全体评估患者进行排序。
   - MCRA 避免直接比较评估患者之间，且计算量与评估队列规模无关，支持小编队和顺序到达场景。

## 3. 实验设计

### 数据集与基准

- 论文构建了 **TCGA-SurvReport** 基准，覆盖六个 TCGA 癌症队列：**BLCA、BRCA、COADREAD、HNSC、LUAD、STAD**。
- 每例患者包含三部分信息：
  - TCGA-Reports 中 OCR 提取的病理文本；
  - 临床记录；
  - 分子图谱数据。
- 使用 Qwen2.5-72B-Instruct 配合固定的提取式提示对原始文本进行清洗和标准化，生成统一患者报告；观察时间和生命状态单独作为生存标签，不输入模型。
- 最终保留同时拥有有效统一报告和生存记录的患者，队列规模为 358–1032 例不等，删失比例超过 60%。

### 对比方法

- **WSI/多模态方法**：TransMIL、TITAN、PAMoE、DPSurv、MCAT、MOTCat、SurvPath、PS3、CIMA、SlotSPE（涵盖 WSI-only、WSI+分子、WSI+报告+分子等设置）；
- **报告中心方法**：Tabular-CoxPH、Tabular-DeepHit、PubMedBERT-DeepSurv、BioMistral-CoxPH；
- **LLM 时间回归基线**：Qwen2.5-7B/72B-Instruct 零样本时间预测、Qwen2.5-7B-Instruct 监督微调时间预测。

### 评估协议

- 所有方法使用**相同的患者队列与相同的患者级五折交叉验证划分**；
- 性能指标为 C-index，报告五次折叠的平均值 ± 标准差；
- CACSurv 使用 Qwen2.5-7B-Instruct 作为骨干，每折采样 1,000 对和 1,000 三元组训练一个 epoch；推理时 \( K = 40 \)。

## 4. 资源与算力

- **论文未明确说明 GPU 型号、数量及具体训练时长**。
- 可参考的信息包括：使用 Qwen2.5-7B-Instruct 作为骨干模型；每折训练 2,000 个小队列（1,000 对 + 1,000 三元组），训练一个 epoch；推理时每例患者调用模型 40 次。
- 整体计算成本适中，但精确的硬件资源和总训练时间未在文中披露。

## 5. 实验数量与充分性

### 实验组数

论文共报告了四组核心实验：

1. **主实验**：在 6 个癌症队列上对比 10 种 WSI/多模态模型 + 7 种报告中心/LLM 基线，共 17 个对比方法；
2. **消融实验一（表 2）**：验证 pair 与 triplet 小队列在训练和推理阶段的不同组合（4 种设置）；
3. **消融实验二（表 3）**：对比 Rank-SFT 与一致性对齐奖励优化 / GRPO（4 个代表性队列 + 6 队列平均）；
4. **敏感性分析（表 4）**：评估推理时参考比较次数 \( K \) 从 10 到 80 的性能变化（4 种取值 × 3 种训练/推理设置）。

### 充分性与公平性评价

- **充分性较高**：主实验覆盖了从传统统计模型、深度生存模型到多模态模型和 LLM 基线的广泛方法；消融实验针对方法核心组件（pair/triplet 设计、奖励优化、参考数量）进行了逐项验证。
- **公平性较好**：所有方法在同一数据集、同一患者划分、同一评估协议下重新运行；使用相同提取特征（CONCHv1.5）和相同的五折交叉验证。
- **略微不足**：消融实验表 3 只详细展示了 3 个代表队列，其余队列仅给出平均结果，细节不够透明；对比 LLM 时间回归基线仅使用 Qwen2.5 系列，未覆盖其他主流 LLM。

## 6. 论文的主要结论与发现

- **CACSurv 在所有六个 TCGA 队列上均取得最高 C-index**，平均 C-index 为 **0.722**；
- 相比最强已发表的生存模型 SlotSPE（0.657），提升 **6.5 个百分点**；相比最强 LLM 时间回归基线 Qwen2.5-72B Time-ZS（0.680），提升 **4.2 个百分点**；
- **Pair + Triplet 混合小队列**在训练和推理阶段均优于仅用 pair，说明三元组关系提供互补的排序监督；
- **一致性对齐奖励优化（GRPO）一致优于直接排序微调（Rank-SFT）**，平均 C-index 从 0.682 提升至 0.722；
- 推理参考数 \( K = 40 \) 已接近性能收敛，继续增加至 80 仅提升 0.001；
- 最终的队列级排序可通过在 40 个随机参考小队列上的平均相对位置稳定获得。

## 7. 优点

- **问题洞察深刻**：论文精准指出 LLM 时间回归与生存评估（C-index）及右删失监督之间的两重不匹配，具有较强的理论动机；
- **方法设计一致性高**：将预测公式、监督信号和推理过程全部对齐到一致性（concordance）这一核心目标上，逻辑自洽；
- **删失数据处理巧妙**：通过奖励函数设计，在不将删失患者随访时间误当作精确事件时间的前提下，保留了其有效排序信息；
- **推理方案实用性强**：蒙特卡洛参考聚合使模型支持顺序到达的患者和任意规模的评估队列，且计算量可控；
- **基准构建规范**：TCGA-SurvReport 统一整合了病理、临床和分子信息，并对 OCR 文本进行了标准化清洗，为后续研究提供了公开资源；
- **实验验证扎实**：包含多类基线、消融和敏感性分析，全部在统一协议下重新运行，可信度较高。

## 8. 不足与局限

- **算力信息缺失**：文中未报告 GPU 型号、数量、训练总时长等资源细节，不利于可复现性和成本评估；
- **骨干模型规模有限**：仅使用 Qwen2.5-7B 一个规模，未探索更大或不同系列模型对性能的影响；
- **对报告质量的依赖**：TCGA-SurvReport 的构建依赖 OCR 文本和 Qwen2.5-72B 的提取式清洗，OCR 噪声和清洗过程中的信息损失可能影响下游性能，论文对这类风险的量化分析不足；
- **消融报告不完整**：表 3 仅展示 3 个代表性队列的详细结果，其余队列只给出均值，消融全面性有所折扣；
- **泛化性不确定**：所有实验均在 TCGA 队列上进行，未涵盖外部数据集、真实世界临床报告或不同医疗机构的报告格式，外部有效性有待验证；
- **推理开销**：每例评估患者需调用模型 40 次（\( K = 40 \)），在更大部署场景中的计算成本可能成为实际应用的瓶颈；
- **比较推理的边界**：模型仅在有限大小的小队列内比较，极端情况下可能出现跨小队列不一致（如偏好非传递性），MCRA 通过平均缓解但仍非理论保证。

（完）
