---
title: Generation-Powered Inference for Distribution-Valued Outcomes
title_zh: 生成式驱动的分布值结果推断
authors: "Yijiao Zhang, Hongzhe Li"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.14542v1"
tags: ["query:med-ai"]
score: 8.0
evidence: 利用生成模型对分布值结果进行统计推断，并以单细胞基因组中预测细胞对扰动响应为实例，符合虚拟细胞模型主题
tldr: 生成模型常产生分布值输出，但其预测未必准确。针对分布值参数的推断，提出生成加权推断(GPI)，通过函数值桥表示将Wasserstein空间推断转化为希尔伯特空间均值函数估计，并引入增强估计。GPI估计量具有一致性和渐近正态性，模拟表明其效率优于仅标注数据，且对生成模型误设有稳健性。在K562 Perturb-seq研究中，利用State模型合成响应改进了通路级表达分布推断。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.14542v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1679, \"height\": 916, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14542v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1510, \"height\": 929, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14542v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1674, \"height\": 855, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14542v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1693, \"height\": 976, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14542v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1664, \"height\": 921, \"label\": \"Figure\"}]"
motivation: 生成模型预测可能不准确，但可提供辅助信息，需要一种不依赖其正确性的推断框架。
method: 通过函数值桥将Wasserstein空间推断转化为均值函数估计，建立预测加权式增强估计，并推导最优信息借用。
result: GPI估计量一致且渐近正态，模拟显示效率比仅标注数据更高，对模型误设稳健；Perturb-seq中提升了推断。
conclusion: 为分布值结果提供通用、高效的推断框架，拓展生成模型的应用。
---

## 摘要
现代生成模型越来越多地产生分布值输出，例如在单细胞基因组学中预测细胞对基因扰动的反应。尽管这些模型提供了有价值的辅助信息，但它们天生不完美，因此需要统计方法能够利用其预测而不依赖其正确性。我们提出生成式驱动推断（GPI），一个利用辅助生成模型改进分布值参数推断的通用框架。聚焦于Wasserstein重心和相关分布泛函，我们引入函数值桥接表示，将非线性Wasserstein空间中的推断转化为希尔伯特空间中均值函数的估计，从而实现了类似于预测驱动推断的增强估计框架。我们开发了一系列具有最优信息借用的GPI估计器，建立了相合性、渐近正态性和同时置信带，并为线性泛函和Wasserstein距离导出了有效推断。模拟研究表明，与仅使用标注数据的方法相比，该方法在效率上有提升，并在生成模型误设定下表现出稳健性能。我们使用K562细胞的Perturb-seq研究来展示所提出的框架，其中由State基础模型生成的合成扰动响应被用于改进与40S核糖体模块扰动相关的通路级共识基因表达分布的推断。

## Abstract
Modern generative models increasingly produce distribution-valued outputs, such as predicted cellular responses to genetic perturbations in single-cell genomics. While these models provide valuable auxiliary information, they are inherently imperfect, creating a need for statistical methods that leverage their predictions without relying on their correctness. We propose generation-powered inference (GPI), a general framework for improving inference on distribution-valued parameters using auxiliary generative models. Focusing on Wasserstein barycenters and related distributional functionals, we introduce a function-valued bridge representation that transforms inference in the nonlinear Wasserstein space into estimation of a mean function in a Hilbert space, enabling an augmented estimation framework analogous to prediction-powered inference. We develop a family of GPI estimators with optimal information borrowing, establish consistency, asymptotic normality, and simultaneous confidence bands, and derive valid inference for linear functionals and Wasserstein distances. Simulation studies demonstrate efficiency gains over labeled-data-only methods and robust performance under generative model misspecification. We illustrate the proposed framework using a Perturb-seq study of K562 cells, where synthetic perturbation responses generated by the State foundation model are used to improve inference for pathway-level consensus gene expression distributions associated with perturbations of the 40S ribosome module.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：现代生成模型（如单细胞基因组学中的基础模型 State、大语言模型、气候模型、数字孪生等）经常输出**分布值结果**，而不仅仅是标量或向量预测。例如在 Perturb-seq 中，生成模型可预测特定基因扰动下细胞状态的表达分布。然而这些模型“生而不完美”，可能存在系统性偏差或无法泛化。因此核心问题是：**如何在生成模型不准确的情况下，利用其输出的分布信息来提高统计推断效率，同时保证推断的有效性？**
- **与现有工作的关系**：现有预测驱动推断（Prediction-Powered Inference, PPI）方法能够用机器学习预测增强有限标注数据的统计推断，但主要面向有限维欧氏参数（如均值、回归系数），无法直接处理分布值参数。
- **本文目标**：提出**生成驱动推断（Generation-Powered Inference, GPI）**框架，将 PPI 思想扩展到非线性 Wasserstein 空间，为分布值结果提供通用的、对生成模型误设稳健的推断方法。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：引入一个**函数值桥接参数（bridge parameter）**，将 Wasserstein 空间中的非线性推断问题转化为 Hilbert 空间中的均值函数估计问题。
  - 对 Wasserstein 重心分布 μ'，定义桥接参数：  
    θλ(·) = μ'⁻¹ ∘ λ(·)，其中 λ 是连续的参考分布函数。
  - 该表示的关键性质（Lemma 1）：θλ(·) = E[Y⁻¹ ∘ λ(·)]，从而将分布值重心的推断简化为普通均值函数推断。
- **信息增强框架**：类似 PPI，将观测数据和生成数据结合：
  - 定义变换后的观测 Zλ = Y⁻¹∘λ，变换后的生成代理 gλ(X) = G(X)⁻¹∘λ。
  - 引入有界线性算子 Γ 控制信息借用，构造分布级整流器（rectifier）R_Γ = E[Zλ − Γgλ(X)]。
  - 基本 GPI 估计量：  
    θ̂λ,Γ = (1/N)∑_{j∈U} Γĝλ(X_j) + (1/n)∑_{i∈O} [Ŷ_i⁻¹∘λ − Γĝλ(X_i)]
  - 在生成模型需要从标注数据训练时，提出**交叉拟合（cross-fitted）GPI**：将标注数据分为 K 折，每折用其他折训练生成模型，再用本折计算整流项，最终加权平均。
- **最优信息借用**：通过最小化渐近协方差算子，得到最优算子 Γ* 满足方程：  
  (1+ρ)Γ*C_gg = C_Zg  
  其中 ρ = n/N，C_gg 为生成代理的协方差核，C_Zg 为观测与代理的交叉协方差核。Γ* 本质上是一个“收缩的函数回归算子”，可对每个方向进行不同缩放和符号校准。
- **可行估计**：使用函数主成分分析（FPCA）估计协方差核和 Γ*，并加入岭参数 κ 以稳定估计。
- **推断方法**：
  - 对桥参数本身：构造基于高斯过程的**同时置信带（SCB）**。
  - 对线性泛函（如分位数区间均值、位移泛函）：构造置信区间。
  - 对 Wasserstein 距离：推导其渐近分布，并提供与对照组比较的假设检验。

## 3. 实验设计：使用数据集、场景、benchmark 与对比方法

- **模拟实验**：
  - 构造分布值结果：以偏态正态分布的变换作为重心，用三个正交函数（位置、尺度、形状）加协变量依赖的随机扰动生成个体分布。
  - 生成模型代理：设计“有偏且平坦化”的代理重心，并通过潜系数的对齐方向（同号/异号）和对齐强度（强/中/弱）控制生成模型与真实条件分布的关系。
  - 四种对齐模式：+++、+-+、-+-、---（表示三个方向上的符号组合），三种强度：强、中、弱。
  - 对比方法：Labeled（仅标注数据）、Naive GPI（Γ = (1+ρ)⁻¹I）、Scalar GPI（Γ = γI，标量调优）、General GPI（本文提出的算子调优）。
  - 评估指标：估计 RMISE、SCB 覆盖率与带长、Wasserstein 检验的拒绝率、线性位移泛函的 CI 覆盖与长度等。
- **真实数据应用**：
  - 使用 **K562 Perturb-seq 数据集**（Replogle et al., 2022），关注 40S 核糖体（40Sr）通路模块，共 74 个扰动。
  - 生成模型：**State 基础模型**（在非 K562 细胞上预训练和微调，K562 扰动被排除，以避免泄漏）。
  - 将 21 个有超过 200 个细胞的扰动视为标注数据，其余 53 个视为未标注数据（其真实数据仅用于基准评估）。
  - Benchmark：
    - **Full observed**：使用全部 74 个扰动的真实观测数据计算的经验重心（作为“金标准”）。
    - **Pooled**：将 21 个真实分布与 State 生成的 53 个代理分布直接合并。
  - 对比方法：Labeled、Naive GPI、Scalar GPI、General GPI，也在真实数据上进行评估。
  - 主要目标：基因的位移函数 μ'⁻¹(u) − μ0⁻¹(u)，以及均值、中心区、上尾函数量，Wasserstein 距离等。
  - 额外分析：基于 General GPI 估计的位移曲线对基因进行聚类，并进行 GO 富集分析，探索生物学意义。

## 4. 资源与算力

- **论文未明确提及使用的算力资源**（如 GPU 型号、数量、训练时长等）。
- 仅提到使用预训练的 State 模型进行推理，以及模拟实验和真实数据分析都使用了大量重复抽样（如 1000 次模拟、1000 次 bootstrap），但并未给出具体的计算环境说明。
- 因此，**这一信息在论文中缺席**，无法从文本中总结具体算力需求。

## 5. 实验数量与充分性

- **模拟实验数量**：
  - 3 种对齐强度 × 4 种对齐方向 = 12 种代理设置，每种设置 1000 次重复，规模较大。
  - 同时对三种不同样本量组合（包括 n=20 和更大/更小样本）进行了扩展模拟（见补充材料）。
  - 评估了多种目标：重心分位函数、线性位移泛函、Wasserstein 检验，以及不同未标注样本量 N 的影响。
- **真实数据实验**：
  - 对 1980 个响应基因评估 GPI 校准效果，并展示 4 个代表性基因的详细结果。
  - 对基因位移曲线进行聚类，并进行 GO 生物学过程和细胞组分的富集分析。
  - 此外有补充材料中的更多基因级结果（图 S12、S17 等）。
- **充分性评估**：
  - **优点**：实验覆盖了生成模型不同程度和方向的误设，对比了多种 GPI 变体，验证了方法在估计精度、推断覆盖和检验功效方面的表现；真实数据验证非常充分，将方法应用于开放数据并给出生物学解释。
  - **局限性**：模拟中假设条件生成模型 G(X) 已知（可以直接计算分位数），未评估生成模型输出本身需要从有限样本估计时的效应（虽然理论中讨论了，但模拟部分没有完全模拟这一过程）；真实数据中“Full observed”仍然只是经验重心，并非真正的金标准，因此 benchmark 本身也有误差。

## 6. 论文的主要结论与发现

- **理论层面**：
  - GPI 估计量具有一致性和渐近正态性，可以构建同时置信带。
  - 最优算子 Γ* 在协方差算子意义上是最优的，能够使所有线性泛函和 Wasserstein 距离的推断效率不差于仅标注数据方法。
  - 即使生成代理完全无信息，GPI 也不会比仅标注数据差；若代理完美线性可预测，可获得最大效率增益。
- **模拟层面**：
  - General GPI 在各种对齐方向和强度下均表现出稳健性，RMISE 更低、置信带更短，同时维持名义覆盖水平。
  - Naive GPI（不调优的 I 算子）在负对齐或混合对齐时可能比仅标注数据更差，说明调优算子的必要性。
  - Scalar GPI（标量调优）在均匀对齐时效果接近 General GPI，但在混合对齐时不如 General GPI。
- **真实数据层面**：
  - 基于 General GPI 的位移估计比直接 Pooled 更接近 Full observed 基准，且置信区间更合理。
  - 聚类分析显示位移曲线具有异质性，富集的 GO 功能与已知细胞过程一致（如有丝分裂、翻译、DNA 复制起始等），说明分布值推断能揭示均值效应掩盖的定量异质性。

## 7. 优点

- **方法创新性**：
  - 提出函数值桥表示，巧妙地将非线性 Wasserstein 空间问题线性化到 Hilbert 空间，使得 PPI 的增强框架可以直接推广。
  - 算子 Γ 的引入允许对生成信息进行方向相关的校准，比单纯标量缩放更灵活，理论上的最优性证明完整。
  - 考虑了交叉拟合的实际场景，生成模型本身可以从标注数据训练，方法适用性强。
- **理论完备性**：
  - 给出了固定算子下的高斯过程极限、最优算子刻画、效率提升公式，以及推断方法的理论保证（SCB、区间、Wasserstein 检验）。
- **实验丰富性**：
  - 模拟设计覆盖多种符号组合与强度，充分评估了方法对生成模型误设的稳健性。
  - 真实数据应用使用了最新的基础模型，并提供了基因水平、通路水平的生物学解读，具有实际意义。

## 8. 不足与局限

- **对生成模型估计误差的模拟不足**：
  - 理论中考虑了生成模型从有限样本估计的误差（Assumption 2），但模拟实验中假设 G(X) 已知，未模拟通过训练生成模型所引入的额外不确定性；真实数据中 State 模型是预训练好的，也没有展示模型不确定性对结果的影响。
- **基准“金标准”不完美**：
  - Full observed 虽然是基于全部观测数据，但本身也存在采样误差；而且仅保留了 74 个扰动中 21 个有足够细胞的扰动作为标注，其余 53 个分布本身稀疏，Full observed 也存在较大不确定性。
- **应用范围限于一维分布**：
  - 方法基于一维 Wasserstein 空间的量化函数表示，对于多维分布（如多元基因表达联合分布）无法直接适用；论文并未讨论高维扩展。
- **未讨论计算效率和可扩展性**：
  - 没有给出算法复杂度、大规模数据下的计算瓶颈或优化策略。
- **设置选择依赖用户**：
  - 参考分布 λ 的选择对桥参数有一定影响，论文给出了例子但未系统讨论如何在实际中选取 λ 以优化特定推断目标。
- **对生成模型的假设较强**：
  - 有效性依赖于生成模型输出的分位数变换满足光滑性和平方可积性，以及交叉拟合中模型的稳定性；在极端生成模型崩溃场景下可能失效。

（完）
