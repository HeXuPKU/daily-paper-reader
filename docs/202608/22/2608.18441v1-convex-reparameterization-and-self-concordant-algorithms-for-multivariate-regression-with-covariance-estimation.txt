Title: Convex Reparameterization and Self-Concordant Algorithms for Multivariate Regression with Covariance Estimation

URL Source: https://arxiv.org/pdf/2608.18441v1

Published Time: Thu, 20 Aug 2026 00:24:43 GMT

Number of Pages: 27

Markdown Content:
# Convex Reparameterization and Self-Concordant Algorithms for Multivariate Regression with Covariance Estimation 

Hongru Zhao 

School of Statistics, University of Minnesota Twin Cities 

zhao1118@umn.edu 

Huiqian Feng 

Department of Applied Mathematics & Statistics, Stony Brook University 

huiqian.feng@stonybrook.edu 

August 20, 2026 

Abstract 

Building on the reparameterization of Zhu (2020) for multivariate linear regression, which yields a jointly convex penalized likelihood in the reparameterized regression coefficient matrix and the precision matrix, we show that the resulting scaled Gaussian loss is standard self-concordant. This places the penalized likelihood problem of jointly estimating the precision matrix and the reparameterized regression coefficient matrix within composite self-concordant optimization (Tran-Dinh et al., 2015) and yields two algorithms: a proximal gradient method and a damped proximal Newton method. In simulations, we study algorithmic robustness, iterations to convergence, and elapsed time. In a protein expression application, compared with the classical-parameterization formulation, the proposed convex formulation gives similar mean squared prediction error and can be substantially faster when the fitted precision matrix is dense. 

# 1 Introduction 

In many applications, such as gene and protein regulatory networks (Friedman et al., 2008; Yin and Li, 2011; Cai et al., 2013) and econometric systems (Demirer et al., 2018; Barigozzi and Brownlees, 2019), one observes multiple responses whose error dependence structure is scientifically meaningful. Multivariate linear regression simultaneously models these response variables in terms of one or more predictors. In such settings, penalized Gaussian likelihood provides a way to estimate regression effects and the error precision matrix jointly, often under structural constraints such as sparsity. 1arXiv:2608.18441v1 [stat.CO] 19 Aug 2026 Concretely, let xraw  

> i

∈ Rp and yraw  

> i

∈ Rq denote the raw predictors and responses. Consider the multivariate linear regression model with Gaussian errors yraw  

> i

| xraw  

> i

∼ N q

> (

α + ( βraw )⊤xraw  

> i

, Σ),

with intercept α ∈ Rq, slope matrix on the raw scale βraw ∈ Rp×q, and precision matrix Ω = Σ −1.In practice, estimation is carried out on centered (and, if desired, standardized) predictors and centered responses; on this working scale we write xi and yi and fit yi | xi ∼ N q

> (

β⊤xi, Σ), where 

β ∈ Rp×q is the slope matrix on the preprocessed scale. Within the classical parameterization ( β, Ω), this model and related estimators have been studied extensively (see, e.g., Rothman et al., 2010; Cai et al., 2013; Yin and Li, 2011; Rai et al., 2012; Lee and Liu, 2012; Chen and Banerjee, 2017). Several estimation strategies are available. One common two-stage strategy estimates β first and then estimates a sparse precision matrix from the residuals, for example by the graphical lasso or constrained ℓ1 minimization for inverse matrix estimation (CLIME) (Friedman et al., 2008; Cai et al., 2011). Conditional-likelihood and conditional Gaussian graphical model methods instead model each response given the predictors and the remaining responses (Yin and Li, 2011; Lee and Liu, 2012; Wang, 2015; Wytock and Kolter, 2013; Sohn and Kim, 2012). Related extensions include multiresponse models that leverage output or task structure (Rai et al., 2012; Chen and Banerjee, 2017), multivariate linear mixed effects models (Navon and Rosset, 2020), and robust multivariate lasso regression with covariance estimation (Chang and Welsh, 2023). Other approaches impose more specific response-dependence structure, such as latent factors (Zhou et al., 2017; Chan et al., 2025) or dense equicorrelation/compound-symmetry covariance structure for strongly associated responses (Ham et al., 2025). A central joint likelihood benchmark is the multivariate regression with covariance estimation (MRCE) method of Rothman et al. (2010), which estimates ( β, Ω) jointly with ℓ1 penalties on the regression coefficients and the off-diagonal precision entries. In the classical parameterization ( β, Ω), however, the joint objective is generally nonconvex, so local optimization methods can converge to different stationary points under different initial values. To address this nonconvexity, following Zhu (2020) we reparameterize the model through the Gaussian natural parameter γ = βΩ and optimize over ( γ, Ω), for which the negative log-likelihood is jointly convex. This transformation also has a natural interpretation from the viewpoint of conditional Gaussian graphical models: for each response coordinate, the column γ·j describes predictor–response links after adjusting for the remaining responses, whereas β = γΩ−1 governs 2the conditional mean and can combine direct predictor effects with effects transmitted through the response dependencies. As a result, sparsity in γ targets conditional predictor–response associations rather than mean regression coefficients alone; we return to this point in Subsection 2.2. We therefore refer to γ as the predictor–response link parameter throughout the paper. On the preprocessed training data this leads to the following composite convex optimization problem: (ˆ γ, ˆΩ) ∈ arg min   

> γ∈Rp×q,Ω∈Sq
> ++
> {

L(γ, Ω) + λγ ∥γ∥1 + λΩ∥Ω∥1,off 

> }

, (1.1) where Sq 

> ++

denotes the cone of q × q symmetric positive-definite matrices, and L(γ, Ω) is the scaled reparameterized Gaussian negative log-likelihood, written explicitly in Section 2. For any matrix 

A = ( aij ), we write ∥A∥max := max i,j |aij |, ∥A∥max ,off := max i̸=j |aij |, ∥A∥1,off := ∑ 

> i̸=j

|aij |. Once (ˆ γ, ˆΩ) is obtained, Lemma 2.1 recovers the slope and intercept on the raw scale. Although the reparameterization yields joint convexity, the geometry of the smooth loss is not governed by a fixed Euclidean geometry: because the likelihood involves Ω −1, its gradient is not globally Lipschitz in a fixed Euclidean norm. As a result, classical fixed-step analyses and step-size choices based on 

L-smoothness are not directly applicable. To handle this geometry, we use the second and third derivatives of the loss. We prove that the scaled loss L is standard self-concordant. This allows us to use composite self-concordant theory (Tran-Dinh et al., 2015) for a proximal gradient method with a local curvature correction and a damped proximal Newton method with the usual Newton-decrement step length. We refer to the resulting estimator as SCA-MRCE (Self-Concordant Algorithms for Multivariate Regression with Covariance Estimation). In the numerical studies, the proximal gradient variants are more stable and faster along a penalty path, while proximal Newton is more attractive when a more accurate solution is needed. We also study aggressive variants that reduce inner computational work and elapsed time, but they do not have the same theoretical convergence guarantees. 

1.1 A one-dimensional motivating example 

To illustrate the nonconvex optimization landscape in the classical mean and precision parame-terization, consider a simple one-dimensional Gaussian setting. Let ¯ x ∈ R and S ≥ 0 denote the sample mean and empirical variance. For a mean parameter μ ∈ R and a precision parameter Ω > 0, 3consider the penalized negative log-likelihood: 

− log Ω + S Ω + Ω ( μ − ¯x)2 + λ1 |μ| + λ2 Ω, (μ, Ω) ∈ R × (0 , ∞). (1.2) For suitable choices of (¯ x, S, λ 1, λ 2), this objective exhibits multiple local minima, as illustrated in Figure 1. This phenomenon persists in higher dimensions when working in ( β, Ω), whereas the natural parameter reparameterization ( γ, Ω) leads to a globally convex landscape.  

> (a) Global landscape of the objective. (b) Zoom near a local minimum.

Figure 1: Penalized univariate Gaussian example with ¯ x = 0 .5, S = 0 .01, λ1 = 2 .8, and λ2 = 0 .10. The classical ( μ, Ω) parameterization yields multiple local minima. 

1.2 Contributions and outline 

Our main contributions are as follows: • Self-concordant geometry: We show that the scaled convex Gaussian loss L in the ( γ, Ω) parameterization is standard self-concordant. This places the likelihood within the composite self-concordant theory (Tran-Dinh et al., 2015) and provides the local norm control used in the convergence analysis. • Globally convergent iterative algorithms: We develop self-concordant first- and second-order methods for composite penalized estimation. The proximal gradient scheme uses a local curvature correction from composite self-concordant theory, while the proximal Newton scheme uses the Newton decrement to obtain a damped step with global convergence and local quadratic convergence. 4• Empirical evaluation: The experiments compare predictive performance, algorithmic robust-ness, outer iteration counts, and path computation time across legacy and aggressive solver variants. The aggressive variants truncate expensive inner updates and reduce computation time, but they do not have the same convergence guarantees. The proximal gradient methods are the most stable and fastest family for path fitting, while proximal Newton uses many fewer outer iterations and remains useful when a more accurate solve is worth the additional inner work. 

Organization. The rest of the paper is organized as follows. Section 2 formulates the working scale and raw-scale models, records the derivatives and local geometry, and establishes self-concordance. Section 3 presents the proximal gradient and proximal Newton algorithms, the alternating direction method of multipliers (ADMM) subproblem solver, the penalty path construction, and the raw-scale recovery and evaluation metrics used later in the paper. Section 4 reports the simulation study. Section 5 presents the real data analysis on the mice protein expression data. Section 6 concludes, and the appendices collect proofs and implementation details. 

# 2 Model Formulation and Local Geometry 

Section 1 introduced the multivariate linear regression model with Gaussian errors and the penalized estimation problem. This section first records the predictor preprocessing and intercept recovery linking the raw and working scales, then explains the statistical interpretation of the reparameterized loss, and finally develops the self-concordant geometry used by the first- and second-order algorithms in Section 3. 

2.1 Predictor preprocessing (centering/standardization) and intercept recovery 

We distinguish the raw data scale from the working scale used for estimation. On the raw scale, the conditional mean model is E(yraw  

> i

| xraw  

> i

) = α + ( βraw )⊤xraw  

> i

. Let Xraw = [ xraw 1 , · · · , x raw  

> n

]⊤ ∈

Rn×p, Y raw = [ yraw 1 , · · · , y raw  

> n

]⊤ ∈ Rn×q, and let ¯ x = 1 

> n

1⊤ 

> n

Xraw ∈ R1×p, ¯y = 1 

> n

1⊤ 

> n

Y raw ∈ R1×q. Here and below, 1m denotes the length-m vector of ones. For centering only, we take Dx = Ip, where 

Ip is the p × p identity matrix. For predictor standardization, we take Dx = diag( sx, 1, . . . , s x,p ),

where sx,j is the sample standard deviation in the training data of predictor j. We then define 5X := (Xraw − 1n ¯x)D−1 

> x

and Y := Y raw − 1n ¯y. We continue to write xi and yi for the corresponding observations on the working scale. Thus the training matrix after predictor preprocessing X is obtained by centering and, if desired, standardizing the raw predictors. On this working scale we fit the model without an intercept yi | xi ∼ N q

(β⊤xi, Σ), where 

β ∈ Rp×q is the slope matrix on the preprocessed scale. The corresponding slope on the raw scale is 

βraw = D−1 

> x

β. 

Lemma 2.1 formalizes the equivalence between fitting on the raw scale with an unpenalized intercept and fitting on the working scale without an explicit intercept. 

Lemma 2.1 (Intercept recovery) . With Dx, X, and Y defined above, consider the Gaussian loss on the raw scale 

Lraw (α, β raw , Ω) := − log det(Ω) + 1

n

> n

∑

> i=1

(yraw  

> i

− α − (βraw )⊤xraw 

> i

)⊤Ω(yraw  

> i

− α − (βraw )⊤xraw 

> i

), Ω ≻ 0.

For every fixed (βraw , Ω) , the unique minimizer in α is α⋆(βraw ) = (¯ y − ¯x β raw )⊤. If β = Dxβraw , then 

Lraw 

(α⋆(βraw ), β raw , Ω) = Lprep (β, Ω) , where Lprep (β, Ω) := − log det(Ω)+ 1

> n

∑ni=1 

(yi−β⊤xi

)⊤Ω(yi−

β⊤xi

). Therefore the problem on the raw scale with an unpenalized intercept is equivalent, after this preprocessing, to the corresponding problem on (X, Y ) with no explicit intercept. 

Accordingly, throughout the remainder of the paper, X denotes the preprocessed training design matrix, Y denotes the centered training responses, and the optimization variables contain no explicit intercept. Raw-scale coefficients are recovered after fitting by Lemma 2.1. An elementary proof is given in Appendix A. 

2.2 Reparameterized Gaussian loss 

For the training matrix after predictor preprocessing X and the centered response matrix Y , let the empirical second moment matrices be ̂ ΣXX := 1 

> n

X⊤X, ̂ ΣY Y := 1 

> n

Y ⊤Y, ̂ ΣY X := 1 

> n

Y ⊤X, ̂ ΣXY := ̂

Σ⊤ 

> Y X

= 1 

> n

X⊤Y. Recall the natural parameter reparameterization γ = βΩ for the slope matrix β

associated with the preprocessed predictor matrix X. Writing ξ := ( γ, Ω), the reparameterized 6averaged Gaussian negative log-likelihood is 

> ˜

L(ξ) = 12

> {

− log det(Ω) + tr (Ω−1γ⊤̂ ΣXX γ) − 2 tr (̂ Σ⊤ 

> XY

γ) + tr (Ω̂ ΣY Y 

> )}

, dom( ˜L) = Rp×q × Sq

> ++

.

For algorithmic development we will work with the scaled loss 

L(ξ) := 2 ˜L(ξ) = − log det(Ω) + tr (Ω−1γ⊤̂ ΣXX γ) − 2 tr (̂ Σ⊤ 

> XY

γ) + tr (Ω̂ ΣY Y 

> )

, dom( L) = dom( ˜L).

This scaling is convenient because it makes L a standard self-concordant function (Lemma 2.3). It is trivial to verify that Lprep in Lemma 2.1 is identical to L.Beyond yielding joint convexity, the reparameterization γ = βΩ also has a natural statistical interpretation. For each response coordinate j, standard Gaussian conditioning gives 

Yj | X = x, Y −j = y−j ∼ N  

> (

x⊤γ·j − Ωj, −j y−j

Ωjj 

, Ω−1

> jj
> )

, (2.1) where γ·j is the j-th column of γ and Ω j, −j is the j-th row of Ω excluding its diagonal entry Ω jj .Hence γ·j /Ωjj is the coefficient of X in the conditional regression of Yj on ( X, Y −j ); it measures predictor–response association after adjusting for the remaining responses. Since Ω jj > 0, this conditional coefficient has the same support as γ·j , so an ℓ1 penalty on γ targets sparsity in these conditional predictor–response links. By contrast, β = γΩ−1 determines the conditional mean 

E(Y | X = x) = β⊤x, and β·j = ∑qk=1 γ·k(Ω −1)kj , so the mean coefficient for response j can combine conditional links through the response network; in particular, β·j may be nonzero even when γ·j = 0. This interpretation is statistical rather than causal and is standard in the conditional Gaussian graphical model (CGGM) literature (Zhu, 2020; Chiquet et al., 2017; Sohn and Kim, 2012). Meanwhile, penalizing the off-diagonal entries of Ω targets sparsity in the response–response conditional dependence network (Wang, 2015; Wytock and Kolter, 2013; Gan et al., 2022). We next record the gradient, Hessian, and local norms needed for the optimization analysis of the preprocessed objective. 72.3 Derivatives, Hessian, and local norms 

We work on the product space (ambient space) Ξ := Rp×q × Sq, with generic increment dξ =(dγ, d Ω) ∈ Ξ, where Sq denotes the space of symmetric q × q matrices. This space is equipped with the Frobenius inner product ⟨dξ 1, dξ 2⟩ := tr( dγ ⊤ 

> 1

dγ 2) + tr( dΩ⊤ 

> 1

dΩ2). When convenient, we identify 

ξ = ( γ, Ω) with the stacked matrix ξ = ( γ⊤ Ω⊤)⊤ ∈ R(p+q)×q (and similarly for increments dξ ). The gradients of L with respect to γ and Ω are ∇γ L(ξ) = − 2̂ ΣXY + 2 ̂ΣXX γ Ω−1, ∇Ω L(ξ) = ̂

ΣY Y − Ω−1 − Ω−1γ⊤̂ ΣXX γ Ω−1. We write ∇L (ξ) := (∇γ L(ξ), ∇ΩL(ξ)) ∈ Ξ. For any increment dξ = (dγ, d Ω) ∈ Ξ, the Hessian quadratic form is ∇2L[ξ]( dξ, dξ ) =tr (dξ ⊤H(ξ) dξ Ω−1). The block matrix H(ξ) ∈ R(p+q)×(p+q) is 

H(ξ) = 

> 

2̂ΣXX − 2̂ΣXX γ Ω−1

−(2̂ ΣXX γ Ω−1)⊤ Ω−1 + 2 Ω −1 γ⊤̂ ΣXX γ Ω−1 

> 

.

Equivalently, ∇2L[ξ] can be viewed as a self-adjoint linear map on Ξ through ∇2L[ξ]( dξ ) = 

> (

H(ξ)dξ Ω−1)

> Ξ

, with the quadratic form given by ⟨dξ, ∇2L[ξ]( dξ )⟩.The Hessian induces the local (semi-)norm at ξ,

∥dξ ∥ξ := 

> √

∇2L[ξ]( dξ, dξ ) = 

> √

⟨dξ, H (ξ)dξ Ω−1⟩.

Because ̂ ΣXX may be singular in high dimensions, ∥ · ∥ ξ is in general only a seminorm. 

2.4 Self-concordance 

Self-concordance replaces global Lipschitz gradient assumptions with a local third derivative in-equality that is well suited to objectives involving log det(Ω) and Ω −1.

Definition 2.2 (Self-concordant function) . Let V be a finite-dimensional real vector space and let C ⊆ V be open and convex. A three times continuously differentiable function f : C → R is called M -self-concordant for some constant M > 0 if, for every x ∈ C and every direction h ∈ V ,the one-dimensional restriction ϕ(t) := f (x + th ), defined for all t such that x + th ∈ C, satisfies 

> ∣∣

ϕ′′′ (0) ∣∣ ≤ M (ϕ′′ (0) )3/2. The special case M = 2 is often called a standard self-concordant function. 8Lemma 2.3 (Self-concordance of the reparameterized Gaussian loss) . The scaled loss L is standard self-concordant. 

The proof is given in Appendix B. With standard self-concordance in place for the scaled objective, we can invoke classical composite self-concordant theory (Tran-Dinh et al., 2015) to justify damped proximal Newton updates with guaranteed global convergence and fast local rates. Lemma 2.3 also changes the computational regime for computing the penalized estimator. Statistically, the curvature of the Gaussian likelihood is governed by the current precision structure and is highly nonuniform on the positive-definite cone, especially near the boundary where the log det term acts as a barrier. Thus a single global Lipschitz constant is typically too crude to reflect the local geometry induced by Ω, whereas self-concordance provides a model-intrinsic local metric. Computationally, step-size selection can therefore use local geometric quantities rather than repeated objective evaluations. For proximal-gradient updates, composite self-concordant theory replaces generic Armijo backtracking with a local curvature correction based on quantities such as the local decrement, thereby avoiding expensive function-evaluation line searches (Tran-Dinh et al., 2015). This distinction is practically important here because evaluating the smooth loss requires computationally demanding O(q3) matrix operations associated with log det(Ω) and Ω −1.The implications for second-order methods are even more substantial. Self-concordance yields a principled damped proximal Newton step length of the form α = (1 + ν)−1, where ν is the local Newton decrement. As shown in Section 3, this choice guarantees monotone descent and global convergence without a separate globalization or heuristic backtracking procedure. Once the iterates enter the local Newton region, ν → 0, so α → 1 and the method transitions to full Newton steps with quadratic local convergence. We next develop a self-concordant proximal gradient baseline, whose step-size correction uses only local geometry, and a damped proximal Newton method, which makes fuller use of curvature while retaining rigorous global guarantees. 

# 3 Algorithms 

By Lemma 2.1, all algorithmic statements below are written for preprocessed training data. The intercept has already been profiled out, so the optimization variables are ( γ, Ω) only. We solve the 9composite convex problem min 

> ξ∈D δ

F (ξ) := L(ξ) + Φ( ξ), Dδ := {(γ, Ω) ∈ Rp×q × Sq 

> ++

: Ω ⪰ δI q

}, (3.1) with safeguarding constant δ > 0 and separable penalty Φ( γ, Ω) = λγ ∥γ∥1 + λΩ∥Ω∥1,off . The first-and second-order schemes below are specialized from the composite self-concordant framework of Tran-Dinh et al. (2015). Because of the nonsmooth penalty, the algorithms use an outer loop and an inner iterative solver for the penalized subproblem; some steps add a nested loop. We refer to the main updates of the objective parameters, such as the proximal gradient and proximal Newton steps, as the outer iterations. 

3.1 Backtracking proximal gradient method 

The proximal gradient (PG) scheme below is the composite self-concordant first-order method of Tran-Dinh et al. (2015) specialized to F = L + Φ. Given ξ(m), the proximal gradient trial point solves 

s(m) = arg min ξ∈D δ

{

⟨∇L (ξ(m)), ξ − ξ(m)⟩ + L(m)

2 ∥ξ − ξ(m)∥2 

> F

+ Φ( ξ)

}

. (3.2) Solving (3.2) reduces to two proximal operators: the γ block is entrywise soft-thresholding, and the Ω block is a positive-definite proximal update based on soft-thresholding of the off-diagonal entries; see Appendix C.1 and Xue et al. (2012). Writing d(m) := s(m) − ξ(m), we use the composite self-concordant step length 

α(m) = β2(m)

ν(m)

(ν(m) + β2(m)

) , ν(m) := ∥d(m)∥ξ(m) , β(m) := 

√

L(m) ∥d(m)∥F .

10 Algorithm 1: Backtracking proximal gradient for (3.1) 

Input: Preprocessed training data ( X, Y ); penalties λγ , λ Ω; safeguard δ > 0; tolerance 

ε > 0. 

Initialize: Choose ξ(0) ∈ D δ and L(0) > 0.  

> 1

for m = 0 , 1, 2, . . . do  

> 2

Set L(m) ← L(m−1) (with L(−1) := L(0) );  

> 3

while True do  

> 4

Compute s(m) from (3.2) and d(m) = s(m) − ξ(m); 

> 5

Compute ν(m) = ∥d(m)∥ξ(m) and β(m) = √L(m)∥d(m)∥F ; 

> 6

if ν2(m)/β 2(m) + ν(m) > 1 then  

> 7

L(m) ← 2L(m); 

> 8

else  

> 9

break ; 

> 10

Set α(m) ← β2(m)/{ν(m)(ν(m) + β2(m))}; 

> 11

Update ξ(m+1) ← ξ(m) + α(m)d(m); 

> 12

if ∥d(m)∥F ≤ ε then  

> 13

break ;

3.2 Damped proximal Newton method 

The proximal Newton (PN) scheme below is the composite self-concordant second-order method of Tran-Dinh et al. (2015) specialized to F = L + Φ. At iterate ξ(m), the proximal Newton subproblem is 

s(m) = arg min ξ∈D δ

{

⟨∇L (ξ(m)), ξ − ξ(m)⟩ + 12 ∇2L[ξ(m)](ξ − ξ(m), ξ − ξ(m)) + Φ( ξ)

}

. (3.3) With Σ (m) := (Ω (m))−1 and stacked representation of ξ, this is equivalently 

s(m) = arg min ξ∈D δ

{ 12 tr (ξ⊤H(m)ξ Σ(m)) + tr (ξ⊤G(m)) + Φ( ξ)

}

, (3.4) 11 where H(m) := H(ξ(m)) and G(m) := ∇L (ξ(m)) − H(m)ξ(m)Σ(m). The direction d(m) := s(m) − ξ(m)

is damped with the standard self-concordant step length α(m) = (1 + ν(m))−1, ν (m) := ∥d(m)∥ξ(m) .

Algorithm 2: Damped proximal Newton for (3.1) 

Input: Preprocessed training data ( X, Y ); regularization parameters λγ , λ Ω; safeguard 

δ > 0; tolerances εstep , ε newton > 0. 

Initialize: Choose ξ(0) ∈ D δ. 

> 1

for m = 0 , 1, 2, . . . do  

> 2

Approximately solve (3.4) by Algorithm 3 to obtain s(m); 

> 3

Set d(m) ← s(m) − ξ(m) and ν(m) ← ∥ d(m)∥ξ(m) ; 

> 4

Set α(m) ← (1 + ν(m))−1; 

> 5

Update ξ(m+1) ← ξ(m) + α(m)d(m); 

> 6

if ∥d(m)∥F ≤ εstep or ν(m) ≤ εnewton then  

> 7

break ;

3.3 ADMM solver for the proximal Newton subproblem 

We solve the PN subproblem approximately by the alternating direction method of multipliers (ADMM). A closely related splitting appears in the supplementary material of Zhu (2020), and we adapt it to our self-concordant PN framework. Introducing a copy variable ζ gives min  

> ξ∈R(p+q)×q, ζ ∈D δ

12 tr (ξ⊤H(m)ξ Σ(m)) + tr (ξ⊤G(m)) + Φ( ζ), s.t. ξ = ζ. (3.5) With scaled dual variable U and penalty parameter ρ > 0, the ADMM updates are 

ξ(k+1) = arg min ξ

12 tr (ξ⊤H(m)ξ Σ(m)) + tr (ξ⊤G(m)) + ρ

2 ∥ξ − ζ(k) + U (k)∥2 

> F

, (3.6) 

ζ(k+1) = arg min ζ∈D δ Φ( ζ) + ρ

2 ∥ξ(k+1) − ζ + U (k)∥2 

> F

, (3.7) 

U (k+1) = U (k) + ξ(k+1) − ζ(k+1) . (3.8) The ξ update reduces to a Sylvester system via the standard diagonalization argument of Zhu (2020), while the Ω block proximal update and the primal-dual stopping rules are summarized in 12 Appendix C. The inner ADMM loop is terminated when gap rel PN ,m 

(ζ(k+1) , W (k+1) ) ≤ εadmm , (3.9) where the dual certificate W (k+1) and the explicit dual objective are given in Appendix C.2. 

Algorithm 3: ADMM for the PN subproblem (3.5) at outer iterate m

Input: H(m), Σ(m), G (m); penalty ρ > 0; inner tolerance εadmm > 0. 

Initialize: Warm start ξ(0) , ζ (0) , U (0) . 

> 1

for k = 0 , 1, 2, . . . do  

> 2

Update ξ(k+1) by solving (3.6);  

> 3

Update γ(k+1)  

> ζ

by entrywise soft-thresholding and update Ω (k+1)  

> ζ

by Appendix C.1;  

> 4

Update U (k+1) ← U (k) + ξ(k+1) − ζ(k+1) ; 

> 5

Form the dual feasible certificate W (k+1) and evaluate (3.9);  

> 6

if gap rel PN ,m 

(ζ(k+1) , W (k+1) ) ≤ εadmm then  

> 7

break ;

Output: s(m) ← ζ(k+1) .

Remark 3.1 (Relation to Zhu (2020)) . The ADMM splitting above is close to the supplementary algorithm of Zhu (2020), but there are two main differences. First, Zhu (2020) does not establish self-concordance of the reparameterized loss, so the Newton direction in that work is globalized by backtracking. Once Lemma 2.3 is available, the PN update uses the self-concordant step 

α(m) = (1 + ν(m))−1, ν (m) := ∥d(m)∥ξ(m) , which gives monotone descent, global convergence, and the usual second-order local rate under composite self-concordant theory. Second, the implementation in Zhu (2020) effectively applies a single soft-thresholding step on the off-diagonal entries of the Ω block. That shortcut is valid whenever the soft-thresholded candidate already satisfies Ω ⪰ δI q, in which case Appendix C.1 also terminates immediately. When the thresholded candidate is not positive definite, however, the PN subproblem needs the additional ADMM correction from Appendix C.1 to enforce the safeguard; without this correction the PN update can fail. 13 3.4 Penalty path construction and warm starts 

The regularization path is initialized at the null model, meaning the largest penalty pair for which the optimizer has no active predictor–response links and a diagonal precision matrix: 

λmax  

> γ

:= 2 

∥∥∥̂ ΣXY 

∥∥∥max , λmax Ω := 

∥∥∥̂ ΣY Y 

∥∥∥max ,off , γ(0) = 0, Ω(0) = diag( ̂ΣY Y )−1. (3.10) The Karush–Kuhn–Tucker (KKT) verification is given in Appendix D. More generally, let 

P = {(λ(ℓ) 

> γ

, λ (ℓ)Ω )}Lℓ=1 

denote any penalty path chosen by the user. Warm starts are then obtained by initializing each fit from a neighboring previously computed solution. A common choice is the geometric path 

λ(ℓ) 

> γ

= λmax  

> γ

η ℓ−1 

> γ

, λ(ℓ)Ω = λmax Ω η ℓ−1Ω , ℓ = 1 , . . . , L, 

where ηγ = ( rγ )1/(L−1) and ηΩ = ( rΩ)1/(L−1) for chosen ratios rγ , r Ω ∈ (0 , 1). When a rectangular search over the two penalties is desired, one may instead use the two-dimensional grid 

λγ,a = λmax  

> γ

η a−1 

> γ

, a = 1 , . . . , L γ , λΩ,b = λmax Ω η b−1Ω , b = 1 , . . . , L Ω,

together with neighborhood warm starts. 

3.5 Raw scale recovery and evaluation metrics 

After solving the problem on the working scale for (ˆ γ, ˆΩ), the corresponding slope and intercept on the raw scale are recovered by ˆβraw = D−1 

> x

ˆγ ˆΩ−1 and ˆ α = (¯ y − ¯x ˆβraw )⊤. For the conditional quantities used below it is also convenient to define ˆ γraw := ˆβraw ˆΩ = D−1 

> x

ˆγ and ˆ γ0 := ˆ α⊤ ˆΩ = ¯ y ˆΩ − ¯x D −1 

> x

ˆγ. 

For later simulation and real-data reporting, model selection and out-of-sample assessment are carried out on the raw data scale. For a raw-scale test sample {(xraw  

> i

, y raw  

> i

)}nte 

> i=1

, we first define the ordinary prediction mean squared error (MSE) MSE te = 1

> nte ·q

∑nte 

> i=1

∥∥∥yraw  

> i

− ˆα − ( ˆβraw )⊤xraw 

> i

∥∥∥22 .

14 To evaluate the conditional structure encoded by (ˆ γ, ˆΩ), define for coordinate j

ˆmraw  

> j

(xraw , y raw  

> −j

) = ˆγ0,j + ( xraw )⊤ ˆγraw  

> ·j

− ˆΩj, −j yraw 

> −j

ˆΩjj 

,

and then the conditional mean squared error (CMSE) CMSE te = 1

nte · q

> nte ∑
> i=1
> q∑
> j=1
> {

yraw  

> ij

− ˆmraw  

> j

(xraw  

> i

, y raw  

> i, −j

)

> }2

.

For MRCE, the same raw-scale evaluation formulas apply after inserting the fitted raw-scale parameters (ˆ α, ˆβ, ˆΩ) returned by that method and setting ˆ γraw = ˆβ ˆΩ and ˆ γ0 = ˆ α⊤ ˆΩ.

3.6 Convergence guarantees for first- and second-order methods 

For Algorithm 1, the analytic step size above is the descent rule of Tran-Dinh et al. (2015, Lemma 12), so each accepted proximal-gradient iterate decreases the objective. If the relevant level set is bounded and the diagonal metric is uniformly bounded below, the iterates converge globally to the unique minimizer by Tran-Dinh et al. (2015, Theorem 13); under additional strong regularity and Hessian approximation conditions, they converge locally at a linear rate by Tran-Dinh et al. (2015, Theorem 14). For Algorithm 2, the damping α(m) = (1 + ν(m))−1 is the optimal self-concordant step in Tran-Dinh et al. (2015, Theorem 6), guaranteeing monotone decrease. Once the Newton decrement is small enough, Tran-Dinh et al. (2015, Theorem 7) gives local quadratic convergence, matching the classical self-concordant Newton behavior in Nesterov (2004). 

# 4 Simulation Study 

We report a simulation study focused on algorithmic robustness and the pathwise computational behavior of the proposed SCA-MRCE solvers. We generate a prespecified sparse multivariate linear regression model with Gaussian errors, fit the full regularization path on a single realization, and compare the terminated penalized objective values, elapsed time, and outer iteration counts across solvers. This design lets us compare the solvers on a common problem instance without additional Monte Carlo variation. Let Xraw  

> i

∈ Rp and Y raw  

> i

∈ Rq follow the independently and identically distributed (i.i.d.) 15 model Xraw 

> i
> i.i.d.

∼ Np(0 , I p), Y raw  

> i

= ( β⋆)⊤Xraw  

> i

+ εi, ε i i.i.d.

∼ Nq

> (

0, (Ω ⋆)−1). Because the penalized convex program is formulated in ( γ, Ω), sparsity is imposed on the natural parameter γ⋆ = β⋆Ω⋆,

rather than directly on β⋆. Specifically, the support of γ⋆ is generated at a prespecified sparsity level (5% nonzeros per response) on the p × q coefficient array; conditional on that support, the nonzero entries are assigned independent random signs and magnitudes drawn from a distribution bounded away from zero, and all remaining entries are set to zero. We generate the precision matrix Ω by independently activating each upper triangular off-diagonal entry with probability 0.05, assigning each active edge weight 0 .5, symmetrizing the matrix, and adding a diagonal shift so that the resulting sparse positive-definite precision matrix has the prescribed condition number 

κ(Ω ⋆) ∈ { 2, 10 , 100 }. Finally, we set β⋆ = γ⋆(Ω ⋆)−1. This is the appropriate simulation target because the penalized convex program acts on ( γ, Ω). We use a single realized data set with n = 1000, p = q = 50, and a geometric path of length 60 initialized at the closed-form null model from Section 3.4, with ratio 10 −4 between the smallest and largest penalties. We consider three conditioning regimes for the precision matrix, 

κ(Ω ⋆) ∈ { 2, 10 , 100 }. Warm starts are used along the path. We compare four SCA-MRCE solvers: 

Legacy_PN , Aggressive_PN , Legacy_PG , and Aggressive_PG . The legacy variants are conservative reference implementations with larger inner solve budgets. The aggressive variants truncate the same outer algorithms: Aggressive_PN caps the PN subproblem ADMM at 100 iterations and the Ω proximal ADMM at one iteration per outer step, while Aggressive_PG truncates the Ω proximal update to one iteration along a PG path. The comparison therefore isolates whether reducing the expensive inner work changes the pathwise solutions materially or mainly lowers the computational cost.                

> Solver Mean terminated penalized loss Total elapsed time (sec) Mean outer iterations
> Legacy_PN 45.6635 8893.73 2.66
> Aggressive_PN 45.6688 113.67 2.71
> Legacy_PG 47.5741 46.56 28.10
> Aggressive_PG 47.6367 4.88 28.39

Table 1: Summary of the fixed-data pathwise comparison. Each row aggregates results over three condition-number settings, κ(Ω ⋆) ∈ { 2, 10 , 100 }, and 60 penalty pairs per setting. “Mean terminated penalized loss” is the average final penalized training objective at solver termination over that 3 × 60 grid. “Total elapsed time (sec)” is the cumulative elapsed time over all runs for the corresponding solver, and “Mean outer iterations” is the average number of outer-loop updates per penalty pair. 16 Figure 2: Pathwise computational comparison for the four SCA-MRCE solver configurations. Columns correspond to κ(Ω ⋆) ∈ { 2, 10 , 100 } from left to right. The top row reports the terminated penalized training loss, the middle row reports elapsed time in seconds, and the bottom row reports the number of outer iterations to termination. In all panels the horizontal axis is log( λγ ). Orange filled circles denote Legacy_PN , blue open circles denote Aggressive_PN , red plus markers denote 

Legacy_PG , and green x markers denote Aggressive_PG . The aggressive variants track the legacy objective values closely, while the main computational differences appear in the amount of inner work required at each path point. 17 Figure 2 and Table 1 point to the same conclusion. PN uses far fewer outer iterations than PG on the same path, but its elapsed time depends strongly on how accurately the inner second-order subproblems are solved. Within the PG family, Legacy_PG and Aggressive_PG produce very similar terminated losses and outer-iteration profiles, and Aggressive_PG is the fastest implementation overall; when the soft-thresholded Ω candidate already satisfies the safeguard, the two PG updates are nearly identical. PN remains attractive when a more accurate solution is needed, but for routine path computation the PG variants are usually the better choice in elapsed time. Truncating the PN inner solves reduces cost substantially, although the resulting inexact steps are more sensitive than the PG updates and can stall at difficult path points. 

# 5 Real Data Analysis 

We apply classical-parameterization MRCE and two SCA-MRCE implementations, Legacy_PG and 

Aggressive_PG , to the Mice Protein Expression data set (Higuera et al., 2015). The data contain measurements on 72 mice, each observed at 15 time points, for a total of 1080 samples. We take the q = 77 protein measurements as the multivariate response Y raw , and we construct Xraw from the experimental factors Genotype, Treatment, and Behavior. Across 100 replicates, every mouse contributes exactly 5 rows to training, 5 to validation, and 5 to test, so ntr = nval = nte = 360. Predictor preprocessing follows Section 2.1: training predictors are standardized, training responses are centered only, and validation and test evaluations are carried out on the raw scale. Each method is tuned over the same 7 ×7 geometric grid with ratio 10 −5 between the smallest and largest penalties on both axes, and the tuning rule is validation MSE. Final reporting uses the test MSE and test CMSE on the raw scale defined in Section 3.5. Table 2 summarizes the study over 100 replicates. Classical-parameterization MRCE, Legacy_PG ,and Aggressive_PG have very similar validation and test MSE, with differences appearing only in the fourth decimal place. The two SCA-MRCE implementations also have smaller and less variable test CMSE than MRCE. The two PG variants are essentially indistinguishable in the reported prediction metrics, which is consistent with the fact that at most grid points the soft-thresholded Ω update already satisfies the safeguard and therefore makes the legacy and aggressive PG steps coincide. The main difference is computational: the recorded average fitting time is 2661 .271 seconds 18 for MRCE and about 6 seconds for the two PG variants, a reduction of roughly 4 .5 × 10 2 in average elapsed time per replicate.                 

> Method Validation MSE Test MSE Test CMSE Elapsed time (sec) MRCE 0.057189 (0.001722) 0.057271 (0.001710) 0.025568 (0.018721) 2661.271
> Legacy_PG 0.057227 (0.001701) 0.057354 (0.001657) 0.020665 (0.001225) 5.916
> Aggressive_PG 0.057227 (0.001701) 0.057354 (0.001657) 0.020665 (0.001225) 5.987

Table 2: Summary of the real data study across 100 replicates. The first three metric columns report mean (standard deviation) across the repeated train/validation/test splits. The elapsed time column reports the corresponding average fitting time recorded by the implementation. Figures 3 and 4 display heatmaps for one replicate; they are visual summaries, not averages over 100 replicates. In every panel, the horizontal axis is λΩ; the vertical axis is λβ for MRCE and λγ for the two SCA-MRCE methods. Figure 3 is consistent with Table 2: there is no evidence that the convex reparameterization sacrifices predictive accuracy. The selected models (red diamonds) lie away from the most heavily regularized row, and the two PG heatmaps are nearly indistinguishable. 

Figure 3: Representative validation MSE heatmaps from one replicate. From left to right the panels correspond to MRCE, Legacy_PG , and Aggressive_PG . The legend column on the right stacks the MRCE red/white legend above the shared PG blue/white legend. The red diamonds identify the models selected by validation MSE. The horizontal axis is λΩ throughout; the vertical axis is λβ for MRCE and λγ for the two SCA-MRCE methods. Figure 4 shows the computational difference more clearly. The MRCE panel exhibits substantial inflation in elapsed time as the penalties move into the weakly regularized region, where the fitted precision matrix can become dense, whereas both PG implementations remain below one second per grid point in this representative replicate. A plausible explanation is that MRCE solves a nonconvex alternating problem and that the corresponding rblasso /blasso computations become 19 more expensive when the fitted Ω is dense. By contrast, the convex PG formulations remain numerically stable across the grid, and their elapsed time varies much less. 

Figure 4: Representative elapsed time heatmaps from one replicate. From left to right the panels correspond to MRCE, Legacy_PG , and Aggressive_PG . The legend column on the right stacks the MRCE red/white elapsed time legend above the shared PG blue/white legend. The horizontal axis is λΩ throughout; the vertical axis is λβ for MRCE and λγ for the two SCA-MRCE methods. The real data analysis is consistent with the simulation study. On this protein expression problem, the convex SCA-MRCE formulation matches classical-parameterization MRCE in ordinary prediction error, improves the conditional prediction metric, and greatly reduces the cost of repeated grid fitting. 

# 6 Conclusion 

We studied a convex formulation of multivariate linear regression with Gaussian errors based on the reparameterization ( γ, Ω) and showed that the corresponding Gaussian loss is standard self-concordant. This result places the penalized likelihood problem within the composite self-concordant framework and justifies both proximal gradient and damped proximal Newton algorithms. Numerically, proximal Newton uses many fewer outer iterations, but its overall cost depends on the inner solves. For penalty path computation, the proximal gradient variants were more stable and usually faster in elapsed time. The aggressive variants were often faster still, but they do not have the same theoretical convergence guarantees. In the protein expression example, the convex formulation matched classical-parameterization MRCE in ordinary prediction error, improved the conditional prediction metric, and reduced computation time substantially. 20 A Proof of Lemma 2.1 

Proof. Fix ( βraw , Ω) with Ω ≻ 0, and write 

ri(α, β raw ) := yraw  

> i

− α − (βraw )⊤xraw  

> i

,

Lraw (α, β raw , Ω) = − log det(Ω) + 1

n

> n

∑

> i=1

ri(α, β raw )⊤Ω ri(α, β raw ).

Since Ω ≻ 0, the map α 7 → L raw (α, β raw , Ω) is strictly convex, and 

∇αLraw (α, β raw , Ω) = − 2

n

> n

∑

> i=1

Ω(yraw  

> i

− α − (βraw )⊤xraw 

> i

) = −2 Ω ((¯ y − ¯x β raw )⊤ − α).

Therefore the unique minimizer in α is α⋆(βraw ) = (¯ y − ¯x β raw )⊤.

Now set β = Dxβraw , so that βraw = D−1 

> x

β. Substituting α⋆(βraw ) back into the residuals gives 

yraw  

> i

− α⋆(βraw ) − (βraw )⊤xraw  

> i

= (yraw  

> i

− ¯y⊤) − β⊤D−1

> x

(xraw  

> i

− ¯x⊤) = yi − β⊤xi.

Hence 

Lraw 

(α⋆(βraw ), β raw , Ω) = − log det(Ω) + 1

n

> n

∑

> i=1

(yi − β⊤xi

)⊤Ω(yi − β⊤xi

) = Lprep (β, Ω) .

Any penalty that does not involve α is unchanged by this substitution, so the penalized problem on the raw data with an unpenalized intercept is equivalent to the corresponding preprocessed problem without an explicit intercept. 

# B Proof of Lemma 2.3 

Proof. Fix ξ = ( γ, Ω) ∈ dom( L) and a direction dξ = ( dγ, d Ω) ∈ Ξ. Let ϕ(t) := L(ξ + t dξ ), for all t

such that Ω + t d Ω ≻ 0. By Definition 2.2, it suffices to show |ϕ′′′ (0) | ≤ 2{ϕ′′ (0) }3/2.

Write ϕ(t) = − log det(Ω + t d Ω) + g(t), where 

g(t) := tr 

(

(Ω + t d Ω) −1(γ + t dγ )⊤̂ ΣXX (γ + t dγ ) − 2̂ Σ⊤ 

> XY

(γ + t dγ ) + (Ω + t d Ω) ̂ΣY Y 

)

.

21 The last two terms in g(t) are affine in ( γ, Ω), hence they contribute neither to ϕ′′ (0) nor to ϕ′′′ (0). Introduce the normalized quantities ¯dΩ := Ω −1/2dΩ Ω −1/2 ∈ Sq, ¯γ := ̂ Σ1/2 

> XX

γ Ω−1/2, ¯dγ := ̂

Σ1/2 

> XX

dγ Ω−1/2, and define ∆ := ¯dγ − ¯γ ¯dΩ. Using (Ω + t d Ω) −1 = Ω −1/2(I + t ¯dΩ) −1Ω−1/2, the only part of g(t) that is not affine becomes tr 

(

(I + t ¯dΩ) −1(¯ γ + t ¯dγ )⊤(¯ γ + t ¯dγ )

)

. Now set A := ¯dΩ, B := ¯γ, C := ¯dγ. Since ( I + tA )−1 = I − tA + t2A2 − t3A3 + o(t3), a direct multiplication gives tr 

(

(I + tA )−1(B + tC )⊤(B + tC )

)

= c0 + tc 1 + t2 tr ((C − BA )⊤(C − BA ))

− t3 tr ((C − BA )⊤(C − BA )A) + o(t3).

for some scalars c0, c 1. Therefore, g′′ (0) = 2 ∥∆∥2 

> F

, g ′′′ (0) = −6 tr(∆ ⊤∆ ¯dΩ) .

For the log determinant term, − log det(Ω+ t d Ω) = − log det(Ω) −log det( I +t ¯dΩ) , and the Taylor expansion of log det( I + t ¯dΩ) yields d2

> dt 2

[

− log det(Ω + t d Ω) 

] 

> t=0

= ∥ ¯dΩ∥2 

> F

, and d3

> dt 3

[

− log det(Ω + 

t d Ω) 

] 

> t=0

= −2 tr (( ¯dΩ) 3). Hence ϕ′′ (0) = ∥ ¯dΩ∥2 

> F

+ 2 ∥∆∥2 

> F

, ϕ ′′′ (0) = −2 tr (( ¯dΩ) 3) − 6 tr (∆⊤∆ ¯dΩ).

In particular, ϕ′′ (0) ≥ 0 for every feasible ξ and dξ . Since the same calculation applies after shifting the base point along any admissible line, every one-dimensional restriction of L has nonnegative second derivative; therefore L is convex. Consequently, |ϕ′′′ (0) | ≤ 2∥ ¯dΩ∥3 

> F

+ 6 ∥∆∥2 

> F

∥ ¯dΩ∥F , where we used | tr(( ¯dΩ) 3)| ≤ ∥ ¯dΩ∥3 

> F

and 

| tr(∆ ⊤∆ ¯dΩ) | ≤ ∥ ∆⊤∆∥F ∥ ¯dΩ∥F ≤ ∥ ∆∥2 

> F

∥ ¯dΩ∥F .

Let x := ∥ ¯dΩ∥F , y := ∥∆∥F . Then ϕ′′ (0) = x2 + 2 y2, |ϕ′′′ (0) | ≤ 2x3 + 6 xy 2. If x = 0 the desired inequality is trivial. Otherwise, with u := y2 

> x2

≥ 0, we obtain |ϕ′′′ (0) | 

> 2{ϕ′′ (0) }3/2

≤ 1+3 u 

> (1+2 u)3/2

. Define 

h(u) := (1 + 2 u)3/2 − 1 − 3u. Then h(0) = 0 and h′(u) = 3 (√1 + 2 u − 1) ≥ 0 for all u ≥ 0, hence 

h(u) ≥ 0 and therefore 1 + 3 u ≤ (1 + 2 u)3/2. This proves |ϕ′′′ (0) | ≤ 2{ϕ′′ (0) }3/2, i.e., L is standard self-concordant. 

# C Additional algorithmic details for the ADMM solvers 

This appendix collects the implementation material omitted from Section 3. The ADMM splitting for the PN subproblem follows Zhu (2020); we record the positive-definite Ω-block proximal map and the primal-dual certificates used to stop the PN subproblem. 22 C.1 The Ω block proximal operator 

This step follows the standard positive-definite ℓ1-penalized precision updates used in ADMM solvers; see, for example, Xue et al. (2012). We record it here because it is used repeatedly inside both the PG and PN implementations. We need to compute prox κ∥·∥ 1,off +ι{Ω⪰δI } (V ) = arg min 

> Ω⪰δI q

{ 12 ∥Ω − V ∥2 

> F

+ κ∥Ω∥1,off 

}

, (C.1) for symmetric V ∈ Sq, threshold κ > 0, and safeguard δ ≥ 0. Let Ω soft = SoftThresh off (V, κ ), where only off-diagonal entries are soft-thresholded and the diagonal is left unchanged. If Ω soft ⪰ δI q,then Ω soft is already the constrained proximal point, so no inner ADMM iterations are needed. In particular, at such updates the legacy and aggressive PG implementations are identical. Otherwise, we enforce the positive-definite safeguard via ADMM with an auxiliary variable K:min  

> Ω∈Sq,K ⪰δI, Ω= K

12 ∥Ω − V ∥2 

> F

+ κ∥Ω∥1,off .

This problem can be solved exactly by Algorithm 1 in Xue et al. (2012). 

C.2 PN subproblem primal-dual gap 

We assume H(m) is positive definite. For the PN subproblem at outer iterate m, define the primal objective 

Qm(ζ) := 12 tr (ζ⊤H(m)ζ Σ(m)) + tr (ζ⊤G(m)) + λγ ∥γζ ∥1 + λΩ∥Ωζ ∥1,off + ι{Ωζ ⪰δI }. (C.2) Consider any dual feasible certificate ( Wγ , Y Ω, S Ω) satisfying 

∥Wγ ∥max ≤ λγ , YΩ,ii = 0 , |YΩ,ij | ≤ λΩ (i̸ = j), SΩ ⪯ 0. (C.3) Set WΩ := YΩ + SΩ and W := ( Wγ , W Ω). Fenchel duality gives the dual objective 

Dm(W ) := − 12 tr 

(

(W + G(m))⊤{H(m)}−1(W + G(m)) Ω (m))

− δ tr( SΩ). (C.4) 23 If H(m) is not positive definite, one could replace the inverse {H(m)}−1 by the Moore–Penrose inverse {H(m)}† in (C.4). Hence the primal-dual gap is gap PN ,m (ζ, W ) := Qm(ζ) − Dm(W ) ≥ 0, (C.5) with relative version gap rel PN ,m (ζ, W ) := gap PN ,m (ζ, W )1 + |Qm(ζ)| . (C.6) In the ADMM implementation, the outer multiplier directly provides Wγ = ρU γ . For the Ω block, a box feasible matrix YΩ can be formed from the clipped multiplier in the nested routine of Appendix C.1 and then combined with a semidefinite slack term SΩ to form WΩ = YΩ + SΩ. This yields an exact computable gap certificate for stopping and logging. 

# D Penalty path initialization at the null model 

Proof. By the KKT conditions, a point ( γ⋆, Ω⋆) with Ω ⋆ ≻ 0 minimizes F (γ, Ω) = L(γ, Ω)+ λγ ∥γ∥1 +

λΩ∥Ω∥1,off if and only if the zero matrix belongs to its subdifferential. We verify the KKT conditions at γ⋆ = 0, Ω⋆ = diag( ̂ΣY Y )−1. First, because γ⋆ = 0,

∇γ L(γ⋆, Ω⋆) = −2̂ΣXY . The subgradient condition for the entrywise ℓ1 penalty is therefore 

∥∥∇γ L(γ⋆, Ω⋆)∥∥max = 2 ∥̂ΣXY ∥max ≤ λγ , which holds whenever λγ ≥ 2∥̂ΣXY ∥max = λmax  

> γ

.Second, since γ⋆ = 0, ∇ΩL(γ⋆, Ω⋆) = ̂ ΣY Y − (Ω ⋆)−1 =̂ ΣY Y − diag( ̂ΣY Y ). The diagonal stationarity condition is satisfied exactly because (Ω ⋆)−1 matches diag( ̂ΣY Y ), while the off-diagonal subgradient condition becomes ∥̂ ΣY Y ∥max ,off ≤ λΩ, which holds whenever λΩ ≥ ∥ ̂ΣY Y ∥max ,off = λmax Ω .All KKT conditions are therefore satisfied. 

# References 

Matteo Barigozzi and Christian Brownlees. NETS: Network estimation for time series. Journal of Applied Econometrics , 34(3):347–364, 2019. 24 T. Tony Cai, Weidong Liu, and Xi Luo. A constrained ℓ1 minimization approach to sparse precision matrix estimation. Journal of the American Statistical Association , 106(494):594–607, 2011. T. Tony Cai, Hongzhe Li, Weidong Liu, and Jichun Xie. Covariate-adjusted precision matrix estimation with an application in genetical genomics. Biometrika , 100(1):139–156, 2013. L. S. Chan, G. Li, E. B. Fauman, X. Yin, M. Laakso, M. Boehnke, and P. X.-K. Song. Dr-FARM: Identification of pleiotropic genetic variants in genome-wide association studies. Nature Communications , 16(1):5789, 2025. Li Chang and Alan H. Welsh. Robust multivariate lasso regression with covariance estimation. 

Journal of Computational and Graphical Statistics , 32(3):961–973, 2023. Sheng Chen and Arindam Banerjee. Alternating estimation for structured high-dimensional multi-response models. In Advances in Neural Information Processing Systems , volume 30, pages 2838–2848, 2017. Julien Chiquet, Tristan Mary-Huard, and Stéphane Robin. Structured regularization for conditional Gaussian graphical models. Statistics and Computing , 27(3):789–804, 2017. Mert Demirer, Francis X Diebold, Laura Liu, and Kamil Yilmaz. Estimating global bank network connectedness. Journal of Applied Econometrics , 33(1):1–15, 2018. Jerome Friedman, Trevor Hastie, and Robert Tibshirani. Sparse inverse covariance estimation with the graphical Lasso. Biostatistics , 9(3):432–441, 2008. Lingrui Gan, Naveen N. Narisetty, and Feng Liang. Bayesian estimation of Gaussian conditional random fields. Statistica Sinica , 32:131–152, 2022. Daeyoung Ham, Bradley S. Price, and Adam J. Rothman. Sparse multivariate linear regression with strongly associated response variables, 2025. arXiv:2410.10025v3. Clara Higuera, Katheleen Gardiner, and Krzysztof Cios. Mice protein expression. UCI Machine Learning Repository , 324, 2015. 25 Wonyul Lee and Yufeng Liu. Simultaneous multiple response regression and inverse covariance matrix estimation via penalized Gaussian maximum likelihood. Journal of Multivariate Analysis ,111:241–255, 2012. Amit Navon and Saharon Rosset. Capturing between-tasks covariance and similarities using multivariate linear mixed models. Electronic Journal of Statistics , 14(2):3821–3844, 2020. Yurii Nesterov. Introductory Lectures on Convex Optimization: A Basic Course , volume 87 of 

Applied Optimization . Springer, 2004. Piyush Rai, Abhishek Kumar, and Hal Daumé III. Simultaneously leveraging output and task structures for multiple-output regression. In Advances in Neural Information Processing Systems ,volume 25, pages 3194–3202, 2012. Adam J Rothman, Elizaveta Levina, and Ji Zhu. Sparse multivariate regression with covariance estimation. Journal of Computational and Graphical Statistics , 19(4):947–962, 2010. Kyung-Ah Sohn and Seyoung Kim. Joint estimation of structured sparsity and output structure in multiple-output regression via inverse-covariance regularization. In Proceedings of the Fifteenth International Conference on Artificial Intelligence and Statistics , volume 22 of Proceedings of Machine Learning Research , pages 1081–1089. PMLR, 2012. Quoc Tran-Dinh, Anastasios Kyrillidis, and Volkan Cevher. Composite self-concordant minimization. 

Journal of Machine Learning Research , 16:371–416, 2015. Junhui Wang. Joint estimation of sparse multivariate regression and conditional graphical models. 

Statistica Sinica , 25(2):831–851, 2015. Matt Wytock and Zico Kolter. Sparse Gaussian conditional random fields: Algorithms, theory, and application to energy forecasting. In Proceedings of the 30th International Conference on Machine Learning , volume 28 of Proceedings of Machine Learning Research , pages 1265–1273. PMLR, 2013. Lingzhou Xue, Shiqian Ma, and Hui Zou. Positive-definite ℓ1-penalized estimation of large covariance matrices. Journal of the American Statistical Association , 107(500):1480–1491, 2012. 26 Jianxin Yin and Hongzhe Li. A sparse conditional Gaussian graphical model for analysis of genetical genomics data. Annals of Applied Statistics , 5(4):2630–2650, 2011. Yifeng Zhou, Peng Wang, Xiang Wang, Ji Zhu, and Peter X.-K. Song. Sparse multivariate factor analysis regression models and its applications to integrative genomics analysis. Genetic Epidemiology , 41(1):70–80, 2017. Yunzhang Zhu. A convex optimization formulation for multivariate regression. In Advances in Neural Information Processing Systems , volume 33, pages 17652–17661, 2020. 27