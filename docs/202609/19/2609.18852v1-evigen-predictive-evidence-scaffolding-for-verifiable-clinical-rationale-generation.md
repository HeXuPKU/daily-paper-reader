---
title: "EviGen: Predictive Evidence Scaffolding for Verifiable Clinical Rationale Generation"
title_zh: EviGen：面向可验证临床理由生成的预测性证据脚手架
authors: "Fengnan Li, Heman Burre, Liwen Sun, Roshni Varma, Matthew M. Engelhard"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18852v1"
tags: ["query:med-ai"]
score: 8.0
evidence: 基于LLM的纵向电子健康记录可验证临床推理生成
tldr: 纵向电子健康记录包含推理临床结局所需的证据，但人工审阅不现实，LLM处理成本高且易遗漏或幻觉。为此提出EviGen三层框架：患者条件检索器用可学习查询按预测归因分数排序证据，LLM生成器以排序证据为脚手架生成临床理由，过程监督验证器在推理步骤级检查并标记不可靠主张。在三个医学预测数据集上，EviGen提升了预测性能与理由忠实度，优于全上下文LLM和RAG基线，并在可用性评估中获临床评审员偏好。该工作为可验证临床理由生成提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.18852v1/fig-001.webp\", \"caption\": \"\", \"page\": 23, \"index\": 1, \"width\": 3300, \"height\": 1500}, {\"url\": \"assets/figures/arxiv/2609.18852v1/fig-002.webp\", \"caption\": \"\", \"page\": 25, \"index\": 2, \"width\": 1448, \"height\": 1086}]"
motivation: 纵向电子健康记录包含临床推理所需证据，但人工审阅不现实，LLM处理成本高且不可靠，易遗漏或幻觉。
method: 三层框架：患者条件检索器按预测归因排序证据；LLM生成器以证据为脚手架生成临床理由；过程监督验证器在推理步骤级标记不可靠主张。
result: 在三个医学预测数据集上，预测性能与理由忠实度优于全上下文LLM和RAG基线，且临床评审员在可用性评估中更偏好。
conclusion: 该工作为可验证临床理由生成提供了有效方案，缓解LLM幻觉与遗漏问题。
---

## 摘要
纵向电子健康记录（EHR）通过病历记录、编码、检验和操作等捕捉患者多年的病史，并包含推断可能临床结局所需的证据。然而，临床医生全面审查这些记录并不现实，而基于大语言模型（LLM）的处理成本高昂且往往不可靠，会遗漏一些相关观察结果，同时虚构其他内容。因此，我们提出 EviGen，一个用于可验证临床理由生成的三层框架，以应对这些挑战。第一层是患者条件化检索器，它使用可学习查询来寻找对临床结局具有预测性、而不仅仅是文本相关的证据，并根据预测归因分数对其进行排序。第二层是 LLM 生成器，它将这种排序后的证据作为脚手架，生成以检索到的文本片段为依据的临床理由。第三层是过程监督验证器，它在推理步骤层面检查生成的临床理由，标记不可靠的论断。在三个医学预测数据集上，EviGen 相较于全上下文 LLM 和 RAG 基线提升了预测性能和理由忠实度，并在可用性评估中受到临床评审人员的青睐。

## Abstract
Longitudinal electronic health records (EHRs) capture years of patient history across notes, codes, labs, and procedures, and contain evidence needed to reason about likely clinical outcomes. However, comprehensive clinician review of these records is impractical, and LLM-based processing is costly and often unreliable, missing some relevant observations while hallucinating others. We therefore propose EviGen, a three-layer framework for verifiable clinical rationale generation that addresses these challenges. The first layer is a patient-conditioned retriever that uses learnable queries to find evidence predictive of, not just textually relevant to, a clinical outcome and ranks it by prediction attribution scores. The second layer is an LLM generator that consumes this ranked evidence as a scaffold to produce a clinical rationale grounded in the retrieved spans. The third layer is a process-supervised verifier that checks the generated rationale at the reasoning-step level, flagging unreliable claims. Across three medical prediction datasets, EviGen improves prediction performance and rationale faithfulness over full-context LLM and RAG baselines, and is preferred by clinical reviewers in a usability evaluation.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究背景**：纵向电子健康记录（EHR）跨越多年，包含病历、ICD 编码、检验、操作等信息，是临床风险预测和推理的重要依据。但患者记录往往极长、证据稀疏且分散，人工全面审阅不现实。
- **核心问题**：
  - 直接将完整 EHR 输入 LLM 不可靠：可能超过有效上下文，关键证据在长输入中被忽略，且注意力计算成本随长度近似二次增长。
  - 标准 RAG 按“文本相关性”检索，但临床重要证据往往不是显式查询能覆盖的，可能遗漏真正有预测价值的观察。
  - 医学文本生成存在幻觉、事实错误、无依据建议等问题；临床场景要求预测不仅准确，还要可追溯、可审计、可验证。
- **整体含义**：论文提出 **EviGen**，一个三层框架，将“预测性证据选择—证据脚手架式理由生成—过程监督验证”连接起来，使 LLM 生成的临床理由能够追溯到原始 EHR 证据，并支持临床医生审查。其目标不是自主诊断，而是可验证的临床决策支持与理由审计工具。

## 2. 方法论

### 2.1 总体框架

EviGen 由三层组成，观察 ID 在各层之间保留，最终输出可追溯到源记录：

1. **Evidence Selection Layer**：从完整 EHR 中选择对结局有预测价值的观察，输出预测概率和排序证据包。
2. **Rationale Generation Layer**：LLM 基于证据包生成结构化临床理由，每条理由引用证据 ID。
3. **Process Verification Layer**：步骤级验证器检查生成理由，标记不可靠推理步骤。

### 2.2 输入与编码

- 使用两种 EHR 模态：
  - 自由文本临床笔记，切分为 200 token 的 chunk，带年龄感知 header。
  - ICD 诊断编码，使用官方描述文本编码。
- 所有 chunk 和编码用共享文本编码器嵌入，论文使用 **Qwen3-Embedding-8B**，维度 \(d=4096\)。

### 2.3 第一层：证据选择层

- **核心思想**：不按文本相似度检索，而是用可学习查询检测“对临床结局有预测贡献”的证据。
- **可学习查询库**：
  - 查询向量 \(Q=[q_0,\dots,q_{N-1}]\) 分为 note queries 和 code queries。
  - 例如 mortality 任务 \(N_n=N_c=4\)，共 8 个查询；ADHD/autism 任务 \(N_n=N_c=6\)，共 12 个查询。
  - 其中一个 note query 始终激活。
- **患者条件动态门控**：
  - 对每个模态做 mean pooling 得到患者摘要 \(s^{(m)}\)。
  - 计算患者摘要与查询的余弦相似度，并与可学习阈值 \(\eta_i\) 比较，决定查询是否激活。
  - 形式为 \(g_i^{(m)}=1[\sigma(\tilde{s}^{(m)\top}\tilde{q}_i)>\sigma(\eta_i)]\)。
  - 由于指示函数不可微，使用 **straight-through estimator** 让查询和阈值可训练。
- **检索与注意力池化**：
  - 每个激活查询对患者 EHR 中 top-K chunk 做余弦检索，论文中 \(K=4\)。
  - 对检索到的 chunk 做 softmax attention pooling，得到上下文向量 \(c_i\)。
  - 每个查询有独立 expert MLP，生成查询特化表示 \(h_i\)；聚合后送入预测头得到风险概率 \(\hat{y}\)。
- **训练目标**：
  - 端到端交叉熵损失，加辅助正则项：查询多样性正则，防止不同查询塌缩到同一模式；简单性正则，限制非共享查询范数。
  - 辅助损失系数 \(\lambda_{aux}=0.01\)，AdamW，最多 20 epoch，按验证集 AUC 早停。
- **归因排序**：
  - 推理时对训练好的预测器应用 **Integrated Gradients**，为每个检索 chunk 计算 signed attribution score。
  - top-attributed chunks、观察 ID、归因分数组成 evidence pack，传给第二层。

### 2.4 第二层：理由生成层

- LLM 接收预测概率 \(\hat{y}\) 和排序后的 evidence pack。
- 生成结构化理由，每个 factor 包含四部分：
  1. **Factor summary**：对证据的描述性总结。
  2. **Supporting evidence**：逐字引用，并带 citation ID，如 `(note_id: XXXXX)` 或 `(
