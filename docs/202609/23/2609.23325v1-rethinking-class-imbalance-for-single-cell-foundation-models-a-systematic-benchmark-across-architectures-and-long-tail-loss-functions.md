---
title: "Rethinking Class Imbalance for Single-Cell Foundation Models: A Systematic Benchmark Across Architectures and Long-Tail Loss Functions"
title_zh: 重新思考单细胞基础模型中的类别不平衡：跨架构与长尾损失函数的系统性基准评测
authors: "Zeyu Dong, Jiahui Zhong"
date: 2026-09-20
pdf: "https://arxiv.org/pdf/2609.23325v1"
tags: ["query:med-ai"]
score: 6.0
evidence: 对scGPT、scBERT、Geneformer等单细胞基础模型在稀有细胞群上的评测，契合基因组大模型主题
tldr: "单细胞基础模型（scGPT、scBERT、Geneformer）在细胞类型分类上可达97.5%准确率，但总体精度会掩盖对稀有疾病相关细胞群的系统性失效，而长尾损失函数常被认为能解决该问题。本文对6种长尾损失、3种架构、3个数据集开展162次受控训练的系统基准评测。结果显示稀有类失效由数据结构和嵌入几何决定而非预训练，可恢复性取决于类别绝对训练样本量；class-balanced loss与LDAM在九种设置中最稳定，logit adjustment则牺牲精确率换召回。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.23325v1/fig-001.webp\", \"caption\": \"\", \"page\": 5, \"index\": 1, \"width\": 481, \"height\": 1063}]"
motivation: 单细胞基础模型报告的高总体准确率可能掩盖对稀有细胞群的系统性失效，而长尾损失函数能否真正修复该问题尚缺乏系统验证。
method: 在3种架构（scGPT、scBERT、Geneformer）、3个数据集和6种长尾损失（CE、加权CE、class-balanced、focal、LDAM、logit-adjusted softmax）上完成162次受控训练，并分析嵌入几何。
result: 普通交叉熵下总体准确率、Macro-F1与稀有类召回差距在全部九种设置中一致；失效分两类，一类可由合适损失恢复，另一类被无关类吸收；重加权效果由类别绝对样本量预测。
conclusion: 稀有类失效源于数据结构而非预训练，class-balanced loss和LDAM跨设置最稳健，logit adjustment仅以精确率换召回，为不平衡生物数据提供可复用基准与实用指南。
---

## 摘要
单细胞基础模型（scGPT、scBERT、Geneformer）在我们的实验中实现了高达97.5%的细胞类型分类准确率，然而这种总体准确率可能掩盖了在稀有且常与疾病相关的细胞群体上的系统性失败，而长尾损失函数被广泛认为可以解决这一问题。我们提出了一个系统性基准，涵盖六种长尾损失函数（交叉熵、加权交叉熵、类别平衡损失、焦点损失、LDAM、logit调整softmax），跨三种架构和三个数据集（多发性硬化、Zheng68K、人类胰腺），共计162次受控训练运行（3个骨干模型 × 3个数据集 × 6种损失 × 3个随机种子）。在普通交叉熵下，总体准确率、Macro-F1与稀有类别召回率之间的差距在所有九种（架构，数据集）设置中保持一致，由数据集结构而非预训练驱动。稀有类别失败本身分为两种模式，具有不同的嵌入几何特征，且在任何损失被选择之前就可见：一些类别可通过合适的损失恢复，而另一些类别虽保持线性可分性，却在所有评估的损失和架构下被吸收到无关类别的邻域中。在可恢复类别中，重加权效果由类别的绝对训练集大小预测，而不是由其在数据集中所占比例或数据集总体不平衡比预测。类别平衡损失和LDAM在所有九种设置中是最一致的选择，而logit调整是以稀有类别精确率换取召回率，而非同时提升二者。我们的结果既提供了可复用的基准，也提供了将基础模型与不平衡生物数据相结合的、基于机制的实际指导原则。

## Abstract
Single-cell foundation models (scGPT, scBERT, Geneformer) achieve cell-type classification accuracy up to 97.5% in our experiments, yet this aggregate accuracy can mask systematic failure on rare, often disease-relevant cell populations that long-tail loss functions are widely assumed to address. We present a systematic benchmark of six long-tail loss functions (cross-entropy, weighted CE, class-balanced loss, focal loss, LDAM, logit-adjusted softmax) across three architectures and three datasets (Multiple Sclerosis, Zheng68K, human Pancreas), totaling 162 controlled training runs (3 backbones x 3 datasets x 6 losses x 3 seeds). The gap between overall accuracy, Macro-F1, and rare-class recall under plain cross-entropy is consistent across all nine (architecture, dataset) settings, driven by dataset structure rather than pretraining. Rare-class failure itself splits into two regimes with distinct embedding-geometry signatures, visible before any loss is chosen: some classes are recoverable by the right loss, while others retain linear separability yet are absorbed into unrelated classes' neighborhoods under every evaluated loss and architecture. Among the recoverable classes, the efficacy of reweighting is predicted by a class's absolute training-set size, rather than its share of the dataset or the dataset's overall imbalance ratio. Class-balanced loss and LDAM are the most consistent choices across all nine settings, while logit adjustment trades rare-class precision for recall rather than improving both. Our results give both a reusable benchmark and mechanism-grounded practical guidelines for combining foundation models with imbalanced biological data.