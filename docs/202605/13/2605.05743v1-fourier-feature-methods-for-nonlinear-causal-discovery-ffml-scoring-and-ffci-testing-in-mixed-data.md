---
title: "Fourier Feature Methods for Nonlinear Causal Discovery: FFML Scoring and FFCI Testing in Mixed Data"
title_zh: 非线性因果发现的傅里叶特征方法：混合数据中的 FFML 评分与 FFCI 检验
authors: Joseph D. Ramsey
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05743v1"
tags: ["query:gwas"]
score: 6.0
evidence: 用于混合数据的非线性因果发现方法
tldr: 本研究针对非线性因果发现中高斯过程和核独立性检验计算成本高的问题，提出了基于随机傅里叶特征（RFF）的两种互补方法：FFML评分和FFCI检验。FFML通过有限维特征表示近似GP边际似然，显著降低了计算复杂度并支持混合数据；FFCI则是一种快速的非参数条件独立性检验。这套工具包在保持概率解释性的同时，大幅提升了在大规模混合数据上的因果发现效率和准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05743v1/fig-001.webp\", \"caption\": \"Figure 1: CPDAGs returned by BOSS+FFML on the Auto MPG dataset (n = 392, one discrete variable: origin). Directed edges (→) indicate orientations determined from the data or background knowledge; undirected edges (—) indicate Markov-equivalent pairs that cannot be oriented from the data alone. (a) No background knowledge; modelyear is treated as endogenous and oriented as a consequence of mpg and weight, which is causally implausible. (b) modelyear constrained to be exogenous via a prior knowledge tier; the resulting graph is both causally more plausible and achieves a higher FFML score (−618.26 vs. −631.88), indicating that the constrained search found a better region of the score landscape.\", \"page\": 15, \"index\": 1, \"width\": 948, \"height\": 387}]"
motivation: 传统的非线性因果发现方法在处理大规模数据时计算复杂度过高，限制了其在实际场景中的应用。
method: 提出了利用随机傅里叶特征近似高斯过程的FFML评分法，以及基于特征空间残差化的快速非参数检验法FFCI，均支持连续与离散混合数据。
result: 实验证明FFML在非线性任务中表现优异，而FFCI在因果发现的召回率和结构汉明距离上优于RCIT，且运行时间仅为后者的三分之一。
conclusion: 该研究通过傅里叶特征技术成功解决了非线性因果发现的计算瓶颈，为混合数据环境下的因果推断提供了高效的工具。
---

## 摘要
高斯过程边际似然评分和核条件独立性检验在理论上对非线性因果发现具有吸引力，但在大规模计算上却令人望而却步。我们提出了两种基于随机傅里叶特征（RFF）的互补方法，为基于评分、基于约束以及混合因果发现构建了一个实用的工具包。傅里叶特征边际似然（FFML）评分通过将 n x n 的核 Gram 矩阵替换为有限维特征表示，来近似精确的高斯过程（GP）边际似然，将计算成本降低至 O(nm^2 + m^3)，同时保留了精确评分的概率解释和自动复杂度惩罚。FFML 通过乘积核构造扩展到混合（连续+离散）父节点集，对于小型离散父节点集采用 Kronecker 路径，否则采用 Hadamard 乘积路径。傅里叶特征条件独立性（FFCI）检验是一种针对混合数据的快速非参数条件独立性（CI）检验。每个变量被单独特征化：连续变量通过 RFF 或正交随机特征（ORF）处理，离散变量通过 Cholesky 分解的分类特征映射处理，随后将各块拼接。条件化在特征空间中使用岭残差化；检验统计量是残差化互协方差的 Frobenius 范数，近似为卡方变量的加权和。尽管 FFML 和 FFCI 共享相同的 RFF/ORF 机制，但它们在架构上有所不同：FFML 为评分构建父节点集的联合核，而 FFCI 为检验对变量进行单独特征化。我们将 FFML 与 TRFF（一种带惩罚的 Student-t 回归替代方案）进行了比较。实验表明，在非线性数据上，BOSS+FFML 的表现优于线性和核岭回归基准。在相同的 PC-Max 实现下运行时，FFCI 和 RCIT 表现出互补的精确率-召回率曲线：RCIT 更精确，而 FFCI 实现了更好的召回率和更低的结构汉明距离（SHD），且运行时间仅为前者的三分之一。

## Abstract
Gaussian process marginal likelihood scores and kernel conditional independence tests are theoretically appealing for nonlinear causal discovery but computationally prohibitive at scale. We present two complementary RFF-based methods forming a practical toolkit for score-based, constraint-based, and hybrid causal discovery.   The Fourier Feature Marginal Likelihood (FFML) score approximates the exact GP marginal likelihood by replacing the n x n kernel Gram matrix with a finite-dimensional feature representation, reducing cost to O(nm^2 + m^3) while retaining the probabilistic interpretation and automatic complexity penalty of the exact score. FFML extends to mixed (continuous + discrete) parent sets via a product-kernel construction, with a Kronecker path for small discrete parent sets and a Hadamard-product path otherwise.   The Fourier Feature Conditional Independence (FFCI) test is a fast nonparametric CI test for mixed data. Each variable is featurized individually: continuous variables via RFF or Orthogonal Random Features (ORF), discrete variables via a Cholesky-factored categorical feature map, with blocks concatenated. Conditioning uses ridge residualization in feature space; the test statistic is a Frobenius norm of the residualized cross-covariance, approximated as a weighted sum of chi-squared variables.   Although FFML and FFCI share the same RFF/ORF machinery, they differ architecturally: FFML builds a joint kernel over a parent set for scoring, while FFCI featurizes variables individually for testing. We compare FFML to TRFF, a penalized Student-t regression alternative. Empirically, BOSS+FFML outperforms linear and kernel-ridge baselines on nonlinear data. When run through the same PC-Max implementation, FFCI and RCIT exhibit complementary precision-recall profiles: RCIT is more precise while FFCI achieves better recall and lower SHD, and runs in one third the time.