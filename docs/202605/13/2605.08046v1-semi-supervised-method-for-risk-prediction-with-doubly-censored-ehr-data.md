---
title: Semi-supervised Method for Risk Prediction with Doubly Censored EHR Data
title_zh: 针对双重截断 EHR 数据的风险预测半监督方法
authors: "Jie Zhou, Enhao Wang, Xuan Wang"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08046v1"
tags: ["query:med-ai"]
score: 9.0
evidence: 利用大规模电子健康记录数据进行半监督风险预测
tldr: 本研究针对电子健康档案（EHR）中临床事件常面临的双重删失（左删失和右删失）以及标注成本高昂的问题，提出了一种新型半监督学习（SSL）框架。该方法通过整合少量经过人工核查的金标准标签和大规模易获取但有偏的代理信息，有效解决了现有方法无法处理双重删失数据的局限。研究不仅确立了估计量的理论有效性，还通过仿真和2型糖尿病风险因素分析证明，该方法在提升风险预测效率和准确性方面显著优于传统监督学习方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08046v1/fig-001.webp\", \"caption\": \"Table 1: Estimation results for regression parameters with simulated data.\", \"page\": 9, \"index\": 1, \"width\": 967, \"height\": 914}]"
motivation: EHR数据中的临床事件常受双重删失影响且金标准标注稀缺，而现有半监督方法难以处理此类复杂数据。
method: 开发了一种结合少量金标准标签与大规模代理信息的半监督学习框架，专门用于双重删失下的风险预测。
result: 仿真实验和2型糖尿病实证研究均显示，该方法比仅使用标注数据的监督学习方法具有更高的估计效率。
conclusion: 该框架为利用大规模不完全标注的EHR数据进行临床风险评估提供了有效的理论支撑和实践方案。
---

## 摘要
大规模电子健康档案（EHR）数据的快速增长为提高临床风险评估的准确性和效率提供了独特机遇。然而，由于临床事件可能发生在记录该数据的医疗系统之外，临床事件结局往往面临双重截断（包括左截断和右截断）。此外，金标准事件发生时间通常只能通过耗时耗力的手工病历审查来确定，导致仅有一小部分患者拥有标签。仅依赖这一有限的标记集在效率上存在局限，而广泛可用的替代结局（如首次诊断代码或首次疾病提及的时间）容易出错，若直接使用可能产生有偏估计。半监督学习（SSL）方法提供了一种整合有标记和无标记数据的规范化途径，先前的工作已证明其在二分类或右截断结局场景中的优势。然而，现有方法无法处理风险预测中的双重截断问题，这带来了额外的算法挑战。为填补这一空白，我们开发了一种新型半监督学习框架，用于在双重截断下结合少量金标准标签与大规模替代信息进行风险预测。我们证明了所提估计量的理论有效性。通过广泛的模拟研究，我们表明，相较于现有的监督估计量（基于标记数据），我们的方法显著提高了估计效率。最后，我们利用美国某医疗系统的 EHR 数据研究 2 型糖尿病（T2D）的风险因素，展示了该方法的实际应用价值。

## Abstract
The rapid expansion of large-scale electronic health record (EHR) data offers unique opportunities to improve the accuracy and efficiency of clinical risk estimation. Yet, because clinical events may occur outside the recording health system, clinical event outcomes are frequently subject to double censoring (both left and right). Besides, gold-standard event times can often only be ascertained through labor-intensive manual chart reviews, yielding labels for only a small subset of patients. Reliance on this limited labeled set alone is limited in efficiency, whereas widely available surrogate outcomes such as the time to first diagnostic code or first disease mention are error-prone and can yield biased estimates if used directly. Semi-supervised learning (SSL) methods provide a principled way to integrate labeled and unlabeled data, and prior work has demonstrated their advantages in settings with binary or right-censored outcomes. However, existing approaches do not accommodate double censoring for risk prediction, which poses additional methodological challenges. To address this gap, we develop a novel SSL framework for risk prediction that combines a small set of gold-standard labels with large-scale surrogate information under double censoring. We establish the theoretical validity of the proposed estimator. Through extensive simulation studies, we show that our method substantially improves estimation efficiency relative to existing supervised estimators (based on the labeled data). Finally, we demonstrate its practical value by applying it to study risk factors for type 2 diabetes (T2D) using EHR data from a health system in the US.

---

## 论文详细总结（自动生成）

### 针对双重截断 EHR 数据的风险预测半监督方法总结

#### 1. 核心问题与整体含义（研究动机和背景）
在利用电子健康档案（EHR）进行临床风险预测时，研究者面临两个核心挑战：
*   **双重截断（Double Censoring）**：患者的真实发病时间可能早于首次就诊（左截断），也可能在随访结束时仍未发生（右截断）。忽视双重截断会导致生存函数和风险效应的估计出现偏差。
*   **标签稀缺与代理偏差**：获取准确的发病时间（金标准）需要耗费大量人力进行病历审查，因此标注数据量极小。虽然 ICD 诊断代码等“代理指标”易于大规模获取，但其存在严重的误分类偏差。
**研究动机**：开发一种能够整合少量金标准标签和海量代理指标的半监督学习（SSL）框架，以解决双重截断数据下的风险预测效率问题。

#### 2. 方法论：核心思想与关键技术
该论文提出了一种基于**半参数线性变换模型**的增强估计框架：
*   **基础模型**：假设 $h(T) = -\beta'Z + \epsilon$，其中 $h$ 是未知的单调递增函数，$\epsilon$ 服从已知分布（如极值分布对应比例风险模型，逻辑分布对应比例优势模型）。
*   **监督估计（第一步）**：利用标注数据 $D_l$，通过非参数极大似然估计（NPMLE）和期望最大化（EM）算法，结合受试者特定的泊松变量和潜在脆弱变量，得到初步的监督估计量 $\hat{\beta}_{SL}$。
*   **增强估计（第二步）**：利用未标注数据 $D_u$ 中的代理指标 $T^*$。
    *   构建两个“工作模型”（Working Models）：模型 1 假设代理指标与真实指标遵循相同的变换模型；模型 2 采用更灵活的变换模型，仅假设线性条件。
    *   **核心公式**：$\hat{\beta}_{SSL} = \hat{\beta}_{SL} - \hat{\Omega} \hat{\Sigma}_{\gamma}^{-1} (\hat{\gamma} - \bar{\gamma})$。其中 $\hat{\gamma}$ 是基于标注数据对代理指标的估计，$\bar{\gamma}$ 是基于全量数据（标注+未标注）的估计。
*   **组合 SSL 估计量**：通过堆叠多个工作模型的增强项，进一步降低渐近方差，实现最优效率。

#### 3. 实验设计
*   **仿真实验场景**：
    *   设置了不同的误差分布（$r=0$ 比例风险，$r=1$ 比例优势）。
    *   模拟了 20% 的左截断和 20% 的右截断比例。
    *   样本量设定：标注数据 $n=200, 400$，未标注数据 $N=5n$。
    *   **Benchmark**：仅使用标注数据的监督学习方法（SL）。
*   **实证研究**：
    *   **数据集**：美国某医疗系统的 2 型糖尿病（T2D）EHR 数据。
    *   **规模**：总计 115,236 名患者，其中仅 1,613 名（约 1.4%）拥有通过人工核查获得的金标准标签。
    *   **代理指标**：首次出现 T2D 相关的 PheCode 时间。

#### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   由于该方法基于半参数统计模型和 EM 算法，而非深度神经网络，其计算开销主要集中在 EM 迭代和大规模矩阵运算上，通常在标准 CPU 服务器上即可运行。

#### 5. 实验数量与充分性
*   **仿真实验**：进行了 500 次重复实验，涵盖了模型正确设定和误设（$r \neq r^*$）的多种组合，验证了估计量的偏差（Bias）、标准差（SE）、覆盖概率（CP）和相对效率（RE）。
*   **实证分析**：使用了真实的、具有代表性的大规模 EHR 队列，并对比了三种不同的 SSL 变体（SSL1, SSL2, SSL3）。
*   **评价**：实验设计较为充分，通过仿真验证了理论性质（一致性和渐近正态性），通过实证展示了实际应用价值，对比过程客观公平。

#### 6. 主要结论与发现
*   **效率显著提升**：在所有场景下，SSL 估计量的标准误均显著低于 SL 估计量，相对效率（RE）最高可提升 80% 以上。
*   **鲁棒性强**：即使代理指标的工作模型设定错误，SSL 估计量依然保持一致性（无偏），且仍能获得效率增益。
*   **组合效应**：结合多个工作模型的 SSL3 估计量在所有实验中表现最优，证明了多源信息整合的有效性。
*   **临床发现**：在 T2D 研究中，SSL 方法成功识别出基线年龄、性别和种族是显著的风险因素，且估计精度远高于传统方法。

#### 7. 优点
*   **理论完备**：首次为双重截断数据下的风险预测建立了半监督推理的理论框架，并证明了其渐近性质。
*   **实用性高**：直接针对 EHR 数据中“标签少、截断多、代理指标有偏”的痛点，降低了对昂贵人工标注的依赖。
*   **灵活性**：允许使用多个工作模型，且不要求代理指标与真实结局之间有严格的参数化关系。

#### 8. 不足与局限
*   **缺失机制假设**：该方法假设标签是完全随机缺失（MCAR）的。在实际 EHR 中，人工核查的对象可能并非随机选择（如倾向于核查复杂病例），这可能导致偏差。
*   **独立性假设**：假设截断时间（L, U）与协变量独立，但在实际医疗场景中，就诊频率（影响截断时间）可能与患者的健康状况（协变量）相关。
*   **计算复杂度**：随着工作模型数量的增加，EM 算法和协方差矩阵的估计计算量会增大。

（完）
