Title: Fourier Feature Methods for Nonlinear Causal Discovery: FFML Scoring and FFCI Testing in Mixed Data

URL Source: https://arxiv.org/pdf/2605.05743v1

Published Time: Fri, 08 May 2026 00:43:50 GMT

Number of Pages: 17

Markdown Content:
# Fourier Feature Methods for Nonlinear Causal Discovery: FFML Scoring and FFCI Testing in Mixed Data 

Joseph Ramsey ∗

Department of Philosophy Carnegie Mellon University Pittsburgh, PA 15213 

jdramsey@andrew.cmu.edu 

Abstract 

Gaussian process (GP) marginal likelihood scores and kernel conditional independence tests are theoretically appealing for nonlinear causal discovery but computationally prohibitive at scale. We present two complementary RFF-based methods forming a practical toolkit for score-based, constraint-based, and hybrid causal discovery. The Fourier Feature Marginal Likelihood (FFML) score approximates the exact GP marginal likelihood by replacing the n × n kernel Gram matrix with a finite-dimensional feature repre-sentation, reducing cost to O(nm 2 + m3) while retaining the probabilistic interpretation and automatic complexity penalty of the exact score. FFML extends to mixed (continuous + dis-crete) parent sets via a product-kernel construction, with a Kronecker path for small discrete parent sets and a Hadamard-product path otherwise. The Fourier Feature Conditional Independence (FFCI) test is a fast nonparametric CI test for mixed data. Each variable is featurized individually: continuous variables via RFF or Or-thogonal Random Features (ORF), discrete variables via a Cholesky-factored categorical feature map, with blocks concatenated. Conditioning uses ridge residualization in feature space; the test statistic is a Frobenius norm of the residualized cross-covariance, approximated as a weighted sum of chi-squared variables. Although FFML and FFCI share the same RFF/ORF machinery, they differ architecturally: FFML builds a joint kernel over a parent set for scoring, while FFCI featurizes variables indi-vidually for testing. We compare FFML to TRFF, a penalized Student-t regression alternative. Empirically, BOSS + FFML outperforms linear and kernel-ridge baselines on nonlinear data. When run through the same PC-Max implementation, FFCI and RCIT exhibit complementary precision–recall profiles: RCIT is more precise while FFCI achieves better recall and lower SHD, and runs in one third the time. 

# 1 Background and Motivation 

Kernel-based methods provide a principled and flexible approach to modeling nonlinear relation-ships in causal discovery. They are attractive both for score-based algorithms, which evaluate candidate parent sets via regression-style objectives, and for constraint-based algorithms, which rely on conditional independence (CI) testing. In greedy score-based search procedures such as FGES [Ramsey et al., 2017] and BOSS [Andrews et al., 2023], the score must be stable under small changes to the parent set, local in the sense that it decomposes by variable, and comparable across parent sets of differing size and composition. 

> ∗

The author used a large language model (Claude, Anthropic) to assist with editing and presentation of this document and takes full responsibility for its content. 

1

> arXiv:2605.05743v1 [stat.ML] 7 May 2026

1.1 Kernel Scores for Causal Discovery 

Gaussian process (GP) marginal likelihoods satisfy these desiderata and serve as natural nonlinear generalizations of linear-Gaussian scores. By integrating out the regression function, GP marginal likelihoods provide an automatic complexity penalty and behave smoothly under parent additions and deletions. However, exact GP marginal likelihoods require forming and factorizing an n × n

kernel Gram matrix, leading to O(n3) time and O(n2) memory, which is computationally prohibitive at scale. Huang et al. [2018] introduced the first practical kernel-based score for causal discovery, demon-strating that nonlinear dependencies could be detected reliably in greedy search without committing to a parametric functional form. Their generalized score measures the improvement in kernel ridge regression fit when a candidate parent is added, using residual operators of the form ( KZ + λI )−2.This construction is a direct inspiration for FFML: both methods use a kernel over the parent set to evaluate the conditional distribution of a child variable, and both are designed to be local and decomposable for use in score-based search. FFML differs from the Huang et al. score in two important respects. First, FFML approximates the exact GP marginal likelihood rather than a regression-residual criterion. The marginal likelihood involves only the single inverse ( KZ + σ2I)−1 together with a log-determinant normalization, giving it a clean probabilistic interpretation and an automatic Occam factor that penalizes complexity without a separate tuning step. Because the Huang et al. score is derived from regression residuals rather than a joint probabilistic model, it does not carry an analogous complexity penalty, and small parent-set changes can induce larger score fluctuations that reduce stability in greedy search. Second, by replacing the n × n Gram matrix with a finite-dimensional RFF representation, FFML reduces cost to O(nm 2 + m3) while retaining this probabilistic interpretation. Finally, neither the Huang et al. score nor the exact GP marginal likelihood supports discrete variables natively; FFML extends to mixed (continuous and discrete) parent sets through a product-kernel construction described in Section 5. 

1.2 Kernel Tests for Conditional Independence 

Kernel Conditional Independence (KCI), introduced by Zhang et al. [2012], provides a theoretically principled nonparametric CI test based on the eigenspectrum of kernel Gram matrices. KCI is consistent against all alternatives and requires no parametric assumptions on the joint distribution, making it attractive for constraint-based causal discovery on nonlinear data. However, Strobl et al. [2019] noted that KCI scales at least quadratically with sample size—more precisely O(n3)due to the required eigendecomposition—rendering it unusable for large datasets and making it computationally infeasible at the 20-node, n = 2 ,000 setting we consider in our experiments. Strobl et al. [2019] introduced the Randomized Conditional Independence Test (RCIT) as a scalable approximation to KCI, replacing the exact Gram matrices with random Fourier feature approximations, reducing cost to linear in n while approximating the KCI test statistic. RCIT represents an important step toward practical nonparametric CI testing and is a direct predecessor of FFCI. However, we find that RCIT as implemented in causal-learn has two limitations that motivate FFCI. First, several of its p-value approximation methods yield p-values that cluster near 0.7 rather than following a uniform distribution under the null. Only the permutation approximation produces well-calibrated p-values in our experiments, but this method is too slow for practical use within a causal discovery algorithm that may request thousands of CI tests even on a sparse graph. This calibration concern is one motivation for FFCI’s p-value approximation methods, which are selected 2for their ability to produce well-calibrated, approximately uniform p-values under the null. Second, RCIT as implemented in causal-learn does not support discrete variables directly. FFCI addresses both limitations. On the p-value side, FFCI offers four approximation methods— gamma, saddlepoint, Davies/Imhof, and a faster residual-based permutation procedure—all selected for their ability to produce well-calibrated, approximately uniform p-values under the null, making each suitable for use within a causal discovery algorithm at scale. The gamma approximation is recommended as the default. On the discrete variable side, FFCI extends to mixed continuous– discrete data through a per-variable feature construction that concatenates RFF/ORF continuous features with a Cholesky-factored categorical feature map for discrete variables, described in Sec-tion 6. FFCI additionally uses separate feature counts for the tested variables ( mXY ) and the conditioning set ( mZ ), allowing a richer representation of the conditioning set where it matters most for test power. In experiments using a common PC-Max implementation for all three PC variants, FFCI and RCIT exhibit complementary precision–recall profiles: RCIT achieves higher precision while FFCI achieves better recall and lower SHD, and runs in approximately one third the time. 

1.3 Computational Setting 

FFML and FFCI address the computational barriers of their exact counterparts—the GP marginal likelihood and KCI—by replacing n × n kernel Gram matrices with finite-dimensional RFF rep-resentations whose dimension m is controlled independently of the sample size. Both methods reduce to O(nm 2) per evaluation (with an O(n3) path retained in FFML when two or more dis-crete parents are present), making them practical at the scales where exact kernel methods are infeasible. Together they form a coherent toolkit: FFML for score-based and hybrid search, FFCI for constraint-based search, both resting on the same RFF/ORF approximation philosophy and compatible bandwidth and regularization strategies. Section 2 reviews the exact GP marginal like-lihood that FFML approximates; Section 3 describes the RFF/ORF machinery common to both methods. 

# 2 Exact Kernel Marginal Likelihood (KML) 

Let Y ∈ Rn be a centered response variable and Z ∈ Rn×d the matrix of parent observations. We model the conditional distribution of Y given Z via a Gaussian process: 

Y = f (Z) + ε, ε ∼ N (0 , σ 2In), f ∼ GP (0 , k (·, ·)) .

Let KZ ∈ Rn×n be the kernel Gram matrix with ( KZ )ij = k(zi, z j ). Integrating out f yields the marginal covariance C = KZ + σ2In, and the GP marginal log-likelihood (up to an additive constant) is: 

SKML (Y | Z) = − 12 Y ⊤C−1Y − 12 log |C|.

This score integrates out f to avoid overfitting, is well-defined even when d > n , and behaves smoothly under parent additions and deletions. Computing it requires a Cholesky decomposition of 

C, costing O(n3) time and O(n2) memory. FFML approximates this score efficiently by replacing 

KZ with a low-rank random feature approximation. 33 Random Fourier Feature Approximation 

For shift-invariant kernels such as the Gaussian RBF kernel, Bochner’s theorem [Rahimi and Recht, 2007] guarantees a spectral representation: 

k(z, z ′) = Eω,b 

h√2 cos( ω⊤z + b) √2 cos( ω⊤z′ + b)

i

,

where ω is drawn from the spectral measure of k and b ∼ Uniform(0 , 2π). We parameterize the Gaussian RBF kernel as k(z, z ′) = exp  −∥ z − z′∥2/bw 2, so the spectral distribution is ω ∼ N (0 , 2bw 2 Id). Using m random samples ( ωj , b j ), we obtain the feature map: 

ϕ(z) = 

q 2

> m



cos( ω⊤ 

> 1

z + b1)...cos( ω⊤

> m

z + bm)

 ∈ Rm, k(z, z ′) ≈ ϕ(z)⊤ϕ(z′).

Stacking rows gives Φ ∈ Rn×m with KZ ≈ ΦΦ ⊤.

3.1 Orthogonal Random Features (ORF) 

Standard RFF draws ω1, . . . , ω m independently, which can produce noticeable Monte Carlo variance when m is modest. Orthogonal Random Features (ORF) [Yu et al., 2016] reduce this variance by generating frequencies that form approximately orthogonal directions, spreading coverage more uniformly in Rd.The ORF construction proceeds in blocks of size d:(1) Sample a random Gaussian matrix and extract an orthonormal basis Q via QR decomposition. (2) Sample radii ri ∼ χ(d) (the distribution of ∥g∥ for g ∼ N (0 , I d)). (3) Set ωi = s r i qi, where qi is the ith row of Q and s = √2/bw. If m > d , the procedure repeats for additional blocks. Phase offsets bj remain i.i.d. Uniform(0 , 2π). Both FFML and FFCI support RFF and ORF interchangeably; the score and test formulas are unchanged, only the frequency sampling differs. ORF typically yields lower-variance kernel approx-imations and increased stability of score differences in greedy search. 

# 4 FFML: Fourier Feature Marginal Likelihood 

4.1 Continuous Parents 

Substituting KZ ≈ ΦΦ ⊤ gives C ≈ ΦΦ ⊤ + σ2In. By the Woodbury matrix identity and the matrix determinant lemma: 

C−1 = 1

σ2 In − 1

σ4 Φ(Φ ⊤Φ + σ2Im)−1Φ⊤, (1) log |C| = ( n − m) log σ2 + log |Φ⊤Φ + σ2Im|. (2) Defining G = Φ ⊤Φ ∈ Rm×m and v = Φ ⊤Y ∈ Rm, the FFML local score is: 

SFFML (Y | Z) = − 12

 Y ⊤Yσ2 − v⊤(G + σ2Im)−1vσ4



− 12

 (n − m) log σ2 + log |G + σ2Im| . (3) All matrix operations occur in m-dimensional space, costing O(ndm + nm 2 + m3), effectively linear in n for fixed m ≪ n. When the parent set is empty the model reduces to Y ∼ N (0 , σ 2In)and the score is computed exactly without features. 44.2 Complexity Penalty and Occam Factor 

The log-determinant term − 12 log |C| plays the role of an Occam factor : models with larger effective parameter volume are automatically penalized. In terms of the eigenvalues {sj } of G:log |G + σ2Im| = X

> j

log( sj + σ2).

This grows with the informativeness of the feature representation and shrinks as σ2 increases relative to the signal. 

4.3 Bandwidth Selection 

Continuous parent columns are globally z-scored. The RBF bandwidth is estimated from a median pairwise squared distance on a random subsample (up to 100 rows by default) of the continuous parents only, so that discrete parents do not distort the continuous metric. The median estimate ˆh2

is then refined by a small grid search over four multipliers {0.35 , 0.70 , 1.40 , 2.80 }. When bandwidth coupling by target is enabled (the default), the bandwidth is cached by (target variable, continuous parents), keeping it stable across alternative parent sets that share the same continuous parents. 

4.4 Feature Coupling by Target 

The random feature basis ( ωj , b j ) is coupled by target only by default: all parent sets of a given target variable share the same features, with the seed depending only on the target index. This stabilizes local score differences S(Y | Pa ∪ { X}) − S (Y | Pa) and reduces spurious edge reversals in greedy algorithms such as BOSS. 

# 5 Mixed (Continuous + Discrete) Parent Sets in FFML 

5.1 Product-Kernel Model 

Embedding integer-coded discrete levels into a continuous Euclidean metric imposes a spurious ordering on unordered categories. FFML instead handles mixed parent sets through a product kernel. Let Z = ( Zc, Z d) with continuous part Zc and discrete part Zd. The mixed kernel is: 

k (zc, z d), (z′

> c

, z ′

> d

) = kcont (zc, z ′

> c

) · kcat (zd, z ′

> d

),

where kcont is the RBF kernel and kcat is a positive-semidefinite categorical kernel with diagonal 1 and off-diagonal ρ ∈ [0 , 1). For multiple discrete parents the categorical kernel factorizes mul-tiplicatively. Setting ρ = 0 gives strict block structure (no sharing across levels); ρ → 1 makes categories nearly indistinguishable. 

5.2 Feature-Space Path: Zero or One Discrete Parent 

For zero or one discrete parent, the product kernel is implemented via the Kronecker feature map: 

ϕmix (zc, z d) = ϕcat (zd) ⊗ ϕcont (zc).

The categorical block ϕcat (zd) is the row of the Cholesky factor A of the L × L level-similarity matrix (diagonal 1, off-diagonal ρ) corresponding to the observed level, ensuring the inner product recovers the product kernel exactly. With L levels the combined feature dimension is m × L, and the FFML score is computed via a Woodbury system of that dimension, with no n × n matrix. 55.3 Direct n × n Kernel Path: Two or More Discrete Parents 

When the Kronecker feature dimension m × Q 

> j

Lj would exceed min( n, D max ), where Dmax =200 ,000 is a fixed threshold, the implementation switches to a direct n × n kernel formulation using the Hadamard product: 

K = Kcont ◦ Kcat ,

where Kcont = ΦΦ ⊤ and Kcat is built entry-wise as a product over discrete parents. The covariance 

C = K + σ2In is Cholesky-factored directly, at cost O(n3). In practice this switch occurs most commonly when two or more discrete parents are present, but a single discrete parent with many levels can also trigger it. Categorical kernel entries are precomputed in packed lower-triangular form and reused across bandwidth-grid candidates. 

5.4 Discrete Targets 

When the child Y is discrete, a Gaussian multi-output surrogate is used: Y is one-hot encoded, each column is mean-centered, and the single-output FFML score is summed across columns. This is not a true multinomial likelihood but is an effective proxy for detecting dependencies in structure scoring. 

# 6 FFCI: Fourier Feature Conditional Independence Test 

6.1 Overview and Motivation 

FFCI (Fourier Feature Conditional Independence) is a fast nonparametric CI test for datasets containing any mix of continuous and discrete variables. It tests H0 : X ⊥ Y | Z by mapping each variable into a finite-dimensional feature space, residualizing with respect to Z via ridge regression in that space, and evaluating a quadratic-form statistic against its null distribution. FFCI differs architecturally from FFML in an important respect. FFML builds a joint kernel over the entire parent set to score a single child variable, using a product-kernel construction that couples continuous and discrete parents together. FFCI instead featurizes each variable individually ,constructing a joint per-variable feature representation by concatenating continuous and discrete feature blocks, and then measures residual cross-covariance between the featurizations of X and 

Y after conditioning out Z. The two methods share the underlying RFF/ORF machinery and ridge regularization, but serve fundamentally different roles: FFML is a scoring criterion for causal search, while FFCI is a hypothesis test for conditional independence. When the dataset contains no discrete variables, all CI queries are delegated transparently to the continuous-only implementation, and FFCI behaves identically to that method given the same hyperparameter settings. 

6.2 Per-Variable Mixed Feature Construction 

For each variable or block of variables V , FFCI constructs a joint feature matrix Φ V by separately featurizing the continuous and discrete components of V and then horizontally concatenating the results: ΦV =  Φcont  

> V

Φdisc 

> V

 ∈ Rn×(mc+md).

6Continuous features. Each continuous variable is z-scored. The bandwidth for the RBF kernel is estimated from the median pairwise squared distance on a subsample of the continuous compo-nents only. RFF or ORF features are then computed with the same construction used in FFML, yielding Φ cont  

> V

∈ Rn×mc .

Discrete features. Each discrete variable with L levels is mapped to a feature vector via a categorical feature map derived from the Cholesky factor A of the L × L level-similarity matrix (diagonal 1, off-diagonal ρ): for observation i at level ℓ, the discrete feature is the ℓth row of A.When ρ = 0 (the default in FFCI), A = IL and the construction reduces to standard one-hot encoding. For ρ > 0, the feature map encodes partial similarity between levels, analogous to the categorical kernel in FFML but applied per-variable rather than as a kernel over a joint parent set. The discrete features for all discrete components of V are concatenated to form Φ disc  

> V

∈ Rn×md .

Separate feature counts for XY and Z. FFCI uses separate feature counts for the tested variables ( X, Y ) and the conditioning set ( Z). The defaults are mXY = 10 and mZ = 100; a larger feature count for Z is appropriate because the residualization step requires a more accurate representation of the conditioning set than is needed for the cross-covariance computation. 

Featurization of the Y -side block. When the conditioning set Z is nonempty, FFCI featurizes 

Y and Z jointly: the feature matrix for the Y -side block is constructed from the concatenation of Y

and all variables in Z, using the mixed feature map above. This joint featurization allows the resid-ualization step to account for the dependence between Y and Z within the feature representation itself. 

6.3 Conditioning via Ridge Residualization 

Conditioning on Z is achieved by regressing the feature matrices for X and Y on the feature matrix for Z using ridge regression and retaining the residuals: ˜ΦX = Φ X − ΦZ (Φ ⊤ 

> Z

ΦZ + λI )−1Φ⊤ 

> Z

ΦX ,

with an analogous expression for ˜ΦY . Here λ > 0 is the ridge parameter, which plays a role analogous to the noise variance in FFML. All matrix operations occur in feature space, with cost 

O(nm 2 

> Z

+ m3 

> Z

) for the residualization step. 

6.4 Test Statistic and Null Distribution 

After residualization, both ˜ΦX and ˜ΦY are column-mean-centered. The test statistic is: 

T = n Cov( ˜ΦX , ˜ΦY ) 2 

> F

.

Under H0 : X ⊥ Y | Z, T follows approximately a weighted sum of chi-squared(1) variables, with weights given by the positive eigenvalues of the sample Khatri–Rao covariance matrix formed from elementwise products of the residualized features. Four methods are available for computing the p-value: (1) GAMMA (default): Satterthwaite/moment-matching gamma approximation. (2) SADDLEPOINT : Lugannani–Rice saddlepoint approximation. (3) DAVIES IMHOF : Davies/Imhof numerical integration. (4) PERMUTATION : residual-based permutation test. 76.5 Marginal Testing (No Conditioning Set) 

When Z is empty, no residualization is performed and the test reduces to a randomized inde-pendence test (RIT): the statistic T = n∥Cov(Φ X , ΦY )∥2 

> F

is computed directly from the (mean-centered) feature matrices, and the same weighted chi-squared approximation is used to obtain a 

p-value. 

6.6 Caching and Reproducibility 

Feature matrices and bandwidth estimates are cached by a key encoding the variable names, active row set, hyperparameters, and a dataVersion counter. Calling bumpDataVersion() after any in-place modification of the dataset invalidates stale cached values. Seeds for feature generation are derived deterministically from variable names and the active row set, ensuring reproducibility across calls. 

# 7 Relationship Between FFML and FFCI 

FFML and FFCI are related but architecturally distinct. Their shared elements are: 

• RFF/ORF approximations to shift-invariant kernels. 

• Median-heuristic bandwidth estimation on continuous variables. 

• Ridge regularization for numerical stability. 

• Cholesky-based categorical feature maps for discrete variables. 

• Avoidance of explicit n × n Gram matrices in the common case. Their key architectural differences are: Aspect FFML FFCI 

Role Local score for causal search CI hypothesis test Feature scope Joint kernel over parent set Per-variable feature map Discrete handling Product kernel (Kronecker/ n×n) Concatenated one-hot or cat. features Default ρ 0.5 0.0 (one-hot) Feature counts Single m Separate mXY and mZ

Output Log marginal likelihood p-value Complexity control Log-determinant Occam factor None (hypothesis test) Despite these differences, using FFML and FFCI together in a hybrid causal discovery algorithm is natural: both rest on the same RFF/ORF kernel approximation philosophy, use compatible bandwidth and regularization strategies, and are computationally efficient at scale. 

# 8 Comparison with TRFF 

TRFF is an alternative local score for mixed data using Student-t regression, also being introduced here as a BIC-style alternative to FFML, with random Fourier features and a BIC-style complexity penalty: 

StRFF = ℓ(ˆθ) − 12 c edf log n, 

8where edf = tr( H) is the effective degrees of freedom and H is the ridge-adjusted hat matrix. The complexity penalties depend differently on the eigenspectrum {sj } of Φ ⊤Φ: FFML penalty ∼ X

> j

log( sj + σ2), (4) TRFF penalty ∼

X  

> jsj
> sj+λ



log n. (5) FFML penalizes via a log-determinant Occam factor; TRFF penalizes via a trace times log n.The two scores also differ in how discrete parents are incorporated. 

FFML TRFF 

Discrete representation Product kernel ( ρ) One-hot dummy columns Smoothing across levels Via ρ parameter None (parametric) Complexity accounting Kernel log-determinant edf penalty Feature space Kronecker or n × n Concatenated design matrix Noise model Gaussian Student-t

Small samples. FFML is often preferable: the integrated-out prior provides regularization with-out relying on asymptotic approximations, and the Occam factor is naturally calibrated. 

Large samples. TRFF scales more easily when parents are predominantly continuous (IRLS with a fixed feature dimension avoids n × n operations). FFML’s n × n path (required for ≥ 2discrete parents) incurs the same asymptotic cost as the exact GP. 

Heavy-tailed noise. The Student-t likelihood in TRFF offers robustness that FFML, which assumes Gaussian residuals, does not provide. 

# 9 Score and Test Comparison 

Method Type Interpretation Complexity Stability Huang et al. Score Kernel ridge regression residual Impl.-dependent Moderate KML (exact) Score GP marginal likelihood O(n3) High FFML Score Approx. GP marginal likelihood O(nm 2)† High ∗

TRFF Score Penalized Student-t likelihood O(nm 2) High KCI Test Exact kernel CI test O(n3) High RCIT Test Approx. kernel CI test O(nm 2) Moderate ‡

FFCI Test Approx. kernel CI test O(nm 2) High 

> ∗

High stability requires coupleFeaturesByTarget =true ; without this, score differences may fluc-tuate across parent sets. 

> †

O(n3) when the Kronecker feature dimension m × Q 

> j

Lj exceeds min( n, D max ); see Section 5. 

> ‡

Stability depends on the PC implementation used; when run through Tetrad’s PC-Max, RCIT exhibits stable behavior comparable to FFCI. Non-default p-value approximation methods in the 

causal-learn implementation [Zheng et al., 2024] yield poorly calibrated p-values but this does not cause orientation failures when a robust PC implementation is used. 910 Practical Considerations 

Feature count. Approximation accuracy improves with m at the cost of increased computation. For FFML, m = 50 is a reasonable default; for FFCI, the default mZ = 100 for the conditioning set is more important to tune than mXY = 10, since residualization quality drives test power. 

Gaussian noise in FFML. FFML assumes Gaussian residuals. A wide feature map makes the mean function nonparametric, but ε remains Gaussian regardless. In heavy-tailed or contaminated settings, TRFF may be preferable. 

FFCI p-values. The p-values produced by FFCI are approximate and should be interpreted as fast screening tools rather than exact finite-sample tests. FFCI offers four approximation methods for the null distribution of the weighted chi-squared statistic: the gamma (Satterthwaite moment-matching) approximation, the Lugannani–Rice saddlepoint approximation, Davies/Imhof numerical integration, and a residual-based permutation test. These methods were selected for their ability to produce well-calibrated, approximately uniform p-values under the null. By contrast, the non-gamma approximation methods available in the causal-learn implementation of RCIT produce 

p-values that cluster near 0.7 rather than following a uniform distribution; however, we find that this calibration issue does not cause orientation failures when RCIT is run through a robust PC implementation such as Tetrad’s PC-Max. The gamma approximation is recommended as the default. 

Mixed data and the ρ parameter. The categorical similarity parameter ρ plays different roles in FFML and FFCI. In FFML, ρ = 0 .5 is the default and controls the degree of function sharing across discrete levels in the joint parent kernel. In FFCI, ρ = 0 is the default, reducing discrete featurization to standard one-hot encoding; nonzero ρ introduces partial similarity between levels in the per-variable feature map. 

# 11 Experiments 

We evaluate FFML and FFCI against competitive baselines on synthetic nonlinear data, assessing both accuracy and scalability. All experiments were conducted on graphs with 20 nodes, average degree 3, and sample size n = 2 ,000. 

11.1 Experimental Setup 

Data generation. We simulate data from a general-noise structural causal model 

Xi = fi

 Pa( Xi), ε i

,

where each fi is a randomly initialized six-hidden-layer feedforward network with 200 units per layer and tanh activations, and εi ∼ tanh( N (0 , 1)). Weights are initialized with Xavier normal scaling (scale factor 5 for hidden layers, 2.5 for the output layer); biases are zero. The noise enters as an additional input column alongside the parent values, so the interaction between parents and noise is fully nonlinear—a strictly harder setting than additive noise. Random DAGs are generated by fixing a uniformly random topological order and including each permitted directed edge independently with probability p = 3 /(n − 1), giving expected average degree 3. The resulting data are not standardized between nodes. All results are averaged over 50 independent replicates, with a fresh 10 random DAG and dataset drawn per replicate. Data generation was implemented independently in Python using numpy and networkx , matching the behavior of Tetrad’s GeneralNoiseSimulation .

Algorithms and baselines. We evaluate two families of algorithms. 

Score-based search (BOSS). The BOSS algorithm [Andrews et al., 2023] is run with three local scores: 

• BOSS + SEM-BIC : linear-Gaussian BIC score with penalty discount 1; fast linear baseline. 

• BOSS + TRFF : Student-t regression with random Fourier features and a BIC-style effective-degrees-of-freedom penalty (50 features, ridge 10 −3, ν = 5). 

• BOSS + FFML : Fourier feature marginal likelihood (50 features, ridge 1.0, bandwidth sub-sampled from 100 rows). 

Constraint-based search (PC). The PC algorithm [Spirtes et al., 2000] is run with three condi-tional independence tests: 

• PC + FisherZ : Fisher Z-test ( α = 0 .01); fast linear baseline. 

• PC + RCIT : Randomized conditional independence test [Strobl et al., 2019], using causal-learn’s implementation wrapped in a JPype bridge so that all three PC variants run through the same Tetrad PC-Max implementation ( α = 0 .01). 

• PC + FFCI : Fourier feature conditional independence test ( α = 0 .01, mXY = 10, mZ = 50, 

λ = 1 .0). All algorithms are implemented in or wrapped for the Tetrad project ( https://github.com/ cmu-phil/tetrad ) and were run via py-tetrad [Ramsey and Andrews, 2023]. All three PC variants use Tetrad’s PC-Max implementation, which applies the maximum of the p-values over all possible conditioning sets when orienting colliders; this is more powerful than standard PC but slower. RCIT is accessed via causal-learn’s CIT interface, wrapped in a JPype bridge that exposes it as a Tetrad IndependenceTest . An earlier version of these experiments ran PC + RCIT through causal-learn’s native PC implementation; the results differed substantially from those reported here, exhibiting poor orientation performance, and the discrepancy was traced to the PC variant rather than the CI test itself. All three PC variants are therefore run through the same Tetrad PC-Max implementation to ensure a fair comparison. KCI [Zhang et al., 2012] and the Huang et al. score were not included as baselines because both require O(n3) operations per evaluation and are computationally infeasible at 20 nodes and n = 2 ,000. 

Evaluation. Estimated graphs are compared against the true CPDAG using four metrics: adja-cency precision (Adj. Prec.), adjacency recall (Adj. Rec.), arrowhead precision (AH Prec.), arrow-head recall (AH Rec.), and structural Hamming distance (SHD). All metrics are computed using Tetrad’s built-in graph comparison utilities, which handle the CPDAG-vs-CPDAG comparison correctly. 

11.2 Results 

Tables 1 and 2 report accuracy metrics; Table 3 reports wall-clock time. 11 Table 1: Score-based methods (BOSS): accuracy over 50 replicates. Mean (SD). Best value per column in bold .Method Adj. Prec. Adj. Rec. AH Prec. AH Rec. SHD BOSS + SEM-BIC 0.791 (0.086) 0.828 (0.075) 0.629 (0.143) 0.681 (0.132) 17.18 (6.43) BOSS + TRFF 0.958 (0.040) 0.770 (0.108) 0.933 (0.109) 0.583 (0.180) 12.06 (5.22) BOSS + FFML 0.907 (0.069) 0.929 (0.060) 0.856 (0.145) 0.790 (0.121) 8.70 (5.84) Table 2: Constraint-based methods (PC-Max): accuracy over 50 replicates. Mean (SD). Best value per column in bold .Method Adj. Prec. Adj. Rec. AH Prec. AH Rec. SHD PC + FisherZ 0.889 (0.073) 0.752 (0.079) 0.510 (0.166) 0.429 (0.142) 19.78 (5.91) PC + RCIT 0.988 (0.022) 0.693 (0.113) 0.618 (0.190) 0.392 (0.146) 17.92 (6.13) PC + FFCI 0.971 (0.036) 0.844 (0.081) 0.534 (0.157) 0.476 (0.163) 16.66 (5.68) 

Score-based results (Table 1). BOSS + FFML achieves the best adjacency recall (0.929 vs. 0.828 for SEM-BIC), best arrowhead recall (0.790), and lowest SHD (8.70). The advantage over SEM-BIC is largest on arrowhead recall, where the linear score loses 11 percentage points, reflecting the nonlinearity of the data generating process. BOSS + TRFF presents a distinctive profile: it achieves the highest adjacency precision (0.958) and arrowhead precision (0.933) of any method, but its arrowhead recall (0.583) is the lowest among the score-based methods. This conservative orientation behavior—few arrowheads predicted, but those predicted are highly reliable—likely reflects the Student-t penalty being more aggressive than FFML’s log-determinant Occam factor at this graph size and sample size. 

Constraint-based results (Table 2). When all three PC variants are run through the same Tetrad PC-Max implementation, the results tell a more nuanced story than a comparison across different PC implementations would suggest. PC + FFCI achieves the best adjacency recall (0.844 vs. 0.752 for FisherZ and 0.693 for RCIT) and the best arrowhead recall (0.476), and the lowest SHD (16.66), confirming that the nonpara-metric test detects dependencies that both the linear test and RCIT miss. PC + RCIT presents a complementary profile: it achieves the highest adjacency precision (0.988) and arrowhead precision (0.618), making it the most conservative of the three methods—few edges and arrowheads are predicted, but those that are predicted are highly reliable. Its SHD standard deviation (6.13) is comparable to the other methods, indicating stable behavior across replicates; the catastrophic orientation failures observed in an earlier version of these experiments were an artifact of running RCIT through a different PC implementation (causal-learn’s standard PC rather than Tetrad’s PC-Max). PC + FisherZ is the weakest of the three on every metric, as expected for a linear test on nonlinear data. The precision–recall tradeoff between FFCI and RCIT mirrors the pattern seen in the score-based comparison between FFML and TRFF: in both cases the method with the more aggressive complexity penalty (TRFF; RCIT) achieves higher precision at the cost of lower recall, while the method with the softer penalty (FFML; FFCI) achieves better overall SHD by recovering more true structure. 12 Table 3: Wall-clock time per replicate (seconds), mean (SD) over 50 replicates. 20 nodes, n = 2 ,000. Method Time (s) BOSS + SEM-BIC 0.02 (0.06) BOSS + TRFF 6.99 (1.42) BOSS + FFML 18.10 (4.13) PC + FisherZ 0.01 (0.01) PC + FFCI 1.31 (0.58) PC + RCIT 3.09 (0.96) 

Runtime (Table 3). FFCI is approximately 2.4 times faster than RCIT (1.31 s vs. 3.09 s per replicate), both running through the same PC-Max implementation. The linear baselines SEM-BIC and FisherZ are orders of magnitude faster, as expected, but pay a substantial accuracy cost on nonlinear data. By contrast, exact kernel methods such as KCI and the Huang et al. score require 

O(n3) operations per evaluation and are computationally infeasible at 20 nodes and n = 2 ,000. 

11.3 Summary 

Across both score-based and constraint-based search, the Fourier feature methods substantially out-perform their linear baselines on nonlinear data, with BOSS + FFML producing the most accurate graphs overall. In the constraint-based setting, FFCI and RCIT exhibit complementary precision– recall profiles when run through the same PC-Max implementation: RCIT is more precise while FFCI achieves better recall and lower SHD. FFCI is additionally faster, running in approximately one third the time of RCIT per replicate. The runtime of both methods is practical at 20 nodes and n = 2 ,000, a scale at which exact kernel alternatives are infeasible. 

# 12 Illustrative Real-Data Example: Auto MPG 

To illustrate BOSS + FFML on real mixed continuous–discrete data, we apply it to the Auto MPG dataset from the UCI Machine Learning Repository [Quinlan, 1993]. The dataset records fuel economy and engineering characteristics for 398 automobiles; after removing six rows with missing horsepower values, n = 392 observations remain on eight variables: mpg , cylinders , displacement ,

horsepower , weight , acceleration , modelyear , and origin . Of these, origin is the only gen-uinely categorical variable (1 = American, 2 = European, 3 = Japanese) and is treated as discrete; all other variables are treated as continuous. Although variables such as cylinders take only a small number of distinct integer values, they admit a natural ordering and are plausibly measured on at least an ordinal scale, so embedding them in a continuous Euclidean metric is reasonable in this context. All continuous variables are z-scored internally by FFML; no additional prepro-cessing is applied. This example is intended as a qualitative illustration of how FFML handles a mixed parent set and how background knowledge can be incorporated, rather than as a quantitative benchmark (no ground-truth graph is available). 

12.1 Default Analysis 

Figure 1(a) shows the CPDAG returned by BOSS + FFML with all defaults and no background knowledge (FFML score: −631 .88). Several features of this graph are immediately plausible. The cluster of undirected edges among displacement , cylinders , and horsepower reflects the 13 well-known multicollinearity among these engineering variables: larger engines have more cylin-ders and produce more horsepower, but the data alone cannot determine the causal order among them. origin is connected to displacement (undirected), consistent with systematic differences in engine displacement across manufacturing regions. The directed paths horsepower → mpg ,

displacement → mpg , and weight → mpg are causally sensible: fuel economy is a direct consequence of the mechanical load placed on the engine. horsepower → acceleration and weight → acceleration 

are also well-supported by Newtonian mechanics. The most striking feature of the unconstrained graph is mpg → modelyear : the algorithm orients model year as a consequence of fuel economy. This is causally implausible—model year is a temporal index determined by the manufacturing calendar, not by any vehicle characteristic. The orientation almost certainly reflects the strong historical trend toward improved fuel economy over the period covered by the dataset (1970–1982), which creates a statistical association that the algorithm, lacking temporal metadata, attributes to a direct causal link in the wrong direction. 

12.2 Constrained Analysis: Model Year as Exogenous 

A user with domain knowledge can address this by placing modelyear in a prior knowledge tier, constraining it to be exogenous (i.e., it may be a cause of other variables but not caused by them). Figure 1(b) shows the resulting CPDAG (FFML score: −618 .26). The higher score of the constrained model—recall that FFML scores are log marginal likeli-hoods, so higher is better—deserves comment. This may appear paradoxical: adding a constraint restricts the search space, so how can the constrained optimum score higher than the unconstrained one? The answer lies in the nature of BOSS’s greedy search over a combinatorially large space. The unconstrained search is not guaranteed to find the global optimum; BOSS explores a local neighborhood of the score function and may settle in a region that is not globally optimal. Con-straining modelyear to be exogenous effectively redirects the search, and the algorithm discovers a different high-scoring region of the space—one that scores higher precisely because it captures the strong predictive signal of modelyear on other variables (particularly mpg and weight ) in a causally coherent direction. The improvement of 13.6 log-likelihood units is substantial and suggests that the constrained model is genuinely a better fit to the data’s dependency structure, not merely a reorientation of the same skeleton. The constrained graph has a natural interpretation in terms of automobile design decisions made over the study period. modelyear → mpg captures the historical trend of improving fuel economy over time, driven by regulatory pressure (particularly U.S. CAFE standards introduced in 1975) and rising fuel prices. modelyear → weight reflects the contemporaneous trend toward lighter vehicles. 

origin → displacement and origin → mpg reflect systematic differences in engineering philosophy across American, European, and Japanese manufacturers, with the latter producing systematically smaller, more fuel-efficient vehicles throughout this period. The edge mpg → weight warrants specific discussion. One interpretation is that it reflects a design priority: engineers targeting a higher fuel economy specification reduce vehicle weight as the primary lever available to them, making weight downstream of the mpg target in the design process. 1

An alternative interpretation is that the displacement → mpg edge present in graph (a) has been removed in graph (b) possibly due to the rerouting of explanatory power through modelyear , and that the true underlying structure may include displacement as an additional direct cause of mpg ,which would leave mpg – weight unoriented in the CPDAG. Distinguishing these interpretations would require either additional domain knowledge or a dedicated sensitivity analysis. 

> 1Thanks to Peter Spirtes for pointing out this interpretation.

14 mpg     

> cylinders
> displacement
> horsepower
> weight
> acceleration
> modelyear
> origin
> (a) No background knowledge Score: −631 .88
> mpg
> cylinders
> displacement
> horsepower
> weight
> acceleration
> modelyear
> origin
> (b) modelyear exogenous Score: −618 .26

Figure 1: CPDAGs returned by BOSS + FFML on the Auto MPG dataset ( n = 392, one dis-crete variable: origin ). Directed edges ( →) indicate orientations determined from the data or background knowledge; undirected edges (—) indicate Markov-equivalent pairs that cannot be ori-ented from the data alone. (a) No background knowledge; modelyear is treated as endogenous and oriented as a consequence of mpg and weight , which is causally implausible. (b) modelyear 

constrained to be exogenous via a prior knowledge tier; the resulting graph is both causally more plausible and achieves a higher FFML score ( −618 .26 vs. −631 .88), indicating that the constrained search found a better region of the score landscape. The one remaining undirected edge in graph (b), cylinders — horsepower , reflects Markov equivalence: both orientations are consistent with the observed conditional independence structure, and neither can be ruled out from the data alone within a CPDAG model. 2

# 13 Conclusion 

We have presented FFML and FFCI, two complementary methods for nonlinear causal discovery in mixed continuous–discrete data. FFML is a scalable approximation to the exact GP marginal likelihood, replacing the n×n Gram matrix with a finite-dimensional RFF representation while retaining the probabilistic interpretation and automatic Occam-factor complexity penalty of the exact score. It handles mixed parent sets through a product-kernel construction, with a Kronecker feature-space path for small discrete parent sets and a direct n × n Hadamard-product kernel path when the discrete parent set is larger. Bandwidth selection is stabilized via a grid search coupled to the target variable, and feature coupling by target ensures stable score differences across parent sets. FFCI is a fast nonparametric CI test that featurizes each variable individually, concatenating continuous RFF/ORF features with discrete categorical features, and measures residual cross-covariance after ridge residualization in feature space. It supports four p-value approximation methods; the default gamma approximation produces well-calibrated p-values under the null. When   

> 2FFML is not score-equivalent (Chickering [2002])—that is, it does not in general assign equal scores to all DAGs in the same Markov equivalence class—so running BOSS in DAG mode rather than CPDAG mode can reliably orient some edges that remain ambiguous in the CPDAG. In this case, BOSS + FFML in DAG mode orients this edge as
> cylinders →horsepower , which is causally plausible: the number of cylinders is a design decision that constrains the horsepower an engine can produce.

15 run through the same PC-Max implementation, FFCI and RCIT exhibit complementary precision– recall profiles: RCIT achieves higher precision while FFCI achieves better recall and lower SHD, and runs in approximately one third the time. Although FFML and FFCI share the same RFF/ORF kernel approximation philosophy and ridge regularization strategy, they differ architecturally: FFML builds a joint kernel over a parent set for scoring, while FFCI featurizes variables individually for testing. Together they form a practical and coherent toolkit for score-based, constraint-based, and hybrid causal discovery at scale. 

# References 

Bryan Andrews, Joseph Ramsey, Ruben Sanchez Romero, Jazmin Camchong, and Erich Kummer-feld. Fast scalable and accurate discovery of dags using the best order score search and grow shrink trees. Advances in neural information processing systems , 36:63945–63956, 2023. David Maxwell Chickering. Optimal structure identification with greedy search. Journal of machine learning research , 3(Nov):507–554, 2002. Biwei Huang, Kun Zhang, Yizhu Lin, Bernhard Sch¨ olkopf, and Clark Glymour. Generalized score functions for causal discovery. In Proceedings of the 24th ACM SIGKDD international conference on knowledge discovery & data mining , pages 1551–1560, 2018. R. Quinlan. Auto MPG. UCI Machine Learning Repository, 1993. DOI: https://doi.org/10.24432/C5859H. Ali Rahimi and Benjamin Recht. Random features for large-scale kernel machines. In Advances in Neural Information Processing Systems , volume 20, 2007. Joseph Ramsey and Bryan Andrews. Py-tetrad and rpy-tetrad: A new python interface with r support for tetrad causal search. In Causal Analysis Workshop Series , pages 40–51. PMLR, 2023. Joseph Ramsey, Madelyn Glymour, Ruben Sanchez-Romero, and Clark Glymour. A million vari-ables and more: the fast greedy equivalence search algorithm for continuous variables and its extensions. Journal of Machine Learning Research , 18(1):1–47, 2017. Peter Spirtes, Clark N Glymour, and Richard Scheines. Causation, prediction, and search . MIT press, 2000. Eric V. Strobl, Kun Zhang, and Shyam Visweswaran. Approximate kernel-based conditional inde-pendence tests for fast non-parametric causal discovery. Journal of Causal Inference , 7(1), 2019. doi: 10.1515/jci-2018-0017. Felix Yu, Ananda Theertha Suresh, Krzysztof Choromanski, Daniel Holtmann-Rice, and Sanjiv Kumar. Orthogonal random features. In Advances in Neural Information Processing Systems ,volume 29, 2016. Kun Zhang, Jonas Peters, Dominik Janzing, and Bernhard Sch¨ olkopf. Kernel-based conditional independence test and application in causal discovery. In Proceedings of the 27th Conference on Uncertainty in Artificial Intelligence (UAI) , pages 804–813, 2012. 16 Yujia Zheng, Biwei Huang, Wei Chen, Joseph Ramsey, Mingming Gong, Ruichu Cai, Shohei Shimizu, Peter Spirtes, and Kun Zhang. Causal-learn: Causal discovery in python. Journal of Machine Learning Research , 25(60):1–8, 2024. 17